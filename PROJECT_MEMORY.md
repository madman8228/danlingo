## 2026-03-26 (Move imported-word browse entry into Courses tab)
- What changed:
  - Added a fixed course card entry in duoxx/app/(tabs)/courses.tsx (shown in ll category) for 导入词汇浏览课.
  - Updated course click handling to route imported-word-course directly to /imported-word-course.
  - Adjusted empty-state condition so ll category does not show empty copy when this fixed browse card is visible.
- What was learned:
  - Users expect discoverability from the Courses tab first; placing this only on Home tool cards is easy to miss.
- Next steps / open questions:
  - Decide whether to also expose this as a dynamic backend course item instead of a fixed local card.
## 2026-03-26 (Hotfix: tabs home unterminated template compile error)
- What changed:
  - Revalidated and repaired malformed string section around ocabSubtitle / swipeLearningCard in duoxx/app/(tabs)/index.tsx so Metro parser no longer reports Unterminated template.
- What was learned:
  - A partially corrupted line in this area can survive visually as mojibake and cause immediate compile-stop before runtime.
- Next steps / open questions:
  - If local red screen persists after code fix, clear Expo/Metro cache and reload.
## 2026-03-26 (Imported-word browse learning course, no exercises)
- What changed:
  - Added duoxx/app/imported-word-course.tsx for browse-only learning over imported lexical entries (search + prev/next + senses/examples/facets), without loading exercises.
  - Added listLearningEntries() + LexicalLearningEntry in duoxx/src/storage/lexicalQuizReviewStorage.ts.
  - Added listLexicalLearningEntries() in duoxx/src/services/pipelineApi.ts (backend-first with local fallback).
  - Added home tool-card entry in duoxx/app/(tabs)/index.tsx pointing to /imported-word-course.
  - Added storage test coverage in duoxx/src/storage/__tests__/lexicalQuizReviewStorage.test.ts for browse listing + latest-import dedupe.
- What was learned:
  - Imported lexical assets can power a standalone learner browse flow directly, without coupling to quiz-generation lifecycle.
  - Duplicate headwords across imports need deterministic latest-wins behavior for learner consistency.
- Next steps / open questions:
  - Decide whether this browse course should also be listed in learning-modules module registry.
  - If backend exposes richer learner lexical APIs later, switch this page to backend contract first and keep local fallback for offline/dev.
## 2026-03-20 (Mongo lexical quiz option migration + distractor hardening)
- What changed:
  - Added backend helper `\\wsl.localhost\Ubuntu-22.04\home\rookie\project\duoxx_server\src\services\lexicalQuizOptionRules.js` and test `...\lexicalQuizOptionRules.test.js` to normalize translation-choice options with deterministic rotation and Chinese-translation-only distractors.
  - Added backend migration script `\\wsl.localhost\Ubuntu-22.04\home\rookie\project\duoxx_server\scripts\migrate-lexical-quiz-options-v1.js` plus package script `migrate:lexical-quiz-options-v1` for dry-run/apply rewriting of legacy `translation_choice / word_card` options in `SeedQuiz` and `GeneratedQuizVersion`.
  - Kept Turing's distractor-quality upgrade in place for current generation/local fallback: same POS first, then closer difficulty, then overlapping scene tags; `true/false` and non-Chinese answers are excluded.
  - Removed the accidental local fake `D:\06-project\expo_duo\duoxx_server` directory after copying the real helper/test into the actual backend workspace.
- What was learned:
  - The real backend lives under the WSL workspace; writing to a same-named local Windows folder silently creates dead files that the server never uses.
  - Old lexical quiz rows can now be normalized safely in dry-run mode before any write, which is the right guardrail for production Mongo cleanup.
  - Current lexical asset data does not reliably carry difficulty/scene metadata yet, so the new distractor scoring must treat those fields as optional and degrade gracefully back to POS-first matching.
- Next steps / open questions:
  - Run the migration with `--apply` in the actual target environment after reviewing the dry-run output.
  - Decide whether to extract the shared distractor rules into one cross-runtime source or keep the server/helper and frontend fallback implementations separate.
## 2026-03-20 (Exercise Review approve-and-activate simplification)
- What changed:
  - Updated duoxx/components/admin/LexicalQuizReviewPanel.tsx so the main review action is now ��׼����Ч, internally sequencing approve then activate.
  - Removed the separate visible activation button for non-active questions and replaced the active-state button with a passive ��ǰ�� badge.
  - Updated duoxx/src/config/adminLexicalQuizReview.ts to match the simplified operator wording.
- What was learned:
  - The user objection was not about backend traceability; it was about exposing a two-step workflow for a single operator intent.
  - Folding the two calls into one visible action keeps the existing backend behavior while removing unnecessary operator friction.
- Next steps / open questions:
  - If the learning-side active quiz consumption is wired next, verify that ��׼����Ч is enough and no explicit re-activation control is still needed in normal operations.
## 2026-03-20 (Exercise Review semantic simplification)
- What changed:
  - Updated duoxx/components/admin/LexicalQuizReviewPanel.tsx to group review data by sourceSenseKey instead of listing raw quiz items as the primary left-hand unit.
  - Reframed the operator UI so it no longer exposes ������ / ��̬�� / ��ǰ�汾; the visible model is now ���� -> �������µ�ȫ����ϰ��.
  - Replaced the generic generation wording with explicit actions: ����ѡ���� and �����ж���.
  - Tightened the left-hand review list density so grouped sense rows consume less vertical space.
  - Updated duoxx/src/config/adminLexicalQuizReview.ts so review labels and action copy match the simplified operator model.
- What was learned:
  - The operator problem was not backend versioning itself; it was leaking internal lifecycle concepts into the UI.
  - Keeping versioning in the data layer while hiding it from operators is the pragmatic middle ground for now.
- Next steps / open questions:
  - Verify in-browser whether ��Ϊ��Ч�� should remain a separate action or be merged into approve.
  - The import review panel still has separate historical encoding cleanup work outside this Exercise Review simplification.
## 2026-03-20 (Import panel tab-switch persistence)
- What changed:
  - Added `duoxx/src/storage/lexicalAssetImportSessionStorage.ts` to persist the `Import & Review` session state locally.
  - Updated `duoxx/components/admin/LexicalAssetImportPanel.tsx` to hydrate/persist `fileName`, `fileType`, `review`, `selectedEntryKey`, `status`, `entryQuery`, and `entryFilter` across tab unmount/remount.
  - Added a `��ջỰ` action for explicitly clearing the local import session.
- What was learned:
  - The import panel loses state because `OperatorWorkbench` conditionally unmounts the tab content when switching to `Exercise Review`.
  - The correct fix within the requested scope is local session persistence, not changing quiz APIs or keeping the panel mounted globally.
- Next steps / open questions:
  - If future UX requires the selected file to survive app restarts, consider promoting this session storage into a more explicit import draft model.
  - Existing quiz/template data can still be stale independently of this UI session state.
## 2026-03-20 (Exercise Review panel UTF-8 repair)
- What changed:
  - Rewrote `duoxx/components/admin/LexicalQuizReviewPanel.tsx` as a clean UTF-8 file to eliminate mojibake in the `Exercise Review` tab UI.
  - Added `duoxx/src/config/adminLexicalQuizReview.ts` to hold all panel copy and status labels with explicit Unicode escapes.
  - Kept the scope limited to admin UI labels/display and did not touch backend routes or import logic.
- What was learned:
  - `LexicalQuizReviewPanel.tsx` had an actual source-encoding problem, not just stale data text.
  - Moving display strings into a UTF-8-safe config file avoids the panel re-acquiring encoding corruption from direct Chinese literals.
- Next steps / open questions:
  - Existing stored quiz records may still contain old garbled content from earlier imports; that requires data re-import or storage cleanup, not this UI-only fix.
  - If the tab still looks wrong after refresh, check whether Expo cache or stored seed data is the remaining source rather than the panel source itself.
## 2026-03-20 (Mongo-backed lexical quiz review chain)
- What changed:
  - Added backend Mongo models in `duoxx_server` for lexical quiz lifecycle:
    - `src/models/LexicalAsset.js`
    - `src/models/SeedQuiz.js`
    - `src/models/GeneratedQuizVersion.js`
    - `src/models/ActiveQuizBinding.js`
  - Exported the new models through `duoxx_server/src/models/index.js`.
  - Added backend lexical quiz service `duoxx_server/src/services/lexicalQuizService.js` implementing lexical asset import, seed quiz persistence, generated quiz versioning, review-state updates, activation, and active quiz reads.
  - Added pipeline routes in `duoxx_server/src/routes/pipeline.js` for seed import, list, generate, approve, reject, activate, and active quiz listing.
  - Added learning-side read endpoint `GET /api/learning/lexical-quizzes/active` in `duoxx_server/src/routes/learning.js`.
  - Repaired a pre-existing broken string literal in `duoxx_server/src/routes/learning.js` (`�п�` / `�ճ�`) so the route file can load again.
  - Updated `duoxx/src/services/pipelineApi.ts` so lexical quiz review methods are backend-first with local AsyncStorage fallback.
  - Deleted unused V4 lexical import files from Expo app:
    - `duoxx/src/services/lexicalAssetImport.ts`
    - `duoxx/src/config/adminLexicalAssetV4.ts`
    - `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`
- What was learned:
  - `learning.js` had a real syntax break caused by historical encoding corruption; editing the file surfaced it and it had to be fixed before route load checks could pass.
  - The lexical quiz review chain can be moved to Mongo without changing operator UX if `pipelineApi` stays backend-first with local fallback.
  - The old V4 lexical import path was already dead at runtime; only service/config/test remnants were left.
- Next steps / open questions:
  - Wire the actual lesson/learning UI to `GET /api/learning/lexical-quizzes/active`.
  - Decide when to remove the frontend lexical quiz local fallback after backend smoke testing.
  - Remove the remaining non-lexical legacy candidate review code once the Mongo-backed lexical quiz path is stable.
## 2026-03-23 (Exercise Review duplicate judgment-question dedupe)
- What changed:
  - Updated `duoxx_server/src/services/lexicalQuizService.js` so review-list loading deduplicates identical quizzes by semantic signature before the operator UI sees them.
  - Added generation-side duplicate suppression so `translation_choice` / `true_false` generation skips writing a new row when the visible question would match an existing seed or generated quiz.
  - Added backend regression coverage in `duoxx_server/src/services/__tests__/lexicalQuizService.test.js` for both review dedupe and duplicate-skip generation.
- What was learned:
  - The duplicate judgment questions were not a renderer problem; the service layer was returning both seed and generated rows for the same visible question.
  - Preventing duplicates at generation time is necessary, but review-list dedupe is still useful for historical Mongo data that already contains duplicates.
- Next steps / open questions:
  - Smoke-test one legacy course in the learner after the backend change.
  - If historical Mongo duplication keeps surfacing in old courses, add a one-time cleanup migration keyed by the same semantic signature.
# Project Memory

## 2026-03-03
- What changed:
  - Verified Pencil MCP connectivity from current Codex session.
  - Created a redesigned web admin wireframe in Pencil (`pencil-new.pen`) with a full-width layout:
    - top header/session strip
    - middle 3-column workspace (jobs / control / log+published)
    - bottom full-width editor zone
  - Added structural labels in the wireframe for each region to support review before implementation.
  - Updated the top-right area of the wireframe to include a user login module:
    - account input placeholder
    - password input placeholder
    - login/logout action buttons
    - current user state text
  - Refined the wireframe again per feedback:
    - replaced the complex top-right auth block with a single login entry trigger
    - added a separate login dialog popup draft with fields:
      - username
      - phone
      - email
      - password
    - moved/removed previous `Current Account / Logout / State` wording from the session strip, indicating account actions are now top-right-triggered.
  - Added a new independent V3 wireframe (content-review-first) in `pencil-new.pen`:
    - new board: `Pipeline V3 - Content Review First` (`nodeId: 16quY`)
    - focus shifted from job-first to question quality review flow:
      - source material selection and stage bar
      - large central visual question review canvas
      - right-side question CRUD tuning panel
      - bottom validation report + approve/import action
    - preserved top-right login entry concept in the V3 header.
  - Implemented the confirmed V3 redesign in backend admin page files:
    - `\\wsl.localhost\Ubuntu-22.04\home\rookie\project\duoxx_server\src\public\pipeline-admin.html`
    - `\\wsl.localhost\Ubuntu-22.04\home\rookie\project\duoxx_server\src\public\pipeline-admin.js`
  - New page behavior now prioritizes ?????????????????
    - visual Unit/Lesson/Exercise browsing
    - question-level CRUD (`???` / `???` / `???` / `??????`)
    - draft save + server revalidation
    - checklist review + approve/reject + publish
  - Reworked auth interaction:
    - top-right single login entry
    - click opens login modal (username/phone/email/password fields)
    - current account and logout moved to top-right area
    - no manual token input exposed
  - Added explicit status board + bottom operation logs so state and actions are visible.
  - Extended the pipeline editor to support structure-level editing (not only question-level):
    - Unit actions: add, delete, move up/down, rename
    - Lesson actions: add, delete, move up/down, rename
    - Exercise actions: move up/down in addition to existing add/duplicate/delete/edit
  - Updated the page markup and JS wiring so these controls are available in the central review workspace.
  - Rebuilt `pipeline-admin.js` cleanly after a failed regex patch attempt corrupted the file, then validated in browser.
  - Simplified the ??) ??????????section to reduce operator confusion:
    - removed visible API origin input from the page
    - added explicit file flow: `??????` -> upload to backend -> show uploaded path/status
    - changed main action wording to `??????? to clearly indicate ??reate+run pipeline??  - Added backend upload capability for source files:
    - new route: `POST /api/pipeline/source-file` (raw octet-stream, 50MB)
    - new service method: `saveUploadedSource(...)`
    - uploaded files are persisted under `uploads/pipeline-sources/`
  - Added frontend upload/process orchestration in `pipeline-admin.js`:
    - file picker + upload status rendering
    - `startProcessing()` now creates job when needed, runs pipeline, then auto loads structured draft to UI
  - Fixed multiple malformed legacy strings in `contentPipelineService.js` that caused syntax-check failures.
  - Fixed auth-expired UX for file upload:
    - when API returns `401` / invalid token, frontend now auto-clears stale credentials
    - auth state switches to ????????????????????and login modal opens automatically
    - clicking `??????` now checks login first and redirects to login if unauthenticated
  - Reduced repetitive auth UI in header:
    - removed duplicate textual auth line (`???????????) from top-right
    - introduced a single `??????? badge as the canonical state indicator
    - when unauthenticated, hide `??????` and `???? controls, keep only `???`
  - Improved initial-load behavior:
    - auto login-modal pop-up now happens only for expired cached token cases
    - plain unauthenticated state no longer auto-opens modal; API refresh errors surface as `??????`
  - Further simplified top-right auth area per UX feedback:
    - removed credential validity badge and account text from UI
    - unauthenticated state now shows only a single `???` button
    - authenticated state now shows only a default avatar icon button
    - clicking avatar opens a logout confirmation action
  - Updated layout container to use full available page width (removed fixed max width) to better utilize desktop screens.
  - Fixed missing avatar issue after login:
    - root cause: avatar was initialized with hidden class behavior that prevented proper reveal
    - now login success shows avatar reliably
  - Added avatar menu interactions:
    - avatar click toggles user menu
    - menu includes `??????` and `????
    - outside click auto-closes menu
- What was learned:
  - Pencil tools are available in-session and can be used to draft UI structure before coding.
  - The wireframe is intentionally low-fidelity and optimized for layout confirmation, not final visual styling.
  - The pipeline draft payload can be edited safely at `units[].lessons[].exercises[]` granularity and persisted via `PATCH /api/pipeline/jobs/:jobId/draft`.
  - Browser verification for `/admin/pipeline` now shows the new Unit/Lesson CRUD controls and working status/log refresh behavior.
  - A clear single-click production entry is essential; ??????????is more understandable than ????????????????split actions.
  - Stale local token can create a false ??ogged-in??impression; explicit 401 recovery handling is required for predictable operator workflow.
- Next steps / open questions:
  - Validate the upload + start-processing flow end-to-end on real files (pdf/doc/docx/txt/md), especially larger files near route size limits.
  - Consider adding resumable/chunk upload if source files may regularly exceed the current 50MB API limit.
  - Consider adding drag-and-drop sorting for Unit/Lesson/Exercise if keyboard/button sorting feels slow.

## 2026-03-03 (Autopilot persistence)
- What changed:
  - Added repo-level closed-loop execution protocol to `AGENTS.md` under `Closed-Loop Autopilot Protocol (Default)`.
  - Added new process document `AUTOPILOT_WORKFLOW.md` with intake -> parallel decomposition -> implementation -> verification -> delivery DoD.
  - Confirmed process memory strategy: keep behavior in files, not only in session context.
- What was learned:
  - Long-term repeatability needs explicit repo instructions; relying on transient chat context is not enough.
  - A short DoD and verification gate reduces partial delivery risk.
- Next steps / open questions:
  - Optionally add command templates per subproject (Expo app vs duoxx_server) into `AUTOPILOT_WORKFLOW.md`.

## 2026-03-04
- What changed:
  - Upgraded `duoxx_server` pipeline extraction to support scanned PDF documents via OCR fallback:
    - Added `src/pipeline/utils/pdfOcr.ts` using `pdf-to-png-converter` + `tesseract.js` + local `eng/chi_sim` language data.
    - Updated `src/pipeline/agents/ingestAgent.ts` to auto-switch from `pdf-parse` to OCR when direct text extraction is empty/weak.
    - Default OCR coverage now processes up to 30 pages (`PIPELINE_OCR_MAX_PAGES` override supported).
  - Added vocabulary-focused structuring for English word-list documents:
    - `src/pipeline/agents/structureAgent.ts` now detects vocabulary lines and generates `vocabulary_card` exercises with metadata (word/phonetic/translation/example/sourceSection).
    - `src/pipeline/types.ts` extended with `vocabulary_card` type and exercise metadata fields.
    - `src/pipeline/agents/domainEnglishAgent.ts` now validates vocabulary-card required fields.
  - Upgraded admin UI to inspect/edit vocabulary cards:
    - `src/public/pipeline-admin.html` added `vocabulary_card` type and dedicated vocab editor panel.
    - `src/public/pipeline-admin.js` renders vocab details in list and supports editing word/phonetic/translation/example fields.
  - Added OCR/runtime dependencies in `duoxx_server/package.json` and refreshed lockfile.
- What was learned:
  - The uploaded `15????????500??pdf` is scan-image based; `pdf-parse` alone returns mostly empty text.
  - OCR fallback is necessary for Chinese/English mixed vocabulary sheets; full-run extraction produced ~1526 vocabulary cards from the sample file.
- Next steps / open questions:
  - Consider adding async job progress updates for long OCR runs (~2+ minutes on multi-page files).
  - Improve vocabulary parser accuracy for broken OCR lines and multi-column edge cases.

## 2026-03-04 (runtime stability fix)
- What changed:
  - Fixed backend startup crash caused by OCR native optional dependency loading too early.
  - Refactored `src/pipeline/utils/pdfOcr.ts` to lazy-load `pdf-to-png-converter` and `tesseract.js` only when OCR fallback is actually used.
  - Updated `src/pipeline/agents/ingestAgent.ts` to treat OCR as best-effort fallback and avoid crashing extraction flow when OCR runtime is temporarily unavailable.
  - Updated orchestrator load error text in `src/services/contentPipelineService.js` to include both `npm i` and `npm run build:pipeline`.
  - Re-ran dependency install in WSL and rebuilt pipeline to regenerate `dist-pipeline`.
- What was learned:
  - Importing OCR stack at module top-level can crash service boot when optional native bindings are missing/corrupted, even before any job runs.
  - Lazy loading + graceful fallback keeps API startup stable while still supporting OCR when environment is healthy.
- Next steps / open questions:
  - Add explicit health check endpoint for OCR runtime (`available/unavailable` + reason) so UI can warn before users start long jobs.

## 2026-03-04 (login modal redesign)
- What changed:
  - Updated admin pipeline login popup UI in `duoxx_server/src/public/pipeline-admin.html`:
    - removed email input field
    - redesigned modal with banner + structured form + action footer
    - kept username as optional display nickname and phone/password as primary credentials
  - Updated login logic in `duoxx_server/src/public/pipeline-admin.js`:
    - removed `loginEmail` DOM dependency
    - changed validation to require `phone + password`
    - changed auth request body to `{ phone, password }`
- What was learned:
  - Email login entry was unnecessary for current operator workflow and added UI noise.
  - A focused phone/password modal is clearer and reduces input ambiguity.
- Next steps / open questions:
  - If later needed, add an explicit ????????? / ????????toggle rather than showing both by default.

## 2026-03-04 (structured data direct-to-course flow)
- What changed:
  - Added structured source mode support across backend pipeline:
    - `src/pipeline/types.ts`: added `SourceInputMode` and CSV format support.
    - `src/pipeline/agents/ingestAgent.ts`: supports `.csv` and stores `inputMode`.
    - `src/models/ContentPipelineJob.js`: added `source.mode` (`auto` / `structured`) and CSV format enum.
    - `src/services/contentPipelineService.js`: accepts `sourceMode`, persists it, and passes mode into orchestrator run input.
    - `src/routes/pipeline.js`: accepts `sourceMode` in `POST /jobs`.
  - Implemented domain-focused structured curriculum generation:
    - Added `src/pipeline/agents/structuredDataTeacherAgent.ts`.
    - Agent parses structured `.csv`, markdown tables in `.md`, and delimited/list-style `.txt`.
    - Generates review-ready draft courses with:
      - vocabulary cards (`vocabulary_card`)
      - teacher-designed translation questions
      - teacher-designed multiple-choice questions
    - Integrated this agent into `src/pipeline/agents/structureAgent.ts` before generic fallback extraction.
  - Updated admin page UX (`src/public/pipeline-admin.html`, `src/public/pipeline-admin.js`):
    - new button: `??????????? (for `.csv/.md/.txt`)
    - mode badge: `???: ?????? / ????????
    - selected mode is sent to backend on job creation.
- What was learned:
  - A dedicated structured mode significantly improves predictable conversion from tabular/list data to reviewable courses.
  - Keeping the generated draft in the existing review UI enables immediate human QA without extra pages.
- Next steps / open questions:
  - Add progress indicator per stage for long structured imports with large row counts.
  - Optionally add downloadable parse report (rows accepted/rejected + reasons) to help operators fix source files quickly.

## 2026-03-04 (CSV-first UX simplification)
- What changed:
  - Simplified `/admin/pipeline` flow from task-centric to course-design-centric:
    - removed visible historical task table from page
    - replaced "current task" controls with a single processing state line + `???????
    - added explicit hierarchy panel that renders `Unit -> Lesson -> Exercise` relationships for loaded draft
    - added serial processing guard (`processing` lock + button disabled while running)
  - Added structured-mode entry and mode badge behavior:
    - `??????` for generic source
    - `??????????? for `.csv/.md/.txt`
    - mode persisted into job creation payload (`sourceMode`)
  - Upgraded structured CSV parser to support typed rows:
    - supports headers like `type,category,content,options,answer,explanation,example,definition`
    - maps to mixed exercise types (`multiple_choice`, `fill_blank`, `translation`, `vocabulary_card`)
    - still falls back to vocabulary-style parsing for other structured CSV shapes
  - Verified with sample typed CSV that mixed types are generated and grouped into lessons by category.
- What was learned:
  - Showing internal job history in the main production screen causes operator confusion; direct status + hierarchy is clearer.
  - CSV type-driven generation provides predictable course composition and better matches operator expectations.
- Next steps / open questions:
  - Add an optional "advanced/history" drawer for debugging instead of exposing tasks on the main screen.
  - Add a structured parse report panel (accepted/rejected rows and reasons) for CSV quality debugging.

## 2026-03-05 (pipeline UX overhaul + mixed CSV vocabulary detection)
- What changed:
  - Redesigned `duoxx_server/src/public/pipeline-admin.html` for full-width web usage:
    - removed the empty third grid column and switched to a 2-column workspace that fills screen width
    - promoted the primary review flow to: `Lesson ?? -> ???? -> ????`
    - moved Unit/Lesson structural operations into a secondary "??????" area to reduce visual noise
    - kept login/avatar, upload, processing, review, publish, and logs in a clearer information hierarchy
  - Updated `duoxx_server/src/public/pipeline-admin.js`:
    - added flattened cross-unit lesson navigator (`lesson-flat`) so clicking any lesson immediately loads its exercises
    - improved course meta display with current lesson and exercise count
  - Enhanced structured vocabulary extraction in `duoxx_server/src/pipeline/agents/structuredDataTeacherAgent.ts`:
    - added mixed CSV parsing for files where header is structured but rows are mostly `word [phonetic] translation`
    - parser now checks answer/content/cell-level candidates and converts to `vocabulary_card` exercises
    - CSV flow now merges standard CSV vocab parsing + mixed CSV vocab parsing + line-style parsing before fallback
    - fixed malformed regex literals and delimiter splitting to keep TypeScript build stable
- What was learned:
  - Operators understand content faster when the UI centers on lesson and exercise relationships instead of internal task abstractions.
  - Real-world CSV data is often semi-structured; robust candidate-based parsing is required to reliably convert vocabulary lists into lessons.
- Next steps / open questions:
  - Optionally add a compact lesson filter/search for very large vocabulary sets.
  - Add a parse diagnostics panel (accepted/rejected lines + reason) to make source data correction easier.

## 2026-03-06 (pipeline redesign PRD alignment)
- What changed:
  - Added PRD draft for full redesign of `/admin/pipeline` in:
    - `duoxx_server/docs/pipeline-redesign/PRD-v1-course-production-web.md`
  - Locked V1 scope with user:
    - no review flow
    - operator can write database
    - paginated DB course loading
    - single upload entry + auto data-type recognition
    - English-only for this version
    - vocabulary material generates one lesson with all word cards
- What was learned:
  - The page must prioritize a content-production path rather than exposing internal task complexity.
  - Vocabulary workflow must be deterministic and editable at record granularity.
- Next steps / open questions:
  - Draft technical specification (state machine, API contract, module boundaries).
  - Break implementation into milestones and test cases before coding.

## 2026-03-06 (pipeline redesign technical specification)
- What changed:
  - Added technical spec for V1 pipeline redesign:
    - `duoxx_server/docs/pipeline-redesign/SPEC-v1-course-production-web.md`
  - Defined Agent Team architecture and lifecycle:
    - runtime: create per "start processing" request
    - skills: configuration-time reusable rule assets
  - Defined V1 interface strategy:
    - keep existing `/jobs` internals
    - add simplified facade API (`/process`, paginated courses, write-db upsert)
  - Defined page state machine, permission matrix, vocabulary-card CRUD model, and error code contract.
- What was learned:
  - User expects deterministic and simple runtime behavior: agents are runtime workers, skills are static capability packs.
  - Frontend must consume a simplified process API to hide orchestration complexity.
- Next steps / open questions:
  - Split implementation into milestones and test plans before coding.

## 2026-03-06 (pipeline redesign implementation plan)
- What changed:
  - Added execution task breakdown:
    - `duoxx_server/docs/pipeline-redesign/IMPLEMENTATION-PLAN-v1.md`
  - Defined milestones and dependencies:
    - M1 facade API
    - M2 runtime Agent Team
    - M3 web workspace redesign
    - M4 write-db + pagination integration
    - M5 testing and UAT
  - Added parallel lane strategy (backend/frontend/test) with merge point at integration phase.
- What was learned:
  - A milestone-first approach is required to avoid rework from UI-first implementation.
  - Facade API is the key decoupling layer so frontend no longer depends on internal job semantics.
- Next steps / open questions:
  - Start M1 implementation (new process/courses/write endpoints).
  - Freeze payload schema for vocabulary card fields before coding CRUD forms.

## 2026-03-06 (M1 facade API implementation)
- What changed:
  - Implemented pipeline facade endpoints in backend route:
    - `POST /api/pipeline/process`
    - `GET /api/pipeline/courses`
    - `GET /api/pipeline/courses/:courseId`
    - `POST /api/pipeline/courses/write`
  - Added new service methods in `contentPipelineService`:
    - `processSource`
    - `listCourses`
    - `getCourse`
    - `writeCourse` (create/update + source job linkage)
  - Added helper functions:
    - process status mapping
    - draft statistics collection
    - source type inference
    - source job id resolution from payload
- What was learned:
  - Facade API can hide existing `/jobs` complexity while preserving backward compatibility.
  - Write-db upsert requires reliable `sourceJobId` resolution from either explicit payload or `draft.sourceDocument.documentId`.
- Validation:
  - `node --check src/services/contentPipelineService.js`
  - `node --check src/routes/pipeline.js`
  - `npm run build:pipeline`
- Next steps / open questions:
  - M2: finalize runtime Agent Team chain for deterministic vocabulary-first generation.
  - M3: migrate frontend to call facade API and simplify UI state machine.

## 2026-03-06 (M2+M3: vocabulary single-lesson + facade frontend migration)
- What changed:
  - Backend curriculum generation updates:
    - `src/pipeline/types.ts`: expanded vocabulary metadata schema with `synonyms`, `antonyms`, `partOfSpeech`.
    - `src/pipeline/agents/structuredDataTeacherAgent.ts`: vocabulary structured output now enforces **single lesson** (`????`) containing all parsed words.
    - `src/pipeline/agents/structureAgent.ts`: fallback vocabulary extraction also produces single-lesson vocabulary draft and includes new lexical metadata fields.
    - `src/services/contentPipelineService.js`: process facade now returns `source.detectedDomain` inferred from generated draft.
  - Frontend pipeline workspace redesign (M3) migrated to facade APIs:
    - `src/public/pipeline-admin.html`: replaced review/publish-oriented layout with production flow layout:
      - single upload entry
      - start processing
      - write database
      - paginated DB courses panel
      - lesson/exercise editor with vocabulary lexical fields
    - `src/public/pipeline-admin.js`: rewritten logic to use:
      - `POST /api/pipeline/process`
      - `GET /api/pipeline/courses`
      - `GET /api/pipeline/courses/:courseId`
      - `POST /api/pipeline/courses/write`
      - removed dependency on job/review/publish UI flow.
- What was learned:
  - Enforcing single-lesson vocabulary output removes ambiguity for operators and aligns with expected learning-card workflow.
  - Facade API migration significantly simplifies front-end state and user cognition compared with job-centric controls.
- Validation:
  - `node --check src/public/pipeline-admin.js`
  - `node --check src/services/contentPipelineService.js`
  - `node --check src/routes/pipeline.js`
  - `npm run build:pipeline`
  - smoke checks:
    - `GET /admin/pipeline` => 200
    - page contains `????` and `????`
    - served JS contains `/process` and `/courses/write`
  - vocabulary fixture simulation confirms:
    - lessonCount = 1
    - lesson title = `????`
    - exercise type = `vocabulary_card`
    - metadata includes lexical fields (`synonyms`,`antonyms`,`partOfSpeech`).
- Next steps / open questions:
  - Add role policy middleware for explicit admin/operator permission matrix (currently inherited auth-only access).
  - Add API-level payload validation for vocabulary metadata arrays and max length constraints.
  - Optional: add "diff vs server draft" indicator before write-db.

## 2026-03-06 (M5 partial: runtime fixes + automated tests)
- What changed:
  - Fixed runtime defects in `duoxx_server/src/public/pipeline-admin.js`:
    - added missing `normalizeList(...)` helper used by vocabulary metadata rendering
    - added missing `closeUserMenu()` helper for avatar menu close behavior
    - added missing `showProfileInfo()` handler for the profile menu action
  - Added automated tests for facade workflow and vocabulary generation rules:
    - `duoxx_server/src/services/contentPipelineService.test.js`
      - covers `processSource` status/type/domain mapping
      - covers `writeCourse` create flow (source job linkage + publish status update)
      - covers `writeCourse` update flow (version increment)
      - covers create failure when `sourceJobId` is absent
    - `duoxx_server/src/pipeline/agents/structuredDataTeacherAgent.test.js`
      - validates mixed CSV vocabulary input generates one lesson (`????`)
      - validates generated exercises are `vocabulary_card`
      - validates lexical metadata keys (`synonyms`, `antonyms`, `partOfSpeech`) exist
- What was learned:
  - The compressed admin JS introduced silent missing helper references that are only exposed by interaction paths; explicit helper smoke checks are needed.
  - Vocabulary domain inference in service uses a hard threshold (`>= 6` vocab cards), so tests must align with this rule.
- Validation:
  - `node --check src/public/pipeline-admin.js`
  - `node --check src/routes/pipeline.js`
  - `node --check src/services/contentPipelineService.js`
  - `npm run build:pipeline`
  - `NODE_ENV=test node ./node_modules/jest/bin/jest.js`
    - 2 suites passed, 5 tests passed
- Next steps / open questions:
  - Decide whether to keep old `/jobs` UI actions hidden-only or remove dead frontend code paths entirely.
  - Add API payload validators for vocabulary card field limits and list length constraints before production freeze.

## 2026-03-06 (test runtime environment fix)
- What changed:
  - Fixed execute permissions in WSL for npm test toolchain binaries:
    - `node_modules/.bin/cross-env`
    - `node_modules/.bin/cross-env-shell`
    - `node_modules/.bin/jest`
- What was learned:
  - On this environment, some `.bin` shims were created without execute bit, causing `EACCES` when running `npm test`.
- Validation:
  - `cd /home/rookie/project/duoxx_server && npm test -- --runInBand` passed (2 suites, 5 tests).
- Next steps / open questions:
  - If permissions regress after reinstall, add a post-install permission check script in the future.

## 2026-03-06 (M5 hardening: facade payload validation)
- What changed:
  - Added strict payload/query validator module:
    - `duoxx_server/src/services/pipelineDraftValidator.js`
    - validates:
      - `POST /api/pipeline/process` input (`sourcePath`, `displayName`)
      - `GET /api/pipeline/courses` query (`page`, `limit`, `language`, `keyword`)
      - `POST /api/pipeline/courses/write` payload and full draft structure
  - Added vocabulary-card field-level constraints before write-db:
    - `metadata.word`, `metadata.translation` required
    - `synonyms` / `antonyms` must be string arrays
    - length limits for phonetic/pos/example fields
    - answer/options type checks for non-vocabulary exercises
  - Added vocabulary draft integrity rule:
    - if all exercises are `vocabulary_card`, draft must contain exactly one lesson
  - Integrated validators into route facade:
    - updated `duoxx_server/src/routes/pipeline.js`
    - standardized 400 responses with `code`, `error`, `details` for validation failures
- What was learned:
  - Frontend simplicity needs backend guardrails; otherwise malformed draft payload can silently enter write-db flow.
  - Keeping first error in `error` while returning full `details` balances UI compatibility and debugging depth.
- Validation:
  - `node --check src/services/pipelineDraftValidator.js`
  - `node --check src/routes/pipeline.js`
  - `npm run build:pipeline`
  - `npm test -- --runInBand` => 3 suites, 11 tests passed
  - Added tests:
    - `duoxx_server/src/services/pipelineDraftValidator.test.js`
- Next steps / open questions:
  - Add role model (`admin` / `operator`) to `User` and apply route-level role guard for pipeline write actions.
  - Remove or isolate legacy `/jobs`/review/publish routes if no longer needed in V1 production UI.

## 2026-03-06 (M5 hardening: role-based access control)
- What changed:
  - Added role model support to user schema:
    - `duoxx_server/src/models/User.js`
    - new field: `role` (`admin` | `operator`), default `operator`
  - Added reusable role middleware in auth layer:
    - `duoxx_server/src/middleware/auth.js`
    - `resolveUserRole(user)` with backward-compatible fallback to `operator`
    - `authorizeRoles(...roles)` middleware with 401/403 handling
  - Applied role guards to pipeline routes:
    - `duoxx_server/src/routes/pipeline.js`
    - all pipeline routes now require `admin` or `operator`
    - legacy review/publish endpoints restricted to `admin` only:
      - `PATCH /jobs/:jobId/review-checklist`
      - `POST /jobs/:jobId/review`
      - `POST /jobs/:jobId/publish`
      - `GET /review/tasks`
  - Added middleware tests:
    - `duoxx_server/src/middleware/auth.test.js`
    - covers: missing-user 401, operator/admin allow logic, legacy no-role compatibility, admin-only deny, invalid config throw
- What was learned:
  - A fallback role strategy is necessary to avoid breaking legacy users that were created before `role` field existed.
  - Restricting legacy review/publish endpoints to admin isolates old flow while keeping V1 operator production path intact.
- Validation:
  - `node --check src/middleware/auth.js`
  - `node --check src/routes/pipeline.js`
  - `node --check src/models/User.js`
  - `npm run build:pipeline`
  - `npm test -- --runInBand` => 4 suites, 17 tests passed
- Next steps / open questions:
  - Add admin UI/API for role assignment (promote/demote operator) instead of relying on direct DB edits.
  - Decide whether to fully retire legacy `/jobs` review/publish API group in next milestone.

## 2026-03-06 (Codex MCP setup: chrome-devtools)
- What changed:
  - Installed global npm package `chrome-devtools-mcp` (version `0.19.0`) on the local Windows environment.
  - Updated Codex client MCP config at `C:\Users\Administrator\.codex\config.toml`:
    - added `[mcp_servers.chrome_devtools]`
    - set `command = "chrome-devtools-mcp"`
- What was learned:
  - `chrome-devtools-mcp` is the official npm package name; scoped package `@chrome-devtools/mcp` is not available on npm.
  - Existing Codex session does not hot-load new MCP servers; a new session/restart is required to see `chrome_devtools`.
- Validation:
  - `chrome-devtools-mcp --version` => `0.19.0`
  - `chrome-devtools-mcp --help` executed successfully.
- Next steps / open questions:
  - Restart Codex session to load the new `chrome_devtools` MCP server into tool registry.

## 2026-03-06 (RBAC closure: admin user-role management API)
- What changed:
  - Added dedicated role management service:
    - `duoxx_server/src/services/userRoleService.js`
    - capabilities:
      - paginated user listing with keyword search (`phone/email/nickname`)
      - role update with safety checks (`admin|operator` only, no self-role change)
  - Added tests for role management service:
    - `duoxx_server/src/services/userRoleService.test.js`
    - covers query/pagination normalization, invalid role rejection, self-update rejection, not-found handling, and success path
  - Extended auth routes with admin-only role management endpoints:
    - `GET /api/auth/users?keyword=&page=&limit=`
    - `PATCH /api/auth/users/:userId/role` body: `{ role: 'admin'|'operator' }`
    - file: `duoxx_server/src/routes/auth.js`
  - Reused existing RBAC middleware (`authenticate + authorizeRoles('admin')`) for new endpoints.
- What was learned:
  - Separating role management into a service keeps auth routes thin and makes permission-sensitive logic easier to unit-test.
  - Blocking self role change avoids accidental admin lockout during manual operations.
- Validation:
  - `node --check src/services/userRoleService.js`
  - `node --check src/routes/auth.js`
  - `node --check src/models/User.js`
  - `npm run build:pipeline`
  - `npm test -- --runInBand` => 5 suites, 23 tests passed
- Next steps / open questions:
  - Add a lightweight admin web page section to call `/api/auth/users` and `/api/auth/users/:id/role` directly.
  - Consider adding audit log records for role changes (who changed whom, old/new role, timestamp).

## 2026-03-09 (README startup command clarification)
- What changed:
  - Updated backend startup guidance in `\\wsl.localhost\Ubuntu-22.04\home\rookie\project\duoxx_server\README.md` to remove ambiguity between three commands.
  - Quick-start now uses `npm run dev` as the default development entry.
  - Clarified `npm run env:dev` is optional (only for syncing `.env.development` to `.env`).
  - Replaced direct `nodemon src/app.js --xiaochao` usage with note that nodemon is equivalent low-level command but not the primary entry.
  - Added a concise `Startup command summary` section in README.
- What was learned:
  - `npm run dev` already sets `NODE_ENV=development` and loader reads `.env.development`, so `env:dev` is not required for normal local startup.
  - Keeping one canonical startup command in README reduces operator confusion.
- Next steps / open questions:
  - Optionally normalize remaining mixed Chinese/English wording in backend README for consistency.
## 2026-03-09 (/admin/pipeline TTS + explanation review V1)
- What changed:
  - Frontend (`d:\06-project\expo_duo\duoxx`):
    - Reworked `components/admin/PipelineDashboard.tsx` to support content-level operations on sentence/word/phrase.
    - Added sentence-level TTS actions: generate/regenerate and play from saved asset URL.
    - Added explanation review workflow UI: open target -> prompt hint -> generate draft -> structured field editing -> approve/reject.
    - Added pending-review sidebar (`pending_review`) with quick open by locator.
    - Added structured payload editors for `zhSummary`, `enSummary`, `breakdown[]`, `pitfalls[]`, `memoryTips[]`.
  - Frontend API layer:
    - Extended `src/services/pipelineApi.ts` with `ContentLocator`, explanation schema types, and calls for generate/list/patch/approve/reject + TTS generate.
    - Extended `src/services/apiClient.ts` with `PATCH` request support and fixed malformed timeout/network fallback messages.
  - Backend (`duoxx_server`):
    - Added `src/services/contentLocator.js` + tests (`contentLocator.test.js`) for normalized locator protocol and source resolution.
    - Added `src/models/CourseExplanationDraft.js` and model export in `src/models/index.js`.
    - Extended `src/services/contentPipelineService.js` with:
      - TTS generation + file persistence (`uploads/pipeline-tts`) + payload upsert by locator.
      - Explanation draft generation, edit, approve, reject, list workflow.
      - Structured payload normalization/validation and approved asset upsert into `exercise.reviewAssets.explanations`.
    - Extended `src/routes/pipeline.js` with endpoints:
      - `POST /courses/:courseId/tts/generate`
      - `POST /courses/:courseId/explanations/generate`
      - `GET /courses/:courseId/explanations`
      - `PATCH /explanations/:draftId`
      - `POST /explanations/:draftId/approve`
      - `POST /explanations/:draftId/reject`
    - Added static serving for `/uploads` in `src/app.js` and fixed malformed legacy strings that broke JS parsing.
    - Replaced/expanded `src/services/contentPipelineService.test.js` to cover TTS locator upsert and explanation lifecycle.
- What was learned:
  - Locator-key upsert is sufficient for V1 idempotency: repeated TTS/explanation approvals replace prior asset at same content location.
  - Draft workflow can stay isolated in `CourseExplanationDraft` while approved assets are embedded directly in `PublishedCourse.payload.exercise.reviewAssets`.
  - Existing repo has unrelated TypeScript errors; file-level transpile/lint checks are required to verify changed frontend files without blocking on historical issues.
- Next steps / open questions:
  - Add backend route/integration tests for HTTP-level behavior (status codes + payload writes).
  - Add frontend automated interaction tests for TTS and explanation approval panel.
  - Consider showing locator jump/highlight in middle panel when clicking pending draft for faster reviewer navigation.

## 2026-03-09 (admin fetch + startup mojibake hotfix)
- What changed:
  - Diagnosed `Failed to fetch` from Windows browser to backend API as local loopback forwarding issue (`localhost:3000` unreachable from Windows while WSL IP was reachable).
  - Added Windows portproxy mapping from `127.0.0.1:3000` -> current WSL IP `:3000`, restoring API reachability for Expo web admin page.
  - Rewrote backend startup/log text to ASCII English in:
    - `duoxx_server/src/app.js`
    - `duoxx_server/src/config/env.js`
  - Kept behavior unchanged while removing garbled/mojibake runtime output.
- What was learned:
  - In this environment, browser access to WSL service is not reliably forwarded via localhost; explicit portproxy is required.
  - Mojibake came from source-string encoding mismatch, not from business logic.
- Next steps / open questions:
  - If WSL IP changes after restart, refresh portproxy rule to the new IP.
  - Optionally automate portproxy refresh via startup script.

## 2026-03-09 (mongo compass + login account recovery)
- What changed:
  - Started MongoDB in WSL with explicit host binding for local/dev access:
    - `mongod --dbpath /home/rookie/.duoxx-mongo/db --bind_ip 127.0.0.1,0.0.0.0 --port 27017`
  - Added Windows loopback forwarding for Compass/local tools:
    - `127.0.0.1:27017 -> <WSL_IP>:27017` via `netsh interface portproxy`.
  - Verified host TCP connectivity: `Test-NetConnection 127.0.0.1:27017` success.
  - Created verified working login account in `duoxx_dev`:
    - phone: `18212340001`
    - password: `123456`
    - role: `operator`
  - Reduced login page ?floating? behavior on web by disabling `KeyboardAvoidingView` for web in:
    - `duoxx/app/login.tsx`
- What was learned:
  - Local login timeouts were caused by Mongo unavailable path rather than auth logic.
  - This machine needs explicit portproxy for stable Windows->WSL localhost access.
- Next steps / open questions:
  - Consider adding startup script to auto-refresh portproxy when WSL IP changes.
  - Optionally promote the operator account to admin if admin-only routes are needed in daily operation.

## 2026-03-09 (course list empty diagnosis and DB switch)
- What changed:
  - Diagnosed empty `/api/pipeline/courses` response as wrong dev DB target (`duoxx_dev` had 0 `publishedcourses`).
  - Replaced `duoxx_server/.env.development` and switched `MONGODB_URI` to `mongodb://localhost:27017/duoxx`.
  - Triggered backend reload so new env is picked up.
  - Verified API now returns 2 courses from `duoxx`.
- What was learned:
  - Current expected course names (e.g. coffee shop / G7 Upper Unit 1 L5) are not present in current Mongo datasets scanned (`duoxx`, `duoxx_dev`).
- Next steps / open questions:
  - Confirm source DB/backup that contains those named courses and import into current Mongo instance.

## 2026-03-09 (pipeline admin flatten layout policy)
- What changed:
  - Added mandatory UI rule in `AGENTS.md` under `Admin UI Constitution (Mandatory)`:
    - forbid nested card-in-card visual stacks
    - forbid "big ring wrapping small rings" patterns
    - require flat row/list first for admin pages
  - Refactored `duoxx/components/admin/PipelineDashboard.tsx` middle panel from multi-layer nested boxes to flat table-like rows:
    - one row per actionable target (sentence/word/phrase)
    - columns: unit/lesson, exercise, target, actions, status
    - removed deep visual nesting for unit -> lesson -> exercise -> target wrappers
- What was learned:
  - Current heavy nesting came from rendering hierarchy directly with boxed containers; flattening by row projection keeps hierarchy readable while removing visual overload.
- Next steps / open questions:
  - Optionally apply same flat-list constitution to right-side review panel sections in a follow-up pass.

## 2026-03-09 (pipeline admin right-panel de-nesting)
- What changed:
  - Refactored `duoxx/components/admin/PipelineDashboard.tsx` right review panel to flat sections:
    - removed nested card wrappers (`pendingBox`, `metaBox`, `breakdownCard`) and replaced with section headers + divider lines + row list items.
    - pending list now uses lightweight rows with bottom separators and active-row highlight only.
    - review workspace now uses single-layer text metadata blocks and section separators.
    - breakdown editor switched from boxed cards to line-separated rows with inline remove action.
  - Simplified style layer to enforce anti-nesting policy:
    - removed obsolete nested container styles from historical hierarchy render.
    - reduced pill-like action tag radius for flatter admin visual language.
- What was learned:
  - Existing workflows (generate/save/approve/reject) can remain unchanged while shifting the UI to flat information architecture.
  - The biggest complexity came from container styling, not data flow; flattening styles preserves behavior with lower visual noise.
- Next steps / open questions:
  - Run manual UI smoke on `/admin/pipeline` at desktop + narrow widths to confirm readability and action discoverability.
## 2026-03-09 (admin constitution scope clarification)
- What changed:
  - Updated `AGENTS.md` `Admin UI Constitution (Mandatory)` with explicit global scope for all admin pages.
- What was learned:
  - Stating scope directly in the constitution avoids page-by-page interpretation drift.
- Next steps / open questions:
  - None.
## 2026-03-09 (static courses source audit + sync to publishedcourses)
- What changed:
  - Investigated course source split:
    - learner course tab uses static metadata from `duoxx/src/data/courses/*`.
    - admin `/api/pipeline/courses` uses backend `PublishedCourse` only.
  - Added backend sync script:
    - `duoxx_server/scripts/sync-static-courses-to-pipeline.js`
    - loads static courses (`coffee-shop`, `airport`, `g7-upper-practice`) from frontend TS files, converts to pipeline payload (`units -> lessons -> exercises`), and upserts `ContentPipelineJob` + `PublishedCourse` by `payload.sourceDocument.externalCourseId`.
  - Added backend npm command:
    - `duoxx_server/package.json` -> `sync:static-courses`.
  - Executed sync against `mongodb://localhost:27017/duoxx` and inserted 6 courses:
    - ???????????G7 Upper Unit 1 L5/L6/L7/L8.
- What was learned:
  - Missing courses in admin page were caused by dual source-of-truth (static frontend list vs DB-backed admin list), not frontend rendering bugs.
  - Converting static course shape to pipeline payload keeps existing admin actions usable without backend API changes.
- Next steps / open questions:
  - If needed, add a backend endpoint or admin button to trigger this sync instead of CLI script.
  - Consider de-duplicating historical duplicate rows (e.g., repeated `15?????3500?`) once source strategy is finalized.
## 2026-03-09 (pipeline login modal de-float + last account memory)
- What changed:
  - Reworked `duoxx/components/admin/PipelineLoginModal.tsx` to remove floating login effects:
    - removed Reanimated modal enter/shake animation.
    - removed strong shadow/elevation style that looked like floating card.
    - kept static centered modal with flat border style.
  - Added "remember last identifier" behavior for admin login:
    - persist last successful/attempted identifier to `@duoxx_pipeline_last_identifier` via `AsyncStorage`.
    - auto-prefill identifier field on modal mount.
    - password is not persisted.
- What was learned:
  - The screenshot login form is from `PipelineLoginModal.tsx`, not the general `app/login.tsx` page.
- Next steps / open questions:
  - If desired, add explicit "clear remembered account" action near the identifier field.
## 2026-03-09 (pipeline course edit/delete + answer mode config)
- What changed:
  - Backend API extension for course CRUD operations in pipeline domain:
    - Added payload validator `validateCourseUpdatePayload` in `duoxx_server/src/services/pipelineDraftValidator.js`.
    - Added routes in `duoxx_server/src/routes/pipeline.js`:
      - `PATCH /api/pipeline/courses/:courseId` (update title + answerMode)
      - `DELETE /api/pipeline/courses/:courseId` (soft delete -> archive)
    - Added service methods in `duoxx_server/src/services/contentPipelineService.js`:
      - `updateCourseMeta` updates `PublishedCourse.title`, `payload.title`, `payload.exerciseConfig.answerMode` and syncs source job draft.
      - `archiveCourse` sets `PublishedCourse.status = archived`.
    - `listCourses` now selects `payload.exerciseConfig.answerMode` for UI use.
  - Frontend admin page updates in `duoxx/components/admin/PipelineDashboard.tsx`:
    - Added `Edit` / `Delete` buttons for each course row.
    - Added inline edit panel supporting:
      - course title editing
      - answer mode selection: `choice / input / dialogue`
      - save/cancel actions.
    - Added delete confirm flow and local UI state resets when deleting selected course.
    - Display selected course answer mode in center panel header.
  - Frontend API extension in `duoxx/src/services/pipelineApi.ts`:
    - Added `updateCourse()` and `deleteCourse()` methods.
    - Added `CourseAnswerMode` type and adjusted status union to include `active`.
  - Tests:
    - Added unit tests in `duoxx_server/src/services/contentPipelineService.test.js` for `updateCourseMeta` and `archiveCourse`.
- What was learned:
  - Existing admin ?create/read? flow already exists via Upload Source + list; this iteration closes ?update/delete? gap with minimal schema change.
- Next steps / open questions:
  - Optionally expose answer mode as a dedicated column/filter in the course list.
  - Optionally add restore endpoint for archived courses.
## 2026-03-09 (pipeline flat list single/multi select + batch actions)
- What changed:
  - Added list selection workflow in `duoxx/components/admin/PipelineDashboard.tsx` for content rows:
    - selection column with checkbox per row.
    - mode switch: `single` / `multiple`.
    - select all / clear controls.
    - batch toolbar actions:
      - `Batch TTS` for selected sentence rows (loops existing TTS API).
      - `Batch Delete` for selected rows.
  - Added frontend API method in `duoxx/src/services/pipelineApi.ts`:
    - `batchDeleteTargets(courseId, locators)` -> `POST /api/pipeline/courses/:courseId/content/batch-delete`.
  - Added backend batch delete endpoint in `duoxx_server/src/routes/pipeline.js`:
    - `POST /api/pipeline/courses/:courseId/content/batch-delete`.
  - Added backend service logic in `duoxx_server/src/services/contentPipelineService.js`:
    - `batchDeleteCourseTargets` resolves locators and deletes targeted content (`sentence/word/phrase`) from course payload.
    - removes linked `reviewAssets.tts/explanations` by locatorKey for deleted targets.
  - Added backend unit test in `duoxx_server/src/services/contentPipelineService.test.js`:
    - validates batch deletion of sentence/word/phrase targets.
- What was learned:
  - Existing locator resolution is sufficient for reliable batch operations; no new locator protocol needed.
- Next steps / open questions:
  - Optionally add backend batch TTS endpoint to reduce request round-trips for large selections.
## 2026-03-09 (pipeline explanation review UX: remove reject reason + typed default prompt hints)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` review flow:
    - removed `Reject Reason` input state and UI field.
    - reject action now no longer blocks on extra input; it sends fallback reason from `reviewNote` or default `Rejected by reviewer`.
  - Added default prompt-hint generator for explanation drafting:
    - new helper `buildDefaultPromptHint(itemType, sourceText, courseTitle, answerMode, exerciseType)`.
    - generates different base hints for `word`, `phrase`, and `sentence` targets.
    - injects course context (`course title`, `answer mode`, `exercise type`) to improve model guidance.
    - used in both `openTargetReview` and `openPendingDraft` when draft has no saved `promptHint`.
  - Adjusted prompt hint placeholder to clarify auto-filled behavior.
  - Reordered one `useEffect` to avoid `flatContentRows` use-before-declaration TypeScript issue.
- What was learned:
  - Default hint quality improves when it carries lightweight course context, even without changing backend prompt pipeline.
  - Reject workflow can stay lightweight for operators while still satisfying backend reason requirement via deterministic fallback.
- Next steps / open questions:
  - If desired, localize default hints fully to Chinese for reviewer consistency.
  - Consider adding a per-course prompt-hint template override persisted in backend for finer control.
## 2026-03-09 (pipeline course editor moved to right content area)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to move course property editor UI from left `Courses` list to middle content panel.
  - Left panel now only focuses on course browsing/select/edit/delete triggers; editor form no longer occupies top area.
  - Added `editingCourse` memo to show clear context title in the right-side editor header (`Edit Course: ...`).
- What was learned:
  - Keeping mutable metadata form out of the list panel improves scalability when more course attributes are added.
- Next steps / open questions:
  - If needed, convert the right-side course editor into tabbed sections when more fields (difficulty/tag/visibility/workflow config) are introduced.
## 2026-03-09 (progress dashboard duplicate back button + responsive learning calendar)
- What changed:
  - Disabled default stack header for progress dashboard route in `duoxx/app/_layout.tsx` to remove duplicate back button (`progress-dashboard` now `headerShown: false`).
  - Refactored `duoxx/src/components/progress/LearningCalendar.tsx` for responsive layout:
    - added container width measurement (`onLayout`) and adaptive calendar sizing (`calendarLayout` memo).
    - day square size and grid gaps now scale with available width.
    - horizontal scroll only enabled when content exceeds container width.
    - month labels, day label block height, and legend square size now follow adaptive metrics.
    - normalized hooks with `useCallback`/`useMemo` to keep lint clean for this file.
- What was learned:
  - Duplicate back icon came from route stack header and in-page custom header both active.
  - Fixed-size heatmap cells made the calendar look non-responsive on wide screens.
- Next steps / open questions:
  - If needed, make `AbilityRadar` size responsive as well to improve consistency across large/small screens.
## 2026-03-09 (home tab: merge today progress + streak into one row)
- What changed:
  - Updated `duoxx/app/(tabs)/index.tsx` to evaluate and implement one-row display for `????` + `????`.
  - Replaced standalone large streak block with compact right-side summary card in the same row as today progress.
  - Both cards navigate to `/progress-dashboard` for full details.
  - Added `streakDays` derived value and new compact styles: `summaryRow`, `summaryCard`, `summaryHeader`, `summaryMetricRow`, etc.
- What was learned:
  - One-row summary works cleanly for current homepage density and avoids duplicated large KPI sections.
- Next steps / open questions:
  - If needed, add small-screen breakpoint to stack the two cards only under very narrow widths.
## 2026-03-09 (dialogue bubble pronunciation icon after speaker name)
- What changed:
  - Added pronunciation button next to speaker name in dialogue bubbles for:
    - `duoxx/app/lesson/[courseId].tsx`
    - `duoxx/app/scene-course/[sceneId].tsx`
  - Clicking the icon now triggers browser speech synthesis for the English sentence (`speechSynthesis`), with active speaking state reflected by icon variant/color.
  - Added cleanup on unmount to cancel ongoing speech playback.
- What was learned:
  - The screenshot matches the dialogue bubble layout used in lesson/scene course message render blocks, so both paths were updated to keep UX consistent.
- Next steps / open questions:
  - If mobile native playback is required, add `expo-speech` fallback so pronunciation works beyond web browsers.
## 2026-03-10 (pipeline per-row edit)
- What changed:
  - Added per-row target Edit/Save/Cancel in /admin/pipeline table.
  - Added frontend API: PATCH /api/pipeline/courses/:courseId/content/update.
  - Added backend route + validator + service method to update sentence/word/phrase text by ContentLocator.
  - Added tests for content update flow and payload validation.
- What was learned:
  - Existing admin flow had course-level edit and batch delete, but no single-target content update endpoint.
- Next steps / open questions:
  - Consider returning invalidated-asset count (tts/explanations) after target updates.## 2026-03-10 (pipeline columns + modal editor)
- What changed:
  - Reworked /admin/pipeline content table columns: added Pronunciation column and ?? column; moved Actions after Status.
  - Pronunciation column now shows play icon when TTS exists, otherwise ?? button.
  - ?? column now shows ?? button when explanation exists, otherwise ?? button (one-click draft generation).
  - Replaced inline row edit with independent modal editor for item text, prompt, answer, and options.
  - Added frontend API method updateExercise() for PATCH /api/pipeline/courses/:courseId/exercises/:exId.
- What was learned:
  - The backend exercise update endpoint supports partial object merge, so modal save can submit edited exercise payload directly.
- Next steps / open questions:
  - Consider adding dedicated metadata fields (translation/POS/synonyms/antonyms) into the modal in addition to generic answer/options fields.## 2026-03-10 (auth token 24h)
- What changed:
  - Updated backend dev JWT access token ttl from 1h to 24h in `duoxx_server/.env.development` (`JWT_EXPIRES_IN=24h`).
  - Updated auth response payload in `duoxx_server/src/routes/auth.js` to return `tokens.expiresIn` dynamically from `config.jwt.expiresIn` instead of hardcoded 3600.
  - Added helper `parseExpiresInToSeconds()` in auth route for consistent numeric seconds in API response.
- What was learned:
  - Actual token signing ttl already comes from config/env; only API response `expiresIn` was hardcoded and could drift from real ttl.
- Next steps / open questions:
  - If production also requires 24h, align `.env`/prod deployment env `JWT_EXPIRES_IN` accordingly.## 2026-03-10 (pipeline compact buttons + label rules)
- What changed:
  - Tightened pipeline table action button sizing (smaller padding, no stretch) to save horizontal space.
  - Renamed table header `Pronunciation` to `??`.
  - Changed explanation button labels: no explanation asset => `??`; has explanation asset => `??`.
  - Unified button visual style across ??/??/Actions columns.
  - Changed ready pronunciation control from icon to text `??` button for consistent style.
  - Added per-column width ratios to reduce wide-column waste in table layout.
- What was learned:
  - RN Web buttons can appear over-wide when parent columns stretch; `alignSelf: 'flex-start'` keeps controls compact.
- Next steps / open questions:
  - If further compression is needed, we can collapse Unit/Lesson secondary text to tooltip-on-hover on web.## 2026-03-10 (pipeline explanation review close button)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx:
    - added closeExplanationReview() to clear active review context (ctiveTarget, currentDraft, promptHint, 
eviewNote, editablePayload).
    - added hasReviewContext state-derived flag for button enabled/disabled behavior.
    - changed Explanation Review header into a row with a Close action button.
    - reused closeExplanationReview() in loadCourseDetails(..., keepActiveSelection = false) to avoid duplicated reset logic.
- What was learned:
  - The right-side review panel had no explicit affordance to exit current review context, causing UX confusion when operators wanted to return to empty state.
- Next steps / open questions:
  - If users also want to hide the whole right panel (not only clear context), add a panel collapse toggle and persist its state per session.
## 2026-03-10 (pipeline explanation review close button v2)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx.
  - Added closeExplanationReview() to clear active review context: activeTarget, currentDraft, promptHint, reviewNote, editablePayload.
  - Added hasReviewContext derived flag for Close button enable/disable.
  - Reworked Explanation Review header into a row and added a Close action button.
  - Reused closeExplanationReview() in loadCourseDetails(..., keepActiveSelection = false) to avoid duplicate reset logic.
- What was learned:
  - Operators need an explicit control to clear current review context and return the right panel to empty state.
- Next steps / open questions:
  - If needed, add a full panel collapse toggle (hide right panel), not just context clear.

## 2026-03-10 (pipeline batch toolbar professional redesign)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx batch toolbar UI in the pipeline middle panel.
  - Reworked the toolbar into a clearer single-layer structure:
    - top area: selection summary + selection mode segmented control
    - bottom area: utility actions (Select All / Clear) and primary/danger actions (Batch TTS / Batch Delete)
  - Added consistent icon+text action buttons and clearer hierarchy for primary vs destructive actions.
  - Added responsive behavior for narrow widths so groups stack cleanly.
  - Kept existing batch handlers and behavior unchanged.
- What was learned:
  - Flat admin layouts can still feel professional when action grouping and hierarchy are explicit.
  - Selection hint text reduces operator uncertainty before batch actions.
- Next steps / open questions:
  - Consider keyboard shortcuts for bulk operations on web (Ctrl/Cmd+A select all, Esc clear selection).## 2026-03-10 (pipeline explanation review full panel close)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx to make Explanation Review a true collapsible right panel.
  - Added isReviewPanelOpen state; right panel now unmounts completely when closed.
  - Split logic into clearExplanationReviewContext() and closeExplanationReview().
  - closeExplanationReview() now clears context and hides the right panel.
  - Clicking explanation entry actions (openTargetReview/openPendingDraft) now auto-reopens the right panel.
  - Added Open Explanation Review button in the middle panel when right panel is hidden.
- What was learned:
  - Operators expect Close to hide the entire panel, not only reset bottom content.
- Next steps / open questions:
  - If needed, persist right-panel open/closed preference per user session.
## 2026-03-10 (pipeline edit persistence fix)
- What changed:
  - Fixed modal save logic where sentence prompt edits could be overwritten by item-text assignment.
  - Introduced `setTargetFieldOnExercise(...)` for locator-based precise target writes (sentence prompt/example, word, phrase).
  - For sentence(prompt), unified canonical value to avoid prompt/item divergence during save.
  - Kept secondary `updateTargetText` call only when target text actually changes (for review-asset invalidation).
- What was learned:
  - Prompt-target rows need a single canonical value; dual inputs without merge rules cause apparent save success with unchanged render.
- Next steps / open questions:
  - If exId collisions exist in imported data, consider adding backend update by locator/index endpoint to guarantee row-accurate exercise patching.
## 2026-03-10 (pipeline batch toolbar minimalization)
- What changed:
  - Refined duoxx/components/admin/PipelineDashboard.tsx batch toolbar to a compact single-row layout.
  - Removed visual noise: dropped extra caption/hint/pill/icon decoration in toolbar.
  - Kept only dense controls: selected count, single/multiple switch, select all, clear, batch tts, batch delete.
  - Reduced spacing, radii, and button padding to improve density and precision.
  - Preserved all existing behavior and action handlers.
- What was learned:
  - For admin operators, compact and direct action bars perform better than explanatory UI.
- Next steps / open questions:
  - If needed, split compact mode by breakpoint: ultra-compact desktop + two-line mobile-only mode.## 2026-03-10 (pipeline prompt hint height + remove review note)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx.
  - Increased Prompt Hint textarea height with a dedicated style promptHintInput (minHeight 120).
  - Removed Review Note feature from explanation review UI.
  - Removed reviewNote state and reset logic from panel workflow.
  - Updated draft actions to stop passing review note data:
    - updateExplanationDraft now sends draftId + structuredPayload + promptHint.
    - approveExplanationDraft now sends only draftId.
    - rejectExplanationDraft now sends fixed reason Rejected by reviewer without note.
- What was learned:
  - Operators only need prompt editing and approve/reject actions here; extra note field adds noise.
- Next steps / open questions:
  - If reviewers still need annotations later, consider storing internal operator logs separately from the approve/reject form.
## 2026-03-10 (lesson-exercise dynamic source)
- What changed:
  - Added backend public route `GET /api/learning/lesson-exercise/:lessonId` in `duoxx_server/src/routes/learning.js` to read latest active PublishedCourse content for G7 vocab lesson IDs.
  - Wired new route in `duoxx_server/src/app.js` under `/api/learning` without operator-role gate.
  - Added frontend API client `duoxx/src/services/learningApi.ts`.
  - Updated `duoxx/app/lesson-exercise/[lessonId].tsx` to prefer backend lesson data for `vocab-*` routes and fallback to static data only on remote failure.
  - Added options mapping compatibility from `metadata.options` for pipeline multiple-choice records.
- What was learned:
  - The learner page was reading static file data (`g7-upper-practice.ts`) and never consuming pipeline DB content, so admin updates could not appear.
- Next steps / open questions:
  - Consider adding explicit stable `courseId` field in PublishedCourse payload to avoid title-regex matching.## 2026-03-10 (pipeline table simplification: drop unit column + inline exercise tag)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx table layout in middle panel.
  - Removed Unit/Lesson column from header and row rendering.
  - Changed Exercise cell rendering to one-line structure: type tab label + exercise text (no top stacked type line).
  - Removed Target title line (e.g., Sentence (prompt)); Target now shows only source content text.
  - Rebalanced column widths after removing Unit/Lesson.
- What was learned:
  - In this admin workflow, unit/lesson metadata was low-value noise compared with direct exercise/target content.
- Next steps / open questions:
  - If location context is occasionally needed, consider showing U/L info in a tooltip or detail popover instead of a dedicated column.

## 2026-03-10 (personalization service v1: goal prediction + dynamic content planning)
- What changed:
  - Added new personalization domain model file: duoxx/src/models/personalization.ts.
  - Added personalization config weights: duoxx/src/config/personalizationWeights.ts and exported via config index.
  - Implemented modular personalization services under duoxx/src/services/personalization/:
    - goalSignalCollector.ts (signal aggregation from profile/goal/error/progress sources)
    - goalPredictionEngine.ts (pluggable goal predictor strategy with rule-based default)
    - 	agOrchestrator.ts (stable learner tag generation)
    - dynamicContentPlanner.ts (blueprint + executable practice plan generation, reusing module registry and quick-start scoring)
    - iEnhancerAdapter.ts (optional AI enhancement adapter with safe fallback)
    - personalizationFacade.ts (public API orchestration + 24h cache)
    - index.ts barrel export for personalization services.
  - Exported personalization model from duoxx/src/models/index.ts.
  - Added tests for new subsystem:
    - goalPredictionEngine.test.ts
    - 	agOrchestrator.test.ts
    - dynamicContentPlanner.test.ts
    - iEnhancerAdapter.test.ts
    - personalizationFacade.test.ts
- What was learned:
  - Existing goalTracker, errorCollectionService, quickStartRecommendationEngine, and profile services provide enough local signals to build a reusable first version without new telemetry.
  - A facade + strategy pattern keeps prediction and generation logic replaceable while preserving stable external APIs.
- Next steps / open questions:
  - Add adapter from generated blueprint to concrete exercise/question generation pipeline when entering "content draft" phase.
  - Define optional backend provider contract for AI enhancement to support cloud-side models without changing facade API.
## 2026-03-10 (personalization service v1 notes fix)
- What changed:
  - Corrected memory log text encoding for personalization subsystem notes.
  - Confirmed added files: goalSignalCollector.ts, goalPredictionEngine.ts, tagOrchestrator.ts, dynamicContentPlanner.ts, aiEnhancerAdapter.ts, personalizationFacade.ts, and related tests.
- What was learned:
  - Use single-quoted PowerShell here-strings when appending markdown containing backticks.
- Next steps / open questions:
  - Optional cleanup pass can normalize earlier control-character artifacts in legacy memory entries.
## 2026-03-10 (home tools + personalization + CAT vocab assessment)
- What changed:
  - Reworked learner home information architecture in `duoxx/app/(tabs)/index.tsx`:
    - replaced direct learning-module grid on home with 3 fixed tool cards:
      - `?????` -> `/personalization`
      - `??????` -> `/progress-dashboard`
      - `???????? -> `/vocab-assessment`
    - added `?????????` secondary entry linking to `/learning-modules`.
    - home now reads latest personalization profile + vocab assessment summary for card subtitles.
  - Added new pages:
    - `duoxx/app/personalization.tsx`: flat profile/goal/preferences editor (all fields optional).
    - `duoxx/app/vocab-assessment.tsx`: CAT-driven vocab test UI with interval/confidence result publishing.
    - `duoxx/app/learning-modules.tsx`: original learning modules moved here with category filters.
  - Added new data/storage/services:
    - `duoxx/src/models/personalization.ts`: appended `PersonalizationProfile` and `VocabAssessmentResult` model family.
    - `duoxx/src/storage/personalizationStorage.ts`: new keys
      - `@duoxx:personalization:{userId}`
      - `@duoxx:vocab-assessment:{userId}`
      - plus capped local assessment logs for calibration follow-up.
    - `duoxx/src/services/vocabAssessmentService.ts`: CAT + IRT(2PL, MAP estimate) engine:
      - item info-based next-question selection
      - theta/SE iterative updates
      - stop conditions (min/max items + target SE)
      - interval-based result publishing + recommendations.
  - Extended recommendation weighting in `duoxx/src/services/quickStartRecommendationEngine.ts`:
    - now reads personalization profile and latest vocab assessment
    - applies additional scoring for focus, preferred tags, and vocabulary-gap signals.
  - Added module category support for grouped filtering:
    - `duoxx/src/modules/types.ts` and `duoxx/src/modules/registry.ts`.
  - Added vocab assessment unit tests:
    - `duoxx/src/services/__tests__/vocabAssessmentService.test.ts`.
- What was learned:
  - Existing project already had enough primitives (`moduleRegistry`, progress dashboard, vocab corpus with IRT params) to implement this without backend API changes.
  - For ??cientific rigor?? interval + confidence output and explicit SE-based stopping are critical to avoid pseudo-precise single-number reporting.
- Next steps / open questions:
  - Integrate phase-2 item parameter recalibration job (offline/batch) to consume saved local assessment logs.
  - Decide whether to sync personalization + assessment records to backend for cross-device continuity.
## 2026-03-10 (pipeline item edit save false-positive fix)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx save flow for modal item editing.
  - Fixed sentence(prompt) canonical merge logic: editing Item text alone now persists correctly instead of being overwritten by unchanged Prompt text.
  - Added helper functions findExerciseByLocator() and readTargetTextFromCourse() for post-save verification.
  - Changed loadCourseDetails() to return latest merged course data.
  - Added post-save server verification: after update, reload course and compare persisted target text; show explicit error if backend did not persist.
- What was learned:
  - The previous merge logic treated Prompt as dominant in sentence(prompt), which caused a success toast despite no actual item-text change when only Item was edited.
- Next steps / open questions:
  - If needed, add integration test coverage for sentence(prompt), word, and phrase edit persistence paths.
## 2026-03-10 (pipeline modal remove redundant item field for sentence prompt)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx modal editor.
  - In sentence(prompt) target mode, hid the Item input block because it duplicates Prompt.
  - Kept Item input for non-duplicated target types (word/phrase/exampleSentence paths).
  - Updated save logic to compute effective target text for sentence(prompt) from Prompt first, avoiding empty/redundant-field issues.
- What was learned:
  - Item and Prompt are the same canonical field for sentence(prompt), so exposing both causes UX confusion.
- Next steps / open questions:
  - If desired, rename the remaining Prompt label to Question Text for clearer operator wording.
## 2026-03-10 (pipeline exercise type tag zh labels)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx.
  - Added formatExerciseTypeTag() mapping exercise type codes to Chinese labels.
  - Switched exercise type tab rendering to use Chinese label mapping (e.g. multiple_choice -> ???).
- What was learned:
  - Type-code tags are clearer for operators when localized to Chinese in this admin UI.
- Next steps / open questions:
  - If needed, extend mapping for any newly introduced exercise types to keep labels consistent.
## 2026-03-10 (pipeline editable exercise display tag)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx to support editable display label for exercise type tags.
  - Added display override storage in exercise metadata: metadata.adminDisplayType.
  - Table type tag rendering now uses override first, fallback to system-mapped type label.
  - Added a new field in Action modal editor: ????????.
  - Save flow now persists/removes metadata.adminDisplayType without changing underlying exercise.type.
- What was learned:
  - Operators need semantic display labels (e.g., ???) that can differ from answer interaction type (e.g., multiple_choice).
- Next steps / open questions:
  - If needed, add quick-select chips for common labels (???/???/???) to reduce manual typing.
## 2026-03-10 (pipeline modal answer editor radio rows)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx answer editor in Action modal.
  - For multiple_choice/true_false with available options, answer input now renders as clickable radio rows (line by line).
  - Selecting a row writes the chosen option text into editorAnswerText and uses existing save flow.
  - Non-single-choice cases still fallback to text input editor.
- What was learned:
  - Reviewer correction speed is much better with row-level radio selection than manual typing for single-choice answers.
- Next steps / open questions:
  - If needed, support multi-select answer UI for question types that can have multiple correct options.
## 2026-03-10 (pipeline course delete moved into edit panel + double confirm)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx course actions UX.
  - Removed Delete button from left course list; list now keeps only Edit.
  - Added Delete button inside course edit panel actions.
  - Refactored delete handler to use (courseId, courseTitle) and support panel-triggered delete.
  - Added required two-step confirmation before archive delete (first confirm + final confirm).
- What was learned:
  - Centralizing destructive actions inside edit context reduces accidental deletes in dense lists.
- Next steps / open questions:
  - If needed, add optional typed confirmation (enter course title) for extra safety on production admins.
## 2026-03-10 (pipeline remove open explanation review button)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx.
  - Removed the Open Explanation Review button from middle panel when right panel is collapsed.
- What was learned:
  - Operators prefer cleaner toolbar area and do not need explicit reopen button there.
- Next steps / open questions:
  - Reopen right panel remains available by clicking explanation actions in table rows.
## 2026-03-10 (pipeline course edit as standalone middle-page mode)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx middle panel rendering.
  - Added two-mode rendering for middle panel:
    - edit mode (editingCourseId present): show only course edit UI as a standalone page view.
    - content mode: show regular course content table and batch toolbar.
  - Updated panel title to reflect edit mode context.
- What was learned:
  - Inline top-mounted edit blocks are visually noisy in admin workflows; dedicated edit mode is clearer for operators.
- Next steps / open questions:
  - If needed, add breadcrumb/back indicator for explicit mode awareness.
## 2026-03-10 (pipeline edit mode course switch + compact mode row + bottom-right actions)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx.
  - Fixed course switching while in edit mode: clicking a course in left list now closes edit mode first, then loads that course content view.
  - Reworked edit-mode layout as standalone page form with bottom action area.
  - Moved Cancel/Save/Delete action group to the bottom-right of edit page.
  - Added inline mode section: title "????" and mode buttons rendered on one row to save vertical space.
- What was learned:
  - Edit-mode state must be explicitly reset on course navigation to prevent stale editor context from hijacking main panel rendering.
- Next steps / open questions:
  - If needed, add a subtle mode badge (EDIT MODE) near panel title for even clearer state awareness.
## 2026-03-10 (pipeline content pagination for large courses)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx content table to paginate rendering.
  - Added pagination state: contentPage and contentPageSize (60/120/240 per page).
  - Added page bar with range display, page-size switch, and Prev/Next controls.
  - Table now renders pagedContentRows instead of full flatContentRows list.
  - Changed Select All behavior to page-scoped selection (Select Page / Unselect Page) to avoid huge bulk selection overhead.
  - Reset page to 1 when course changes; clamp page index when total pages shrink.
- What was learned:
  - Rendering all rows for very large courses causes UI lag; paged rendering is an immediate low-risk fix without backend API changes.
- Next steps / open questions:
  - If flattening itself becomes a bottleneck on very large datasets, add backend content pagination API and fetch by page.
## 2026-03-10 (pipeline vocab duplicate rows in 3500-word course)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx target expansion logic for vocabulary_card.
  - Added normalization + dedupe rules while building targets:
    - skip prompt target when prompt text equals metadata.word.
    - skip duplicate example/phrase targets when text repeats prompt/word/example or repeats in phrases.
  - Example-sentence rows now show sentence text in Exercise column to avoid repeated word-stem display.
- What was learned:
  - The repeated-word issue came from frontend flattening one exercise into multiple targets with identical text.
- Next steps / open questions:
  - Consider a "primary target only" filter toggle for operators who want one-word-one-row view.
## 2026-03-10 (pipeline explanation panel hidden until item edit click)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx so right-side Explanation Review is hidden by default.
  - Added course-switch reset to force-close review panel.
  - Right panel now renders only when both panel-open flag and activeTarget exist.
- What was learned:
  - Showing a persistent empty review panel without explicit item selection causes operator confusion.
- Next steps / open questions:
  - None.
## 2026-03-10 (pipeline vocabulary-card duplicate fix for airport-duty course)
- What changed:
  - Tightened dedupe rule in duoxx/components/admin/PipelineDashboard.tsx buildTargetsForExercise().
  - For vocabulary_card, when metadata.word exists, prompt target is no longer expanded as a separate row.
  - Keeps canonical word row plus non-duplicate example/phrase rows.
- What was learned:
  - Some courses still had prompt/word near-duplicate rows that passed previous equality-based filter; using word as canonical target avoids this class of duplicates.
- Next steps / open questions:
  - If needed, add a UI toggle to show hidden prompt rows for debugging only.
## 2026-03-10 (pipeline pronunciation column play icon when audio exists)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx pronunciation cell rendering.
  - If tts asset exists (audioUrl), show play icon button for all target types.
  - If no audio, keep existing behavior: sentence can generate; non-sentence stays disabled.
- What was learned:
  - Previous logic hard-gated by itemType===sentence, so word rows looked disabled even when audio was available.
- Next steps / open questions:
  - If needed, allow direct TTS generation for word/phrase targets as well.
## 2026-03-10 (pipeline hide vocabulary example rows; edit in modal only)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx buildTargetsForExercise() to stop expanding vocabulary-card exampleSentence as standalone table rows.
  - Added editorExampleSentence state in exercise edit modal.
  - For vocabulary_card, modal now shows field: "Example sentence (extension)" and save flow persists/removes metadata.exampleSentence.
- What was learned:
  - Operators treat example sentence as secondary/extension content and prefer it inside edit context rather than flat list rows.
- Next steps / open questions:
  - If needed, apply same hide-in-list strategy to phrase extensions as well.
## 2026-03-10 (pipeline show item id in course content rows)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx row rendering in content table.
  - Added per-row item id display under Exercise text: ID: <exerciseId>.
  - Added fallback id format when exerciseId is missing: uX-lY-eZ.
- What was learned:
  - Operators need stable row identity for review/edit communication; inline id is faster than opening modal.
- Next steps / open questions:
  - If needed, add copy-to-clipboard button for item id.
## 2026-03-10 (audit: pipeline "extension knowledge" decoupling status)
- What changed:
  - Read-only investigation; no product code changed.
  - Audited duoxx/components/admin/PipelineDashboard.tsx and duoxx/src/services/pipelineApi.ts save chain for edit-modal field "????????????".
  - Audited duoxx/scripts/course-import/import-mongo-course-csv.js for existing normalized knowledge collections and dedupe indexes.
- What was learned:
  - Current edit-modal "??????" persists as `exercise.metadata.exampleSentence` via `/api/pipeline/courses/:courseId/exercises/:exerciseId` and is not represented as independent graph nodes/edges in current app API contract.
  - Repo contains import-time normalized collections (`knowledge_points`, `lesson_items`) with unique indexes, but no visible runtime API wiring from the edit modal to those collections.
- Next steps / open questions:
  - Need backend runtime route/model repo to confirm whether server-side write path maps modal edits into normalized `knowledge_points` references.
## 2026-03-10 (pipeline item numeric id column)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx content table to add dedicated numeric item ID column.
  - Row ID now shows sequential number (1..N) based on current content order, not backend string exerciseId.
  - Removed previous inline ID: ex:... text under Exercise content.
- What was learned:
  - Operators need human-readable numeric row IDs for review communication, separate from technical exercise IDs.
- Next steps / open questions:
  - If needed, add toggle for page-local numbering vs global numbering.
## 2026-03-10 (pipeline default 30 per page + fuzzy search for exercise)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx default contentPageSize from 120 to 30.
  - Removed 120/page and 240/page options; page size options are now 30/page and 60/page.
  - Added exerciseSearchQuery with fuzzy matching for Exercise text.
  - Added search bar UI (input + clear) and switched pagination to filtered rows.
- What was learned:
  - Search must happen before pagination to keep counts/page navigation consistent.
- Next steps / open questions:
  - If needed, extend search target to include Target column text as optional toggle.
## 2026-03-10 (pipeline pronunciation generate enabled for word items)
- What changed:
  - Updated duoxx/components/admin/PipelineDashboard.tsx TTS generation flow.
  - Removed single-item hard gate that limited TTS generation to sentence rows.
  - Updated batch TTS to process all selected rows (not only sentence rows).
  - Updated pronunciation column rendering so rows without audio always show a clickable generate button.
  - Repaired editor parse issues caused by malformed legacy characters and revalidated with lint.
- What was learned:
  - The blocked button behavior was a frontend restriction, not a locator/API limitation in this layer.
- Next steps / open questions:
  - If backend enforces type-specific TTS constraints, surface per-row failure reason in the table.
## 2026-03-10 (pipeline left course fuzzy search + category filters)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` left courses panel.
  - Added fuzzy search input for courses (`title/courseId/language/status`).
  - Added category filters for `status` and `language` with dynamic options from loaded courses.
  - Added filtered result summary (`filtered/total`) and changed list rendering to `filteredCourses`.
  - Added filter-related UI styles (`courseFilterPanel`, chips, search row).
- What was learned:
  - Course list filtering can stay fully local and responsive without backend API changes for current data size (<=100 courses loaded).
- Next steps / open questions:
  - If course count grows beyond current fetch limit, add backend-side query/filter endpoints with pagination.
## 2026-03-10 (pipeline toolbar redesign - single row contract)
- What changed:
  - Refactored duoxx/components/admin/PipelineDashboard.tsx top control area from two stacked bars into one compact toolbar.
  - Toolbar layout: left search group, right stats + page-size + pager controls.
  - Replaced large clear-search text button with compact icon button.
  - Added responsive fallback (mobile stacks vertically only when needed).
- What was learned:
  - Feature-by-feature insertion caused fragmented admin toolbar; a single-row layout contract prevents visual drift.
- Next steps / open questions:
  - Optionally extract this toolbar into a reusable admin component to enforce consistency across admin pages.## 2026-03-10 (pipeline course filter compactness + semantic cleanup)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` course filter UI to reduce vertical space.
  - Kept search always visible; category filters now render inline and only when multi-option filtering is meaningful.
  - Added label formatters for clearer chip text (`Any/Active/...`, `Any/EN/ZH`).
  - Auto-reset status/language filters to `all` when only one real option exists, preventing hidden stale filters.
  - Result counter now appears only when filters/search are actually active.
- What was learned:
  - Showing `all + only one option` chips adds confusion and no operational value; conditional rendering keeps admin UX concise.
- Next steps / open questions:
  - If needed, replace chips with one compact dropdown per dimension for very large option sets.

## 2026-03-10 (pipeline duplicate-course root-cause check)
- What changed:
  - Investigated why `/admin/pipeline` shows duplicate course title `15�챳�����3500��`.
  - Confirmed duplicates are real DB records in `publishedcourses`, not frontend rendering duplication.
  - Verified two active records exist for same `title + language` with different `_id`, `version`, and `sourceJobId`.
  - Traced backend logic:
    - `src/services/contentPipelineService.js` `writeCourse` create branch always inserts a new `PublishedCourse` when `courseId` is absent.
    - `src/services/contentPipelineService.js` `listCourses` returns all active records directly (no dedupe/latest-only grouping).
    - `src/models/PublishedCourse.js` has no uniqueness constraint for active natural key (`title/language` or source job mapping).
- What was learned:
  - Duplicate title entries happen when the same course is published multiple times via different source jobs without reusing `courseId`.
  - Current schema/version strategy supports multiple versions, but list API is not collapsing versions for admin display.
- Next steps / open questions:
  - Decide desired rule: keep one active row per logical course (archive older), or keep multi-version but list latest only.
  - Then enforce at service+DB level (upsert/unique index or status-aware partial unique index).

## 2026-03-10 (admin pipeline�����޸�)
- What changed:
  - Fixed mojibake/garbled Chinese UI labels in `duoxx/components/admin/PipelineDashboard.tsx`.
  - Replaced corrupted strings in course mode selector, table headers (`����/���`), action button states (`����/�༭/�޸�`), and exercise-edit modal labels/placeholders/buttons.
  - No API contract or data model changes; UI copy only.
- What was learned:
  - The reported ������ item ���롱 came from corrupted frontend literals in the admin page, not from latest DB course payload encoding.
- Next steps / open questions:
  - If any old stored course text itself is still mojibake, add a one-time DB repair script and ingest-time encoding guard.

## 2026-03-10 (L5��ɻ��������Դ��ʽ����)
- What changed:
  - Updated `duoxx/src/components/exercises/MultipleChoiceExercise.tsx` to stop forcing line breaks after numeric prefix + em-dash in question stems.
  - New behavior keeps stems in one line (unless source already contains explicit newline), and normalizes legacy pattern like `4. ��Hello` to `4. Hello`.
- What was learned:
  - The visible `4.` / `Hello` split is caused by frontend formatting logic, not user action.
  - Additional content integrity risk exists in source CSV: unquoted commas in exercise stem rows can split fields and truncate stems during import.
- Next steps / open questions:
  - Introduce strict source schema and pre-import lint gate for exercise rows to reject malformed CSV before build/import.
  - Optionally migrate authoring format to JSONL/Markdown+YAML canonical assets, with CSV generated as review/export view only.

## 2026-03-10 (L5 line-break fix + reviewed question-asset workflow)
- What changed:
  - Fixed exercise source row answers in `duoxx/resources/7U/人教版七年级上册Unit 1 Good morning! 知识点及练习题（csv格式�?csv` (rows 76-78) so strict import no longer fails on missing answer.
  - Re-verified `duoxx/scripts/course-import/import-7u.js` strict export path; `npm run course:export:7u:mongo-csv` now completes with `0 warnings`.
  - Confirmed exported row for L5 Q4 remains single-line stem in `duoxx/resources/7U/.build/mongo-csv/pep_g7_upper_u1/exercise_items.csv`.
  - Added reusable text asset workflow files:
    - `duoxx/resources/7U/review/question-assets.ndjson`
    - `duoxx/resources/7U/review/question-assets.template.ndjson`
    - `duoxx/resources/7U/review/QUESTION_ASSET_WORKFLOW.md`
  - Added asset tooling scripts:
    - `duoxx/scripts/course-import/lint-question-assets.js`
    - `duoxx/scripts/course-import/export-approved-assets.js`
  - Extended Mongo CSV importer `duoxx/scripts/course-import/import-mongo-course-csv.js` with `--extra-exercise-csv` to append reviewed exercise CSV rows before apply.
  - Added npm scripts in `duoxx/package.json`:
    - `course:assets:lint:7u`
    - `course:assets:export:7u`
- What was learned:
  - Current line-break symptom had two layers: frontend normalization bug and upstream CSV row-quality risk.
  - A stable NDJSON source-of-truth with review status metadata is safer than direct authoring in mixed CSV when English commas are frequent.
  - Incremental reviewed import can be done without changing existing DB schema by exporting approved assets into `exercise_items.csv` shape and appending via importer flag.
- Next steps / open questions:
  - Integrate reviewed-asset ingestion into admin `/admin/pipeline` save/approve flow (UI-triggered approval writeback).
  - Consider moving legacy `Unit1.csv` authoring to NDJSON canonical assets and generating CSV artifacts only for compatibility.
## 2026-03-10 (7U data re-apply to MongoDB)
- What changed:
  - Re-exported `resources/7U/.build/mongo-csv` with strict mode: 0 warnings.
  - Applied overwrite import to MongoDB in WSL environment using:
    - `node scripts/course-import/import-mongo-course-csv.js --input resources/7U/.build/mongo-csv --extra-exercise-csv resources/7U/.build/review/exercise_items.reviewed.csv --apply --mongo-uri mongodb://127.0.0.1:27017 --mongo-db duoxx`
  - Import result: units=1, lessons=8, knowledge=158, exercises=61, lesson_items=219 (all matched+modified).
  - Verified DB row `exercise_id=ex_44c1334112de39cce860` stem is single-line `4. —Hello, Li Ming! —______, Zhang Wei!`.
- What was learned:
  - Windows shell lacked `mongosh` binary; running import inside WSL resolves apply step reliably.
- Next steps / open questions:
  - If app page still shows old text, verify it is reading DB API path instead of local static fallback cache.
## 2026-03-10 (pipeline course list meta chips in Chinese)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` course list item meta display.
  - Replaced raw `en | active` subtitle text with icon chips in Chinese.
  - Added language/status tag resolvers (`resolveCourseLanguageTag`, `resolveCourseStatusTag`).
  - Moved meta chips to the start of the course tag line (above title) per operator request.
  - Added styles for compact meta chips (`courseTagsRow`, `courseTag`, `courseTagText`).
- What was learned:
  - Operator-facing metadata is clearer as visual tags than raw code text strings.
- Next steps / open questions:
  - If needed, make chip colors status-specific (e.g., active=green, draft=orange, archived=gray).
## 2026-03-10 (pipeline duplicate-course fix + one-time cleanup)
- What changed:
  - Backend service `contentPipelineService.listCourses` now dedupes active courses by natural key (`title + language`) and keeps latest (`version desc, updatedAt desc`).
  - Backend service `contentPipelineService.writeCourse` now defaults to update mode when `courseId` is absent but an active same-title+language course exists; create mode is used only when no match exists.
  - Relaxed `validateCourseWritePayload` to allow write requests without `sourceJobId` when they can resolve to update path by natural key.
  - Added one-time cleanup script `duoxx_server/scripts/cleanup-duplicate-courses.js` and npm script `cleanup:duplicate-3500`.
  - Executed cleanup for duplicated `15天背完初�?500词` records: kept `_id=69aaafeac973401a4d7270e7`, deleted `_id=69aaa910f4ab43d6ef08b80b`.
  - Updated one source job reference from deleted course id to kept course id.
- What was learned:
  - Duplicate active courses were caused by create-on-write behavior when `courseId` was omitted.
  - Even after data cleanup, list-side dedupe is still needed as a guardrail.
- Next steps / open questions:
  - Optional hard constraint: add DB-level unique policy for active natural key (title+language+status) after evaluating migration risk.
## 2026-03-10 (pipeline move course language/status tags to content header)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to remove language/status chips from the left course list items.
  - Added selected-course language/status chips in the content-area header, positioned directly after `Single / Multiple` controls.
  - Added memoized selected-course tag data (`selectedCourseLanguageTag`, `selectedCourseStatusTag`).
  - Added header chip styles (`batchCourseMetaRow`, `batchCourseMetaChip`, `batchCourseMetaText`) and removed unused list chip styles.
- What was learned:
  - Operators expect metadata near active controls/context, not on the browsing list row.
- Next steps / open questions:
  - If needed, allow clicking these header chips to open quick filter by language/status.
## 2026-03-10 (pipeline exercise type chips forced to Chinese)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` `formatExerciseTypeTag()` to enforce Chinese labels across all courses.
  - Added type-key normalization for common English variants (`fill blank`, `multiple choice`, `true/false`, etc.).
  - Preserved override only when it is already Chinese text; otherwise mapped to Chinese dictionary.
  - Added fallback Chinese label `��������` for unknown types.
- What was learned:
  - Type labels can vary by source metadata; normalization before mapping is needed for consistent operator UI language.
- Next steps / open questions:
  - If needed, externalize type mapping to a shared constants file for backend/frontend parity.
## 2026-03-10 (pipeline merge course edit into content header)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to remove per-item `Edit` button from the left course list.
  - Merged course edit form rendering into the right content page header area (inline, same page), instead of a separate edit-only view.
  - Added `Edit Course` trigger in the content header (near Single/Multiple and course meta tags).
  - Reused existing save/delete course handlers; now edit flow is scoped to currently selected course.
- What was learned:
  - Operators expect course-level operations in the active content context, not in the browsing list row.
- Next steps / open questions:
  - If needed, collapse inline edit form into a one-line compact mode on small screens.
## 2026-03-10 (pipeline remove left list edit and inline course edit in content header)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to remove `Edit` action from each left course list item.
  - Refactored main content panel to always render one scroll context; removed separate edit-only branch.
  - Course edit form now renders inline at the top of selected course content area.
  - Added `Edit Course` trigger button in content header controls (near mode/meta chips).
  - Course save/delete actions now operate against selected course context in the inline header form.
- What was learned:
  - Keeping course edit in the same content context reduces navigation mode switching and aligns with operator expectation.
- Next steps / open questions:
  - If needed, collapse inline edit block by default on mobile to reduce vertical space.
## 2026-03-11 (courses page include dynamic published courses)
- What changed:
  - Added public learning list API in backend: `GET /api/learning/courses` via `duoxx_server/src/routes/learning.js`.
  - Added dynamic lesson-id scheme `vocab-course-<courseMongoId>` and lesson resolver for `/api/learning/lesson-exercise/:lessonId`.
  - Added `vocabulary_card -> fill_blank` runtime mapping in backend learning route so large vocab courses can be practiced in existing lesson-exercise screen.
  - Frontend `duoxx/app/(tabs)/courses.tsx` now merges local static metadata with remote `/api/learning/courses` results and dedupes by normalized title.
  - Frontend learning API expanded in `duoxx/src/services/learningApi.ts` with `getCourses()`.
- What was learned:
  - `/courses` tab previously only rendered static metadata from `src/data/courses/index.ts`, so DB-published courses (including `15天背完初�?500词`) could never appear.
  - Using `vocab-course-<id>` keeps existing route rule (`startsWith('vocab-')`) unchanged while enabling direct DB course binding.
- Next steps / open questions:
  - Consider adding server-side filtering/sorting for `/api/learning/courses` (by tag/category) when course count grows.
  - Consider dedicated `vocabulary_card` UI component to replace current fill-blank compatibility mapping.
## 2026-03-11 (pipeline header edit icon + mode relocation)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx`.
  - Removed standalone `Edit Course` button from content toolbar.
  - Added inline course title edit in content header (edit icon + save/cancel).
  - Moved course answer mode switch to the left of `Select Page`.
  - Repaired multiple corrupted UI text literals that were breaking TSX parsing.
  - Verified with `npx eslint components/admin/PipelineDashboard.tsx` (pass).
- What was learned:
  - Encoding-corrupted literals can break unrelated JSX parsing later; normalized literals keep admin page stable.
  - Operators prefer contextual editing in the content header instead of separate edit entry points.
- Next steps / open questions:
  - Optional: centralize admin labels/constants to reduce future literal corruption and duplication.
## 2026-03-11 (backend startup log timestamp)
- What changed:
  - Updated `duoxx_server/src/app.js` startup banner to print:
    - `Started At (local)`
    - `Started At (ISO)`
    - `Process PID`
- What was learned:
  - With nodemon hot reload, timestamp + PID makes each restart immediately identifiable in console logs.
- Next steps / open questions:
  - Optional: add an incrementing boot counter persisted in memory for even faster visual diff.
## 2026-03-11 (unique course sequence id courseNo)
- What changed:
  - Added DB-level unique course sequence field `courseNo` in `duoxx_server/src/models/PublishedCourse.js`.
  - Implemented atomic allocator via Mongo `counters` collection (`published_course_no`) and pre-save hook for new `PublishedCourse` docs.
  - Added unique partial index on `courseNo` to guarantee uniqueness when present.
  - Updated `contentPipelineService.listCourses` projection to include `courseNo`.
  - Added one-time backfill script `duoxx_server/scripts/backfill-course-no.js` and npm script `backfill:course-no`.
  - Executed backfill successfully for current DB.
- What was learned:
  - Existing course docs had no sequential business id; Mongo `_id` uniqueness alone did not satisfy operator serial-number requirement.
  - Counter+pre-save hook provides concurrency-safe incremental numbering across all create paths (write/publish/sync scripts).
- Next steps / open questions:
  - Optional: expose `courseNo` in admin UI left list for easier operator communication.
## 2026-03-11 (vocab course 3500 lesson sanitization + reusable cleaned asset)
- What changed:
  - Added runtime sanitizer `duoxx/src/services/learningLessonSanitizer.ts` for remote lesson payloads.
  - Added course-specific asset patch table `duoxx/src/data/vocabCourseAssetPatches.ts` (drop 13 known dirty rows for `vocab-course-69aaafeac973401a4d7270e7`).
  - Updated `duoxx/app/lesson-exercise/[lessonId].tsx` to sanitize remote vocab lessons and fallback to local cleaned asset when needed.
  - Enhanced `duoxx/src/components/exercises/FillBlankExercise.tsx`: if sentence has no `___`, render standalone answer input.
  - Added reusable cleaned asset export script `duoxx/scripts/course-import/export-vocab-lesson-clean.js` and npm script `course:assets:export:vocab-clean`.
  - Generated reusable asset file `duoxx/resources/vocab/15day3500.cleaned.lesson.json` (+ README).
  - Added tests `duoxx/src/services/__tests__/learningLessonSanitizer.test.ts`.
- What was learned:
  - Root cause is mixed data quality in remote vocab lesson payloads: all fill-blank prompts lacked `___`, and some category-summary rows were imported as exercises.
  - A sanitizer+patch model provides immediate runtime stability while preserving a path to upstream data source repair.
- Next steps / open questions:
  - Move the same cleaning rules to backend course export/import so mobile/web no longer needs runtime patching.
  - Add a strict lint gate for vocab assets before publishing to avoid summary/meta rows entering exercise collections.
## 2026-03-11 (pipeline show courseNo in admin list)
- What changed:
  - Updated `duoxx/src/services/pipelineApi.ts` `CourseDraft` type to include optional `courseNo`.
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to show each course row serial label (`��ţ�<courseNo>`), with fallback `��ţ�--` when missing.
- What was learned:
  - Backend already returns `courseNo`, but missing frontend type/rendering made the unique sequence invisible to operators.
- Next steps / open questions:
  - Optional: add sort/filter by `courseNo` in left list when course count grows.
## 2026-03-11 (pipeline list courseNo inline before title)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` course list title rendering to single-line `courseNo. title` format.
  - Removed separate second-line serial display (`��ţ�N`) from each course row.
- What was learned:
  - Operators scan left list faster with one-line primary text; serial number should behave as title prefix, not metadata row.
- Next steps / open questions:
  - Optional: support click-to-copy `courseNo` for operator communication.
## 2026-03-11 (vocab FSRS-lite state machine + lesson route integration)
- What changed:
  - Added `duoxx/src/services/vocabMemoryService.ts` implementing a FSRS-lite memory scheduler:
    - state machine: `new -> learning -> review -> mastered`
    - per-word state persistence in AsyncStorage
    - review grading (`again/hard/good/easy`) based on correctness + response time
    - due-time scheduling and lesson exercise prioritization
  - Added tests `duoxx/src/services/__tests__/vocabMemoryService.test.ts`.
  - Integrated scheduler into `duoxx/app/lesson-exercise/[lessonId].tsx` for `vocab-*` routes:
    - reorder lesson exercises by due priority before lesson start
    - record each fill-blank vocab review after answer submission
    - preserve route compatibility with existing lesson engine
- What was learned:
  - Existing app flow can keep the generic `LessonEngine`; only pre-session ordering + post-answer review recording is needed to support spaced vocabulary review.
  - A lightweight FSRS-like model works as a drop-in without changing current exercise component contracts.
- Next steps / open questions:
  - Add ��today due / new / mastered�� counters to lesson header UI for transparency.
  - Optional backend sync API for vocab memory states to support multi-device continuity.
## 2026-03-11 (pipeline header alignment: title + edit icon + answer mode)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` content header layout.
  - Moved answer mode controls (`ѡ��/����/�Ի�`) from batch toolbar into the title row.
  - Title row now renders in order: title -> edit icon -> answer mode buttons.
  - Removed duplicated old quick-mode block from batch action area.
- What was learned:
  - Keeping title-level metadata/actions in one horizontal row improves scan speed and reduces perceived control distance.
- Next steps / open questions:
  - Optional: if header becomes crowded on narrow widths, collapse answer mode buttons into a compact dropdown.
## 2026-03-11 (data quality constitution + executable vocab asset gate)
- What changed:
  - Added executable lint gate script `duoxx/scripts/course-import/lint-vocab-clean-asset.js`.
  - Added package scripts:
    - `course:assets:lint:vocab-clean`
    - `course:assets:verify:vocab-clean`
  - Added governance docs:
    - `duoxx/DATA_QUALITY_CONSTITUTION.md`
    - `duoxx/BACKEND_DATA_REMEDIATION_CHECKLIST.md`
  - Updated `duoxx/resources/vocab/README.md` with lint/constitution references.
  - Verified gate end-to-end via `npm run course:assets:verify:vocab-clean` (PASS).
- What was learned:
  - A written "constitution" only works if tied to fail-fast scripts that can block publish/import paths.
- Next steps / open questions:
  - Wire `course:assets:verify:vocab-clean` into CI workflow and backend release checklist as required gate.
## 2026-03-11 (pipeline title edit input compact width)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` header editing layout so only the title region turns into an input.
  - Kept mode buttons visible in the same header row during title editing.
  - Removed `flex: 1` from `contentTitleInput` and applied dynamic width clamp to prevent full-row input expansion.
- What was learned:
  - In this header layout, `flex: 1` on TextInput overrides intended compact behavior and stretches edit mode across the row.
- Next steps / open questions:
  - Optional: convert dynamic width to measured text width for exact pixel-fit behavior.
## 2026-03-11 (lesson exercise header shows course name at top-left)
- What changed:
  - Updated `duoxx/app/lesson-exercise/[lessonId].tsx` header layout.
  - Added course title display (`lessonData.lesson.title`) to the top-left area, next to close button.
  - Added single-line truncation style to avoid squeezing progress/hearts area.
- What was learned:
  - For lesson pages with long titles, `flex: 1 + minWidth: 0 + numberOfLines=1` is required to keep center/right header controls stable.
- Next steps / open questions:
  - Optional: add a lightweight subtitle (e.g., unit/lesson) only when backend provides stable metadata fields.
## 2026-03-11 (frontend backend-fallback ban enforced in lesson flow)
- What changed:
  - Updated `duoxx/app/lesson-exercise/[lessonId].tsx` to remove runtime backend-data fallback behavior for vocab routes:
    - removed sanitizer/static JSON fallback path
    - added strict backend contract validation (`exerciseModuleIds` alignment, fill_blank placeholder, answers non-empty)
    - backend payload invalid/missing now enters explicit error state instead of silent fallback
    - header/completion title now uses course display title with priority `source.title` -> `lesson.title`
  - Updated `duoxx/src/components/exercises/FillBlankExercise.tsx` to remove no-placeholder input fallback; now shows contract error and disables submit when `___` missing.
  - Updated `duoxx/DATA_QUALITY_CONSTITUTION.md` to ban frontend data-healing fallback, with only presentation-only mapping (e.g., i18n labels) as allowed exception.
- What was learned:
  - Silent frontend fallback hides backend contract drift and causes title/data inconsistency across list/detail pages.
  - Contract-first rendering (fail-fast + telemetry/error state) makes backend ownership boundaries enforceable.
- Next steps / open questions:
  - Remove/retire remaining sanitizer patch artifacts (`learningLessonSanitizer`, static cleaned lesson fallback asset references) once backend import pipeline is fully fixed.
  - Add CI grep/lint rule to block future reintroduction of runtime lesson fallback patterns.
## 2026-03-11 (strict no-backend-fallback follow-up)
- What changed:
  - Updated `duoxx/app/(tabs)/courses.tsx` to remove title-normalization dedupe of remote courses, avoiding frontend masking of backend duplicate-course problems.
  - Updated `duoxx/src/components/exercises/fillBlankAnswerMode.ts` to stop injecting fallback correct options into choice lists when backend options are wrong/missing.
  - Updated `duoxx/src/components/exercises/fillBlankAnswerMode.test.ts` expectation to reflect non-healing behavior.
- What was learned:
  - Even ��friendly merge/dedupe�� and ��auto-insert correct option�� logic can hide backend data defects and delay true root-cause fixes.
- Next steps / open questions:
  - Sweep other backend-facing modules and classify fallback types into:
    1) allowed presentation-only mapping
    2) forbidden data-healing fallback requiring removal.
## 2026-03-11 (courses tab backend-only source)
- What changed:
  - Updated `duoxx/app/(tabs)/courses.tsx` to stop merging local static course metadata.
  - Courses tab now uses only backend `/api/learning/courses` response as the source of truth.
  - On API failure, course list falls back to empty list instead of static local list.
- What was learned:
  - Duplicate course cards were caused by local+remote merge, not remote API duplicates.
- Next steps / open questions:
  - If backend availability is a concern, add a user-visible retry/empty-state action for failed course fetch.
## 2026-03-11 (backend-only course rendering refactor)
- What changed:
  - Updated `duoxx/app/(tabs)/courses.tsx` to load course cards from backend only (`/api/learning/courses`), removing local static merge.
  - Refactored `duoxx/app/lesson-exercise/[lessonId].tsx` to backend-only lesson loading:
    - removed local static fallback (`SAMPLE_LESSONS` and `g7-upper-practice` fallback path)
    - always fetches `learningApi.getLessonExercise(lessonId)`
    - keeps contract validation and vocab memory prioritization
  - Replaced `duoxx/app/lesson/[courseId].tsx` with a lightweight redirect page to `/lesson-exercise/:courseId`, removing static `src/data/courses` dependency from lesson route.
- What was learned:
  - Duplicate cards were caused by local+remote merge; once local source is removed and lesson route is backend-driven, the course experience has a single source of truth.
  - Legacy file encoding in `lesson-exercise` can break patch tooling; safer edits were done via controlled text replacement.
- Next steps / open questions:
  - `src/data/courses/*` is now effectively unused by runtime flow and can be deleted in a follow-up cleanup PR.
  - If backend `/api/learning/lesson-exercise/:lessonId` is unavailable, add a user-facing retry CTA in error state.
## 2026-03-11 (settings child-account create no-response fix)
- What changed:
  - Updated `duoxx/app/settings.tsx` child-account creation flow.
  - Added visible inline form error state (`childFormError`) so Web users get feedback even when `Alert.alert` is not prominent.
  - Added pre-submit validation in UI (parent phone, username pattern, password length).
  - Added `catch` branch for unexpected runtime exceptions during create.
  - Disabled create button when not parent account or request is in-flight.
- What was learned:
  - The primary ��no response�� UX came from relying on modal alert feedback only; in some environments this appears as silent failure.
- Next steps / open questions:
  - Optional: add a success inline toast/banner in settings for consistency with error feedback.
## 2026-03-11 (constitution update + cloze upstream contract)
- What changed:
  - Updated `AGENTS.md`:
    - implementation principle switched to root-cause-first (not minimal-patch-first)
    - added `Product/Data Constitution (Mandatory)` covering source-first correction and contract-first publishing
  - Added `duoxx/docs/CLOZE_UPSTREAM_DATA_CONTRACT.md`:
    - canonical cloze schema (tokens + gap spans)
    - CSV authoring fields (`tokens_json`, `gaps_json`)
    - import lint/publish constraints and migration steps
- What was learned:
  - Current `___` marker contract is too brittle for vocabulary-derived fill tasks; canonical gap spans are more stable and review-friendly.
- Next steps / open questions:
  - Implement linter/importer support for `cloze` schema and retire marker-based fallback in runtime payload mapping.
## 2026-03-11 (pipeline token-expired state sync)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to enforce auth-state sync on API 401/invalid-token errors across major actions, not only course list/detail loading.
  - Reused `handleAuthError()` in upload/process/write, course edit, answer mode update, TTS generation, exercise item save, batch delete, and explanation draft generate/save/approve/reject flows.
  - Added auth-check handling in `loadPendingDrafts()` and upload catch path (`error.message`) to avoid stale Logout UI state after token expiry.
  - Verified targeted lint via `npm run lint -- --no-error-on-unmatched-pattern components/admin/PipelineDashboard.tsx app/admin/pipeline.tsx` (pass).
- What was learned:
  - Fixing only initial load paths is insufficient; token expiry can happen during any admin action and must always trigger a unified logout transition.
- Next steps / open questions:
  - Optional: centralize unauthorized handling in a shared API-layer interceptor to remove duplicated component-level checks.
## 2026-03-11 (pipeline JSX unicode text-node rendering fix)
- What changed:
  - Updated `duoxx/components/admin/PipelineDashboard.tsx` to replace JSX text-node unicode escape literals with real display text.
  - Fixed table headers and row action text that rendered as raw `\\uXXXX` in UI:
    - `����`, `���`, row action `�༭`, selection mark `?`.
  - Verified with targeted lint: `npm run lint -- --no-error-on-unmatched-pattern components/admin/PipelineDashboard.tsx` (pass).
- What was learned:
  - In JSX text nodes, `\\uXXXX` is rendered literally; unicode escape decoding only happens inside JS string literals/expressions.
- Next steps / open questions:
  - Optional: run a repo grep for `>\\u[0-9a-fA-F]{4}` on admin components to prevent similar regressions.
## 2026-03-11 (settings child-account create feedback fix)
- What changed:
  - Updated `duoxx/app/settings.tsx` child-account create feedback flow.
  - Fixed root cause where form error state was immediately cleared after submit attempt, making failures look like "button flashed but nothing happened".
  - Added stable inline success message (`childFormSuccess`) shown under create toggle, so success feedback does not rely on modal alert rendering.
  - Added local duplicate-username precheck before service call, with explicit inline error.
  - Input changes now clear feedback via dedicated handlers instead of effect-based auto-reset.
  - Verified via:
    - `npm run lint -- --no-error-on-unmatched-pattern app/settings.tsx`
    - `npm test -- src/services/__tests__/familyAccountService.test.ts`
- What was learned:
  - Effect-driven feedback reset coupled with `childFormError` dependency can instantly erase fresh errors and create a false "no-op" experience.
- Next steps / open questions:
  - Optional: normalize/replace mojibake service error strings in `familyAccountService` to ensure readable fallback messages everywhere.
## 2026-03-11 (settings child-account password visibility + list simplification)
- What changed:
  - Updated `duoxx/app/settings.tsx` child-account UX:
    - create-form password now supports show/hide toggle (eye icon), and generated password auto-switches to visible.
    - child account list no longer duplicates display name and username when they are identical.
    - list now shows password line with reveal toggle and added `��������` action per child account.
    - reset action generates a new password, persists it, and shows inline success feedback.
  - Updated `duoxx/src/models/user.ts`:
    - added optional `ChildAccount.passwordPlain` (local-only retrieval field).
  - Updated `duoxx/src/services/familyAccountService.ts`:
    - `createChildAccount()` now stores `passwordPlain` alongside hash.
    - added `resetChildPassword()` API to rotate child password and persist new plain+hash.
  - Updated tests `duoxx/src/services/__tests__/familyAccountService.test.ts`:
    - verify plain password persistence on create.
    - verify reset password flow updates stored password state.
  - Verified:
    - `npm run lint -- --no-error-on-unmatched-pattern app/settings.tsx src/services/familyAccountService.ts src/services/__tests__/familyAccountService.test.ts src/models/user.ts`
    - `npm test -- src/services/__tests__/familyAccountService.test.ts`
- What was learned:
  - Parent-side retrieval requires explicit local password retention or reset workflow; hash-only storage cannot support "show existing password" UX.
- Next steps / open questions:
  - Optional hardening: replace `passwordPlain` with encrypted local storage keychain-based field if security requirements increase.
## 2026-03-12 (settings child-account delete flow cross-platform fix)
- What changed:
  - Updated `duoxx/app/settings.tsx` child-account delete flow to split into confirm + execute steps:
    - `confirmDeleteChildAccount(account)`
    - `executeDeleteChildAccount(account)`
  - Added cross-platform confirmation behavior:
    - Web uses `globalThis.confirm(...)`
    - Native keeps `Alert.alert(...)` with Promise-based resolve.
  - Replaced delete-result alert-only feedback with inline feedback (`childFormError` / `childFormSuccess`) so users always see outcome.
  - Added row-scoped delete loading key (`delete:<childId>`) and in-row `ɾ����...` state, avoiding prior global lock behavior from `Boolean(childActionLoadingId)`.
  - Kept UI structure minimal (same delete entry icon), no extra modal/card layer.
  - Added automated coverage in `duoxx/src/services/__tests__/familyAccountService.test.ts`:
    - `deletes child account and shrinks child list`.
- What was learned:
  - `Alert.alert` button callback flow is unreliable for RN Web confirmation UX; explicit Web confirm branch avoids no-op deletion behavior.
- Next steps / open questions:
  - Optional: extract confirm helper into shared utility for consistent cross-platform destructive actions across pages.
## 2026-03-12 (settings child-account inline success messages removed)
- What changed:
  - Updated `duoxx/app/settings.tsx` to remove all inline success messages in child-account area.
  - Removed `childFormSuccess` state, all `setChildFormSuccess(...)` writes, success `<Text>` rendering block, and `childFormSuccessText` style.
  - Kept inline error feedback (`childFormError`) unchanged.
  - Kept create/reset/delete behavior and delete confirm/loading logic unchanged.
  - Verified via `npm run lint -- --no-error-on-unmatched-pattern app/settings.tsx`.
- What was learned:
  - For this screen, users prefer low-noise operation feedback and only want actionable error visibility.
- Next steps / open questions:
  - Optional: if needed later, replace removed success text with subtle non-persistent toast to avoid layout shifts.
## 2026-03-12 (swipe entry merged into learning tab; standalone tab removed)
- What changed:
  - Updated `duoxx/app/(tabs)/_layout.tsx` to remove the standalone `swipe-learning` bottom tab; tabs now keep `ѧϰ / �γ� / �ҵ�` only.
  - Updated `duoxx/app/(tabs)/index.tsx` to add a high-priority `ˢ��ѧӢ��` card at the top of the learning page tool section, routing to `/modules/swipe-learning`.
  - Deleted legacy route file `duoxx/app/(tabs)/swipe-learning.tsx` (no compatibility route retained).
  - Updated `duoxx/src/modules/swipe-learning/screens/SwipeLearningScreen.tsx` to fully own the swipe screen implementation in module layer and removed reverse dependency on app route files.
- What was learned:
  - Keeping `app route -> src module` one-way ownership avoids route-level coupling and makes future navigation refactors lower-risk.
- Next steps / open questions:
  - Optional: add a small route-level smoke test to assert `/modules/swipe-learning` remains reachable after future tab/layout changes.
## 2026-03-12 (sentence-rule decoupling v1 cutover)
- What changed:
  - Backend learning contract switched to snapshot protocol:
    - Reworked `duoxx_server/src/routes/learning.js` to serve lesson data from `LessonItemRef + ExerciseSnapshot + SentenceAsset`.
    - `/api/learning/lesson-exercise/:lessonId` now returns fill_blank using `sentence + tokens + gaps` (no `___` dependency).
    - `/api/learning/courses` now dedupes by `title+language` and keeps latest version.
  - Added backend migration/import tooling:
    - `duoxx_server/scripts/migrate-sentence-rule-v1.js` (one-time migration from `PublishedCourse.payload` to sentence/rule/snapshot/ref chain).
    - `duoxx_server/scripts/lint-sentence-rule-csv.js` (hard lint gate for `sentences.csv`, `rules.csv`, `lesson_bindings.csv`).
    - `duoxx_server/scripts/import-sentence-rule-csv.js` (CSV import + precompile snapshot upsert).
    - `duoxx_server/scripts/lib/sentenceRuleCsv.js` (CSV parsing/normalization helpers).
    - Added npm scripts in `duoxx_server/package.json` for migrate/lint/import.
  - Added backend tests:
    - `duoxx_server/src/services/sentenceRuleCompiler.test.js`
    - `duoxx_server/src/routes/learning.test.js`
  - Frontend fill_blank protocol migrated:
    - Extended `duoxx/src/models/exercise.ts` with `FillBlankToken` and `FillBlankGap`.
    - Reworked `duoxx/src/components/exercises/FillBlankExercise.tsx` to render by token-gap protocol (legacy marker fallback retained only for old data safety).
    - Updated `duoxx/src/validators/fillBlankValidator.ts` and test for multi-gap validation.
    - Updated `duoxx/src/services/lessonEngine.ts` to accept `string | string[]` fill_blank answers.
    - Rewrote `duoxx/app/lesson-exercise/[lessonId].tsx` contract validation to require `tokens+gaps` for fill_blank modules.
- What was learned:
  - Frontend `___` placeholder contract is the direct source of runtime drift; token-gap protocol is stable and replay-friendly.
  - Running backend test commands from Windows UNC path breaks `cross-env`; invoke through WSL shell (`wsl ... bash -lc`).
- Next steps / open questions:
  - Run `migrate:sentence-rule-v1 --apply` on target DB and verify key lesson IDs now resolve through snapshot refs.
  - Publish authoritative CSV templates/examples for `sentences.csv/rules.csv/lesson_bindings.csv` to upstream content producers.
  - Remove remaining legacy marker assumptions from non-core modules once old payloads are fully migrated.
## 2026-03-12 (/admin/pipeline rule-template enhancement v2 baseline)
- What changed:
  - Backend V2 rule/candidate domain added:
    - `duoxx_server/src/models/ExerciseCandidate.js` (candidate review queue).
    - `duoxx_server/src/models/SentenceAssetChange.js` (sentence asset change review).
    - Extended `duoxx_server/src/models/RuleTemplate.js` with `targetSelector`, `outputSpec`, `operatorIds`, `difficultyPolicy`, `enabled`.
    - Updated `duoxx_server/src/models/index.js` exports.
  - Backend engine + service flow implemented:
    - Added `duoxx_server/src/services/ruleTemplateEngine.js` (rule matching + candidate generation, supports one-sentence multi-rule matches and multi-outputSpec outputs).
    - Extended `duoxx_server/src/services/contentPipelineService.js` with:
      - sentence asset list/create/change-review methods,
      - rule template CRUD/version/state methods,
      - preview generation (`preview` non-persistent),
      - candidate generation/list/update/approve/reject,
      - approval writes `ExerciseSnapshot + LessonItemRef`.
  - Backend routes expanded in `duoxx_server/src/routes/pipeline.js`:
    - `GET/POST /sentence-assets`,
    - `PATCH /sentence-assets/:sentenceId/changes`,
    - `GET /sentence-asset-changes`, `POST .../:changeId/approve|reject`,
    - `GET/POST /rule-templates`,
    - `POST /rule-templates/:ruleId/version`,
    - `PATCH /rule-templates/:ruleId/:ruleVersion`,
    - `POST /rule-templates/:ruleId/preview`,
    - `POST /rule-templates/:ruleId/generate-candidates`,
    - `GET/PATCH/POST /exercise-candidates*` review endpoints.
  - Frontend API + admin UI connected:
    - Extended `duoxx/src/services/pipelineApi.ts` with sentence asset/rule template/preview/candidate APIs and types.
    - Added `duoxx/components/admin/RuleTemplateWorkbench.tsx` (Rules / Preview / Candidates / Sentences tabs).
    - Updated `duoxx/components/admin/PipelineDashboard.tsx` to expose `Rules V2` entry and open the new workbench modal.
- What was learned:
  - Existing `PipelineDashboard.tsx` had multiple historical mojibake fragments that can silently break JSX parsing; linting this file after edits is mandatory.
  - One-to-many matching should stay server-side so preview and candidate review remain deterministic and traceable per `ruleId/ruleVersion`.
- Next steps / open questions:
  - Add dedicated integration tests for new pipeline endpoints (`preview`, `generate-candidates`, `approve/reject`).
  - Extend review UI with bulk approve/reject and inline draftPayload editor safety checks (JSON schema validation).
  - Add formal migration/backfill script for existing rule templates to populate new V2 fields consistently.
## 2026-03-12 (/admin/pipeline v2 follow-up: batch candidate review + evidence view)
- What changed:
  - Backend:
    - Added batch review endpoint `POST /api/pipeline/exercise-candidates/bulk-review` in `duoxx_server/src/routes/pipeline.js`.
    - Added service method `bulkReviewExerciseCandidates()` in `duoxx_server/src/services/contentPipelineService.js`, supporting batch approve/reject with per-item failure isolation.
  - Frontend API:
    - Extended `duoxx/src/services/pipelineApi.ts` with `bulkReviewExerciseCandidates()` and `BulkReviewExerciseCandidatesResult`.
  - Frontend admin UI:
    - Upgraded `duoxx/components/admin/RuleTemplateWorkbench.tsx` candidate tab with:
      - multi-select checkboxes,
      - `Select All` toggle,
      - `Batch Approve` / `Batch Reject` actions,
      - explicit column layout (`Rule`, `KnowledgePoint`, `Sentence`, `Status`, `Actions`).
    - Upgraded preview tab to display match evidence summary (`reason`, token indices, condition summary).
- What was learned:
  - Batch review should continue-on-error and return partial failures instead of aborting whole operation.
  - For reviewer efficiency, evidence summary and source-rule columns are more important than dense card details.
- Next steps / open questions:
  - Add candidate JSON inline edit modal with schema guard (to prevent malformed payload writes).
  - Add backend integration tests for `bulk-review` (approve + reject + mixed-failure cases).
## 2026-03-12 (rule-v2 demo seed script)
- What changed:
  - Added backend seed script `duoxx_server/scripts/seed-rule-v2-demo.js`.
  - Script seeds 3 demo `SentenceAsset` rows + 3 demo `RuleTemplate` rows for `/admin/pipeline` Rules V2 walkthrough.
  - Added npm scripts in `duoxx_server/package.json`:
    - `seed:rule-v2-demo:dry` (dry-run)
    - `seed:rule-v2-demo` (apply)
  - Seed script prints direct admin workflow steps (`Preview -> Generate Candidates -> Batch Approve`).
- What was learned:
  - Dry-run default and explicit `--apply` prevents accidental data writes during onboarding.
- Next steps / open questions:
  - Optional: add `--course-id` flag to auto-print a concrete `lessonId=vocab-course-{courseId}` in seed output.
## 2026-03-12 (Rules V2 Studio simplification: sentence+rule direct apply)
- What changed:
  - Reworked `duoxx/components/admin/RuleTemplateWorkbench.tsx` to add a default `Studio` tab.
  - `Studio` now shows:
    - sentence list (search + single select),
    - rule list (search + multi-select + select-all),
    - generated exercise series panel (preview candidates + generated candidate rows).
  - Added direct actions for operators:
    - `Preview Selected` (runs selected rules against selected sentence),
    - `Generate Series` (creates candidates for selected sentence + selected rules).
  - Kept existing advanced tabs (`Rules/Preview/Candidates/Sentences`) for power workflows.
- What was learned:
  - Operators need one-screen "select sentence + select rules + see output" flow; rule-json forms are still useful but should be secondary.
  - Running per-selected-rule requests keeps behavior explicit and avoids "all rules" noise in daily testing.
- Next steps / open questions:
  - Add optional "apply to multiple sentences" batch mode in Studio (currently single-sentence apply for clarity).
  - Consider adding server-side aggregated endpoint to reduce client fan-out calls when many rules are selected.
## 2026-03-12 (Rules V2 Studio usability fixes)
- What changed:
  - Updated `duoxx/components/admin/RuleTemplateWorkbench.tsx` Studio view UX:
    - removed sentenceId display from sentence rows,
    - increased sentence text size for readability,
    - removed confusing secondary line (`translation`) from sentence list rows,
    - added auto-preview trigger when sentence/rule selection changes,
    - added empty-state hint when no preview/candidate result.
  - Normalized file encoding to UTF-8 without BOM and resolved hook/lint warnings.
- What was learned:
  - Operators expect selection to immediately show preview results; requiring manual preview click causes “no result�?confusion.
- Next steps / open questions:
  - Optional: add an explicit toggle `Auto Preview On/Off` for very large rule sets.
## 2026-03-12 (Rules V2 Studio readability + non-match visibility)
- What changed:
  - Updated `duoxx/components/admin/RuleTemplateWorkbench.tsx`:
    - sentence rows now show `ID: {sentenceId}` with neutral style,
    - sentence/rule/generated panels use larger text for readability,
    - preview logic now always shows per-selected-rule result rows (including non-match / no-candidate reasons) instead of blank panel,
    - cleaned Studio labels and empty-state guidance text.
- What was learned:
  - Empty preview panel is interpreted as failure by operators; explicit non-match rows are required for trust and debugging.
- Next steps / open questions:
  - Optionally add explicit `match-only` filter toggle in generated panel to hide unmatched rows when needed.
## 2026-03-12 (Rules V2 not-found root cause fix)
- What changed:
  - Backend compatibility fix in `duoxx_server/src/services/contentPipelineService.js`:
    - preview/generate rule queries changed from strict `{ status: 'active', enabled: true }`
      to tolerant filters `{ status: { $ne: 'archived' }, enabled: { $ne: false } }`.
  - This allows legacy rules with missing `status/enabled` fields to be found by preview and candidate generation.
- What was learned:
  - Rule list and preview used inconsistent default semantics, causing "visible in list but not found in preview" failures.
- Next steps / open questions:
  - Optional one-time data cleanup: backfill missing `status/enabled` on historical `RuleTemplate` docs.
## 2026-03-12 (Rules V2 generated exercise readability: question + answer)
- What changed:
  - Enhanced `duoxx/components/admin/RuleTemplateWorkbench.tsx` Studio generated panel to render per-candidate details:
    - question prompt,
    - resolved answer (supports `correctAnswer`, `answer`, and cloze `gaps[].answers` fallback),
    - options list when present.
  - Added helper utilities `toStringArray()` and `resolveCandidateAnswer()` for robust payload parsing.
- What was learned:
  - Operators need review output in assessment form (question/answer/options), not only metadata and prompt summary.
- Next steps / open questions:
  - Optional: add an "Edit draft payload" quick action for inline correction before candidate approval.

## 2026-03-12 (adaptive recommendation V1 in lesson flow)
- What changed:
  - Implemented backend adaptive learning production loop in `duoxx_server`:
    - Added models: `src/models/LearningAttempt.js`, `src/models/UserKnowledgeState.js`, `src/models/UserSnapshotState.js`, `src/models/LearningSession.js`.
    - Exported new models in `src/models/index.js`.
    - Added feature flags in `src/config/index.js`: `learning.adaptiveV1Enabled`, `learning.adaptiveV1RolloutPercent`.
    - Added service `src/services/adaptiveLearningService.js` with session start/submit/state logic, 80/20 candidate pools, scoring, no-repeat window, fallback chain, and knowledge/snapshot state updates.
    - Added tests `src/services/adaptiveLearningService.test.js`.
    - Extended learning routes in `src/routes/learning.js`:
      - `POST /api/learning/sessions/start`
      - `POST /api/learning/sessions/:sessionId/submit`
      - `GET /api/learning/sessions/:sessionId/state`
      - kept existing lesson endpoint for compatibility fallback.
  - Implemented frontend adaptive lesson integration in `duoxx`:
    - Extended API client `src/services/learningApi.ts` with adaptive session request/response types and methods.
    - Updated `app/lesson-exercise/[lessonId].tsx`:
      - logged-in users attempt adaptive session start and render `firstExercise`
      - local grading for `multiple_choice` and `fill_blank` before submit
      - submit to adaptive API and render returned `nextExercise`
      - explicit mastery feedback controls (`mastered`/`not_mastered`)
      - recommendation reason UI
      - runtime fallback to static lesson sequence when adaptive submit fails.
- What was learned:
  - Current project has unrelated global TypeScript errors, so full `tsc --noEmit` is not a reliable gate for this feature; targeted lint/tests are required per-change.
  - WSL backend tests must be run inside WSL path to avoid Windows UNC + `cross-env` execution failures.
- Next steps / open questions:
  - Add route-level integration tests for `/sessions/start|submit|state` happy path and 401/403/409 branches.
  - Add analytics dashboards for adaptive metrics (completion rate, repeat rate, API latency) aligned with rollout stages.
  - Decide default production values for `LEARNING_ADAPTIVE_V1_ENABLED` and rollout percentage before full user rollout.
- Addendum:
  - Added adaptive start-route tests in `duoxx_server/src/routes/learning.test.js` (401 + success path).
  - Added env defaults in `duoxx_server/.env.development` for adaptive rollout control.
## 2026-03-13 (V3 architecture reserve: tasks, prescriptions, learner capability)
- What changed:
  - Extended backend content/learning domain to reserve the V3 architecture:
    - Added models in `duoxx_server/src/models/`:
      - `KnowledgeNode.js`
      - `KnowledgeEdge.js`
      - `TaskBlueprint.js`
      - `LearningPrescription.js`
      - `UserCapabilityState.js`
      - `DocumentAsset.js`
      - `ParagraphAsset.js`
    - Extended existing models:
      - `SentenceAsset.js` now includes document/paragraph linkage, sequence, syntax frames, sentence pattern, knowledge node refs, scene tags, tags, quality status.
      - `ExerciseSnapshot.js` now reserves `true_false`, `primaryKnowledgeNodeId`, `knowledgeNodeIds`, `assetRefs`, `diagnosticRole`, `trainingMode`, `interactionMode`, `sceneTags`, `explanationNodeIds`, `qualityScore`.
      - `LearningSession.js` now includes `prescriptionId`, `sourceTaskId`, `entryType`, `entryId`.
    - Updated `duoxx_server/src/models/index.js` exports.
  - Extended backend services/routes:
    - `duoxx_server/src/services/contentPipelineService.js`:
      - sentence asset normalization upgraded to new schema
      - added `list/createKnowledgeNode`
      - added `getKnowledgeGraph`
      - added `list/createTaskBlueprint`
      - added `getCoverageOverview`
    - `duoxx_server/src/routes/pipeline.js`:
      - added `/knowledge-nodes`
      - added `/knowledge-nodes/:nodeId/graph`
      - added `/task-blueprints`
      - added `/coverage/overview`
    - `duoxx_server/src/services/adaptiveLearningService.js`:
      - adaptive sessions now create `LearningPrescription`
      - added lightweight `UserCapabilityState` update on answer submit
      - added `getLatestPrescription()`
    - `duoxx_server/src/routes/learning.js`:
      - session start/state now return `prescriptionId`
      - added `/api/learning/prescriptions/latest`
      - added `/api/learning/tasks/blueprints`
      - `true_false` snapshots now map to renderable multiple-choice output
  - Extended frontend API and entry scaffolding:
    - `duoxx/src/services/learningApi.ts`:
      - added `LearningPrescriptionSummary`
      - added `TaskBlueprintSummary`
      - added `getLatestPrescription()`
      - added `getTaskBlueprints()`
    - `duoxx/src/services/pipelineApi.ts`:
      - extended `SentenceAsset`
      - added `KnowledgeNode`, `KnowledgeEdge`, `KnowledgeGraphResponse`, `TaskBlueprint`, `CoverageOverview`
      - added knowledge/task/coverage API methods
    - `duoxx/app/(tabs)/index.tsx`:
      - added new top-level growth entry cards:
        - `��������Ĵ���`
        - `���������޸�`
        - `��ʼ����ѵ��`
    - Added new pages:
      - `duoxx/app/prescriptions.tsx`
      - `duoxx/app/weakness-workbench.tsx`
      - `duoxx/app/task-training.tsx`
- What was learned:
  - The safest upgrade path is not replacing the current adaptive service, but layering `Prescription` and `CapabilityState` onto it so lesson-based flow still works.
  - `KnowledgeNode` must serve both retrieval and explanation/navigation, so it cannot stay as a flat tag list.
  - Reserving `trainingMode` and `interactionMode` now avoids a later hard break when exercises move from recognition-only into output/conversation tasks.
- Next steps / open questions:
  - Wire admin `/admin/pipeline` UI to the new `Knowledge / Tasks / Coverage` APIs.
  - Add first real `TaskBlueprint` seed data and connect task entry to session start beyond lesson-only mode.
  - Build a unified frontend knowledge detail container that uses graph traversal instead of per-component explanation payloads.
## 2026-03-13 (V3 admin UI follow-up: Architecture Workbench)
- What changed:
  - Added `duoxx/components/admin/ArchitectureWorkbench.tsx` as a new admin workbench for V3 reserve layers.
  - The workbench provides 3 flat operational views:
    - `Knowledge`: list/create knowledge nodes, inspect graph preview.
    - `Tasks`: list/create task blueprints, inspect task detail.
    - `Coverage`: query snapshot coverage by knowledge node / task / scene tag.
  - Integrated the new workbench into `duoxx/components/admin/PipelineDashboard.tsx` via a new header entry button `Architecture` and a dedicated fullscreen modal, parallel to existing `Rules V2`.
- What was learned:
  - `/admin/pipeline` should expose new architecture layers in independent workbenches instead of mixing them into the already-dense course content table.
  - For this stage, flat lists + create forms + graph/coverage inspection are enough; a visual graph editor would add complexity before the graph model is fully operationalized.
- Next steps / open questions:
  - Connect `KnowledgeNode` selection in exercise review and explanation flows.
  - Add `TaskBlueprint -> start training` bridge once session start supports task entry directly.
  - Add coverage drill-down from aggregated counts to specific `ExerciseSnapshot` rows.
## 2026-03-13 (candidate review chain upgraded with knowledge node metadata)
- What changed:
  - Extended backend `ExerciseCandidate` in `duoxx_server/src/models/ExerciseCandidate.js` with:
    - `primaryKnowledgeNodeId`
    - `knowledgeNodeIds[]`
    - `diagnosticRole`
    - `trainingMode`
    - `interactionMode`
    - `sceneTags[]`
  - Extended `duoxx_server/src/services/contentPipelineService.js`:
    - candidate generation now seeds review metadata from `SentenceAsset` + exercise type defaults
    - candidate patch API now supports updating knowledge/training metadata
    - candidate approval now persists these fields into `ExerciseSnapshot`
    - approved snapshot now writes:
      - `primaryKnowledgeNodeId`
      - `knowledgeNodeIds[]`
      - `assetRefs`
      - `diagnosticRole`
      - `trainingMode`
      - `interactionMode`
      - `sceneTags[]`
      - `qualityScore`
  - Extended `duoxx_server/src/routes/pipeline.js` candidate patch route to accept the new metadata fields.
  - Extended frontend `duoxx/src/services/pipelineApi.ts` candidate types and update payload.
  - Upgraded `duoxx/components/admin/RuleTemplateWorkbench.tsx` candidate review tab:
    - added candidate review editor
    - operator can now edit prompt / explanation / answers / options
    - operator can now assign `primaryKnowledgeNodeId`, `knowledgeNodeIds`, `diagnosticRole`, `trainingMode`, `interactionMode`, `sceneTags`
    - candidate rows now expose `Edit` before `Approve/Reject`
- What was learned:
  - Knowledge graph and coverage APIs are not enough by themselves; candidate approval is the real seam where graph metadata must enter the formal snapshot pool.
  - Default training profiles can be derived safely from current exercise type (`cloze`, `multiple_choice`, `true_false`) without blocking future richer task modes.
- Next steps / open questions:
  - Replace free-text role/mode inputs in candidate editor with constrained pickers for safer operator UX.
  - Surface `knowledgeNodeIds` and `diagnosticRole` directly in lesson/exercise analytics and coverage drill-down.
  - Add backend tests for candidate patch/approve persistence of new snapshot metadata.
## 2026-03-13 (coverage drill-down + constrained candidate metadata editor)
- What changed:
  - Extended backend pipeline service/routes for snapshot drill-down:
    - `duoxx_server/src/services/contentPipelineService.js` added `listExerciseSnapshots()` with filters:
      - `knowledgeNodeId`
      - `taskId`
      - `sceneTag`
      - `status`
    - `duoxx_server/src/routes/pipeline.js` added `GET /api/pipeline/exercise-snapshots`.
  - Extended frontend pipeline API in `duoxx/src/services/pipelineApi.ts`:
    - added `ExerciseSnapshotSummary`
    - added `listExerciseSnapshots()`.
  - Upgraded `duoxx/components/admin/ArchitectureWorkbench.tsx` coverage tab:
    - coverage query now loads both aggregate distributions and matching snapshot rows
    - added `Matched Snapshots` panel for operational drill-down from aggregate coverage to concrete published exercises.
  - Upgraded `duoxx/components/admin/RuleTemplateWorkbench.tsx` candidate editor UX:
    - replaced free-text editing for `diagnosticRole`, `trainingMode`, `interactionMode` with constrained selector rows (pill buttons)
    - keeps operator freedom where needed, but prevents malformed enum values from entering formal snapshots.
- What was learned:
  - Coverage without drill-down is not enough for operators; they need to see which exact snapshots satisfy or miss a filter.
  - The highest-risk review metadata should use constrained UI, not raw text inputs, otherwise the formal snapshot pool degrades quickly.
- Next steps / open questions:
  - Add `Matched Snapshots -> open in course/lesson context` deep links.
  - Replace free-text taskType/nodeType creation fields with constrained selectors as well.
  - Add route/service tests for `GET /api/pipeline/exercise-snapshots` filtering semantics.
## 2026-03-13 (admin workbench follow-up)
- Changed files + intent:
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: constrained `KnowledgeNode.nodeType` and `TaskBlueprint.taskType` to fixed selectors; added `Open Course Context` action from Coverage snapshot rows.
  - `duoxx/components/admin/PipelineDashboard.tsx`: wired snapshot context jump back into course detail pane; converted `clearExplanationReviewContext`, `loadCourseDetails`, and `loadPendingDrafts` to stable callbacks to remove hook dependency warnings.
- Learned:
  - Coverage drill-down is materially more useful when operators can jump straight from a snapshot row back to the owning course context.
  - Workbench creation forms should use constrained enums instead of free text; otherwise metadata quality degrades quickly.
- Next steps / open questions:
  - Add direct snapshot management view in `/admin/pipeline` instead of relying only on candidate approval and coverage drill-down.
  - Add editable graph edges for `KnowledgeNode` so knowledge navigation is not read-only.
## 2026-03-13 (snapshot workbench)
- Changed files + intent:
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: added `Snapshots` tab for direct published snapshot operations; supports search/filter, detail view, metadata editing, archive/reactivate, and course-context jump.
  - `duoxx/src/services/pipelineApi.ts`: extended snapshot list filters and payload fields; added `updateExerciseSnapshot` client.
  - `duoxx_server/src/services/contentPipelineService.js`: extended snapshot listing filters and fields; added snapshot update service for prompt/explanation/knowledge/training/status metadata.
  - `duoxx_server/src/routes/pipeline.js`: added `PATCH /api/pipeline/exercise-snapshots/:snapshotId` and expanded snapshot list query support.
- Learned:
  - Published snapshot management needs to live beside coverage, not behind candidate review only; operators need direct archive/reactivate and metadata fixes on the final pool.
  - Snapshot search must include prompt/course/lesson/snapshotId to be operationally usable.
- Next steps / open questions:
  - Add dedicated snapshot option/answer editor for multiple-choice and true/false payloads.
  - Add graph-edge editing in `Knowledge` so node graph is not read-only.
## 2026-03-13 (task session + graph edges + snapshot payload editor)
- Changed files + intent:
  - `duoxx/app/task-training.tsx`: cleaned text encoding and added direct task start CTA; task training now routes into the shared exercise screen and is gated for logged-in users.
  - `duoxx/app/lesson-exercise/[lessonId].tsx`: generalized exercise screen to support `entryType=task` in addition to lesson entry without duplicating the UI.
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: added knowledge-edge create/archive controls and expanded snapshot editor to handle multiple-choice/true-false `options + correctAnswer` payloads.
  - `duoxx/src/services/learningApi.ts`: adaptive session start now supports task entry payload.
  - `duoxx/src/services/pipelineApi.ts`: added knowledge-edge CRUD clients and expanded snapshot update payload.
  - `duoxx_server/src/services/adaptiveLearningService.js`: adaptive engine now supports `task` sessions via task-derived snapshot pools and task-scoped prescriptions.
  - `duoxx_server/src/routes/learning.js`: session start endpoint now accepts `entryType=task` + `taskId`.
  - `duoxx_server/src/services/contentPipelineService.js`: added knowledge-edge CRUD service methods and snapshot update support for options/correctAnswer.
  - `duoxx_server/src/routes/pipeline.js`: added knowledge-edge routes and expanded snapshot update route payload.
  - `duoxx_server/src/routes/learning.test.js`: added task-session route coverage.
- Learned:
  - Task training can reuse the same exercise renderer if session bootstrap is generalized; the real split is session context, not screen UI.
  - Snapshot operations need two layers: metadata editing for all snapshots and payload editing only for choice-based snapshots; cloze gap truth still belongs upstream.
  - Knowledge graph editing becomes operationally useful once the operator can create and archive edges directly from the selected node context.
- Next steps / open questions:
  - Add upstream cloze gap editor based on sentence tokens rather than editing snapshot payload directly.
  - Add task start CTA inside admin `Tasks` workbench as a preview/debug path for operators.
  - Add snapshot revision history or stale markers when upstream assets change and published snapshots need re-review.
## 2026-03-13 (cloze token editor + stale snapshots)
- Changed files + intent:
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: added task debug start button, snapshot `reviewState` filter/display, token-based cloze gap editor, and sentence-asset-backed contiguous gap selection.
  - `duoxx/components/admin/PipelineDashboard.tsx`: wired task debug launch from admin architecture workbench.
  - `duoxx/src/services/pipelineApi.ts`: added sentence asset detail fetch, snapshot reviewState/gaps fields, and richer snapshot list/update filters.
  - `duoxx_server/src/models/ExerciseSnapshot.js`: added `reviewState` schema field.
  - `duoxx_server/src/services/contentPipelineService.js`: added sentence asset detail getter, snapshot review-state/gap normalization, stale-marking on sentence asset approval, and richer snapshot list/update logic.
  - `duoxx_server/src/routes/pipeline.js`: added `GET /sentence-assets/:sentenceId` and expanded snapshot routes for `reviewState`/`gaps`.
- Learned:
  - Cloze editing must stay token-driven; free-form answer editing creates data drift immediately.
  - Snapshot stale marking is a necessary bridge between upstream asset corrections and published exercise operations.
  - Admin task debugging is easiest when it reuses the public task training route rather than building a second debug-only renderer.
- Next steps / open questions:
  - Add sentence-asset review queue UI so stale snapshots can be paired with the exact upstream change that triggered them.
  - Add snapshot stale bulk actions (`mark fresh`, `archive stale`, `rebuild from upstream`) for operators managing larger pools.
  - Add richer graph edge editing (change endpoints/weights/metadata) beyond status toggling.
## 2026-03-13 (sentence change review + stale bulk ops + edge editor)
- Changed files + intent:
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: completed `Changes` tab UI for sentence-asset review, added impacted snapshot drill-down, added stale snapshot bulk actions in `Snapshots`, and upgraded `Knowledge` edge handling from status toggle to editable target/relation/weight editor.
  - `duoxx/src/services/pipelineApi.ts`: added snapshot batch update client and expanded admin clients used by sentence-change review / stale snapshot operations.
  - `duoxx_server/src/services/contentPipelineService.js`: `updateKnowledgeEdge` now persists endpoint changes; added `batchUpdateExerciseSnapshots` for operator bulk status/review-state updates.
  - `duoxx_server/src/routes/pipeline.js`: added `POST /api/pipeline/exercise-snapshots/batch-update`.
  - `duoxx_server/src/services/contentPipelineService.test.js`: added coverage for editable knowledge-edge updates and snapshot batch updates.
- Learned:
  - Once sentence-asset review exists, operators immediately need the linked published snapshots in the same pane; otherwise stale-state is visible but not actionable.
  - Bulk snapshot operations should target the current filtered stale set, not require per-row patching.
  - Knowledge graph maintenance is only practical when operators can correct edge endpoints, not just archive broken links.
- Next steps / open questions:
  - Add `rebuild from upstream` for stale cloze snapshots, driven by current sentence tokens and rule/compiler truth rather than manual snapshot edits.
  - Add richer sentence-change diff coverage (tokens / pattern / knowledgeNodeIds), not just text/translation/pattern.
  - Add pipeline route tests for the new batch snapshot endpoint and sentence-change review path.
## 2026-03-13 (stale cloze rebuild from upstream)
- Changed files + intent:
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: added `Rebuild Stale Cloze` action in `Snapshots`, scoped to current stale cloze result set.
  - `duoxx/src/services/pipelineApi.ts`: added `rebuildExerciseSnapshotsFromUpstream()` client.
  - `duoxx_server/src/services/contentPipelineService.js`: added token-derived gap answer rebuild helper and `rebuildExerciseSnapshotsFromUpstream()` for cloze snapshots only.
  - `duoxx_server/src/routes/pipeline.js`: added `POST /api/pipeline/exercise-snapshots/rebuild-from-upstream`.
  - `duoxx_server/src/services/contentPipelineService.test.js`: added cloze rebuild coverage.
  - `duoxx_server/src/routes/pipeline.test.js`: added route coverage for snapshot batch update + rebuild-from-upstream.
- Learned:
  - `rebuild from upstream` must stay separate from generic batch status updates; it changes question truth, not just operator state.
  - Rebuilding cloze safely only needs current `SentenceAsset.tokens` plus existing gap ranges; it should not silently touch non-cloze snapshots.
- Next steps / open questions:
  - Extend sentence-change review diff to show `tokens / knowledgeNodeIds / sceneTags` changes, not just text/translation/pattern.
  - Add per-result summary UI for rebuild responses (`rebuilt / skipped / failed`) instead of only alert-based feedback.
  - Consider a controlled `rebuild from upstream` path for non-cloze snapshots only after defining rule/compiler truth for those types.
## 2026-03-13 (changes diff + rebuild result panel)
- Changed files + intent:
  - `duoxx/components/admin/ArchitectureWorkbench.tsx`: expanded sentence-change review with `knowledgeNodeIds / sceneTags / tokens` before/after visibility and a compact field-level diff summary; added `Last Rebuild Result` panel for stale cloze rebuild feedback.
- Learned:
  - Operators need a compact diff verdict (`changed/same`) alongside raw before/after values; otherwise large sentence metadata changes are hard to scan.
  - Rebuild actions need an in-page result summary, not just alert-based failure output, or operators cannot quickly assess what was rebuilt vs skipped.
- Next steps / open questions:
  - Surface full rebuild result rows with direct links back to affected snapshots instead of truncating to the first few entries.
  - Add token-level visual diff for sentence-change review when token arrays are large.
## 2026-03-13 (pipeline dashboard init-order fix)
- Changed files + intent:
  - `duoxx/components/admin/PipelineDashboard.tsx`: moved the `selectedCourse -> loadPendingDrafts` effect below the `loadPendingDrafts` callback definition to remove the web runtime `Cannot access 'loadPendingDrafts' before initialization` crash.
- Learned:
  - `useEffect` can still trip temporal-dead-zone issues when it captures `const useCallback` values declared later in the component body; this is a runtime initialization-order bug, not a hooks-rule bug.
- Next steps / open questions:
  - If `/admin/pipeline` still throws after refresh, inspect the next callback/effect pair for the same initialization-order pattern.
## 2026-03-13 (architecture workbench stale filter + task empty state)
- Changed files + intent:
  - duoxx/components/admin/ArchitectureWorkbench.tsx: clarified the Tasks empty state so operators understand that Start Task Debug appears only after a task blueprint exists and is selected.
  - duoxx_server/src/services/contentPipelineService.js: fixed listExerciseSnapshots() so 
eviewState is applied to the snapshot query; stale filtering now returns only stale snapshots.
  - duoxx_server/src/services/contentPipelineService.test.js: added test coverage asserting the snapshot query receives the requested 
eviewState.
- Learned:
  - The grey stale actions were caused by a backend filter bug rather than the button logic; once corrected, zero-result stale views are represented honestly.
  - Admin task-debug affordances need an explicit empty-state explanation or operators read the missing CTA as a broken feature.
- Next steps / open questions:
  - Seed one minimal TaskBlueprint and one stale snapshot set so the Architecture Workbench is directly demoable without manual data prep.
## 2026-03-13 (operator review hub)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: added a simplified operator-first admin hub with four tabs: Import, Knowledge Review, Exercise Review, and Task Review.
  - duoxx/components/admin/PipelineDashboard.tsx: added Review Hub entry, renamed Architecture entry to Advanced, and wired operator actions to upload source / advanced mode / task debug.
- Learned:
  - The internal architecture models can remain, but the default operator surface must hide graph-building and task-blueprint authoring; operators should review machine suggestions rather than model the system by hand.
  - Exercise candidate review is the only currently complete review chain, so it should be promoted to the main admin path before knowledge/task review status APIs are expanded.
- Next steps / open questions:
  - Add true review statuses and approve/reject APIs for knowledge suggestions and task packs so the review hub can become fully review-driven instead of read-mostly for those two tabs.
  - Merge the old Rules V2 candidate-review workflow into the new Exercise Review once parity is complete.
## 2026-03-16 (review hub knowledge/task approval flow)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: upgraded Knowledge Review and Task Review from read-only pages into real review flows with status filters, edit fields, and Approve / Reject / Save Edit actions.
  - duoxx/src/services/pipelineApi.ts: added 
eviewStatus support plus update/approve/reject APIs for KnowledgeNode and TaskBlueprint.
  - duoxx_server/src/models/KnowledgeNode.js: added 
eviewStatus and 
eviewMeta.
  - duoxx_server/src/models/TaskBlueprint.js: added 
eviewStatus and 
eviewMeta.
  - duoxx_server/src/services/contentPipelineService.js: added list filtering by 
eviewStatus, plus update/review methods for knowledge nodes and task blueprints; rejected suggestions now archive automatically.
  - duoxx_server/src/routes/pipeline.js: added PATCH, pprove, and 
eject routes for knowledge nodes and task blueprints.
  - duoxx_server/src/services/contentPipelineService.test.js: added coverage for knowledge approval and task update flows.
- Learned:
  - The operator-first admin surface only becomes credible once non-exercise suggestions have real review state transitions; read-only summaries are not enough.
  - Rejected machine suggestions should leave the active pool immediately, otherwise the review queue never truly shrinks.
- Next steps / open questions:
  - Move auto-generated knowledge/task items into explicit suggestion generation pipelines rather than relying on all active records as a proxy queue.
  - Merge the old Rules V2 candidate review UI into Review Hub -> Exercise Review after feature parity is complete.

## 2026-03-16 (review hub suggestion pipeline + demo seed + exercise review parity)
- Changed files + intent:
  - duoxx_server/src/services/contentPipelineService.js: added regenerateReviewHubSuggestions() and seedReviewHubDemoData() for explicit operator suggestion queue + demo bootstrap.
  - duoxx_server/src/routes/pipeline.js: added POST /api/pipeline/review-hub/suggestions/refresh and POST /api/pipeline/review-hub/seed-demo.
  - duoxx_server/src/routes/pipeline.test.js: added route tests for both new review-hub endpoints.
  - duoxx_server/src/services/contentPipelineService.test.js: added tests for suggestion generation and demo seeding behavior.
  - duoxx/src/services/pipelineApi.ts: added regenerateReviewHubSuggestions() and seedReviewHubDemoData() API clients.
  - duoxx/components/admin/OperatorWorkbench.tsx: Import tab got Generate Suggestions + Seed Demo Data; Exercise Review now supports multi-select, batch approve/reject, preview, and metadata editing parity.
- Learned:
  - Review Hub needs explicit bootstrap actions so operators can instantly get testable queues and stale snapshots without manual model crafting.
  - Exercise review parity with Rules V2 mostly depends on batch operations and metadata controls.
- Next steps / open questions:
  - Add candidate de-dup guard for repeated sentenceId + ruleId + ruleVersion + exerciseType pending rows.
  - Add provenance badges in review lists so operators can see evidence source without opening Advanced.
## 2026-03-16 (review-first hardening: candidate dedup + provenance + authoring gate)
- Changed files + intent:
  - duoxx_server/src/services/contentPipelineService.js: added exercise-candidate dedup refresh (pending upsert / approved skip), candidate identifier resolver (_id or candidateId), and richer generate result counts (created/updated/skipped).
  - duoxx_server/src/services/contentPipelineService.test.js: added tests for pending dedup refresh, approved-skip behavior, and candidateId-based approval lookup.
  - duoxx/components/admin/OperatorWorkbench.tsx: single-item candidate actions now use stable mongo _id; added evidence/provenance badges in candidate list/detail.
  - duoxx/components/admin/RuleTemplateWorkbench.tsx: added evidence badges and generation stats (created/updated/skipped); fixed Preview button handler typing.
  - duoxx/components/admin/ArchitectureWorkbench.tsx: added default review-only gate for Knowledge/Tasks authoring (Advanced OFF by default, explicit enable required).
  - duoxx/src/services/pipelineApi.ts: added GenerateRuleCandidatesResult type with optional updated/skipped counts.
- Learned:
  - Candidate queue noise is primarily from repeated generate operations; dedup at generation time reduces operator load more than downstream cleanup.
  - Candidate APIs should resolve both business id (candidateId) and mongo id for backward compatibility across old/new admin surfaces.
  - Defaulting Architecture Workbench to review-only significantly lowers operator cognitive load while preserving advanced controls.
- Next steps / open questions:
  - Add backend unique index strategy for candidate dedup signature to harden against race conditions.
  - Add explicit ��skip reason�� visibility in RuleTemplateWorkbench when generation returns skippedCount.
  - Evaluate moving more day-to-day operations from Architecture Workbench into Review Hub to keep Advanced truly exceptional.
## 2026-03-16 (candidate dedup DB hardening + conflict recovery)
- Changed files + intent:
  - duoxx_server/src/models/ExerciseCandidate.js: added partial unique index uniq_pending_candidate_signature for pending candidate signature (sentenceId+courseId+lessonId+exerciseType+ruleId+ruleVersion).
  - duoxx_server/src/services/contentPipelineService.js: added duplicate-key recovery path in generateExerciseCandidates(); on 11000 it refreshes existing pending candidate instead of failing. Added helpers for dedup query, duplicate-key detection, and pending candidate refresh mapping.
  - duoxx_server/src/services/contentPipelineService.test.js: added race/conflict test ensuring duplicate-key create is recovered into pending refresh; all candidate dedup tests pass.
- Learned:
  - Service-layer dedup is not enough under concurrent writes; DB-level uniqueness for pending queue is required to make generation idempotent.
  - Recovering duplicate-key errors in service keeps operator workflows smooth even during concurrent generate actions.
- Next steps / open questions:
  - Add one-off cleanup script for historical pending duplicates before enabling index in already-dirty datasets.
  - Consider adding reason-level skipped breakdown (pproved_exists, duplicate_recovered) for studio UX transparency.
## 2026-03-16 (pending candidate cleanup script)
- Changed files + intent:
  - duoxx_server/scripts/cleanup-duplicate-pending-candidates.js: added one-off dedup script for pending queue by candidate signature; dry-run by default, --apply to delete duplicate rows.
- Learned:
  - Enforcing a new partial unique index is safer with an explicit pre-clean path for historical datasets.
- Next steps / open questions:
  - Run script in target env before first restart after index rollout if duplicate pending rows already exist.
## 2026-03-16 (generation transparency: skipped reasons)
- Changed files + intent:
  - duoxx_server/src/services/contentPipelineService.js: generateExerciseCandidates() now returns skippedReasons map for operator-visible diagnostics.
  - duoxx/src/services/pipelineApi.ts: GenerateRuleCandidatesResult extended with optional skippedReasons.
  - duoxx/components/admin/RuleTemplateWorkbench.tsx: studio panel now shows skipped reason breakdown (e.g. pproved_exists).
- Learned:
  - Operators need actionable skip diagnostics, not only aggregate skipped counts, to trust auto-generation behavior.
- Next steps / open questions:
  - Add reason codes for more paths (
ule_not_matched, invalid_sentence_asset) if needed for deeper triage.
## 2026-03-16 (import wizard v2: csv/markdown/txt via admin page)
- Changed files + intent:
  - `duoxx/components/admin/OperatorWorkbench.tsx`: implemented Import Wizard V2 (5-step flow) in Review Hub -> Import: upload source, structure parse, business validation, draft review edit, and publish action; added format-aware template download buttons for CSV/MD/TXT.
  - `duoxx/src/services/pipelineApi.ts`: added `import-jobs` API contracts and clients (`createImportJob`, `parseImportJob`, `validateImportJob`, `getImportJobDraft`, `updateImportJobDraft`, `publishImportJob`) and file-type detection (`detectImportFileType`).
  - `duoxx/src/services/adminImportWizard.ts`: added local strict parser/validator helpers for structured Markdown/TXT protocol and draft-level validation/merge summary.
  - `duoxx/components/admin/PipelineDashboard.tsx`: wired `onImportPublished` callback so course list refreshes after successful publish.
  - `duoxx/src/services/__tests__/adminImportWizard.test.ts`: added focused tests for structured text parsing and strict validation behavior.
- Learned:
  - Import UX must work with both new `import-jobs` APIs and legacy fallback path during backend rollout; otherwise admin workflow blocks.
  - Local parser/validator in frontend is useful for immediate operator feedback, but publish gate truth should still come from backend `validate/publish` once endpoints are fully available.
- Next steps / open questions:
  - Backend repo still needs full implementation of `import-jobs` route family and canonical draft persistence to complete end-to-end production flow.
  - Add backend integration tests for CSV/MD/TXT parity and blocking-error publish rejection.
  - Consider adding import history list (job audit panel) in Review Hub so operators can reopen a previous failed/approved job.## 2026-03-17 (web startup stabilization: operator syntax + SSR-safe progress storage)
- Changed files + intent:
  - `duoxx/src/modules/vocab-recognition/services/vocabProgressService.ts`: guarded constructor/load/save/clear storage access so Expo Web SSR does not touch AsyncStorage before `window` exists.
  - `duoxx/src/modules/sentence-building/services/sentenceProgressService.ts`: applied the same SSR-safe storage guard for sentence progress state.
  - `duoxx/scripts/start-expo-stable.js`: hardened stable Expo startup by keeping dependency validation disabled for weak/offline networks and forcing `BROWSER=none` so restricted shells do not fail on auto-open.
  - `duoxx/package.json`: pointed `npm run web` to the stable startup script instead of raw `expo start --web`.
- Learned:
  - The real startup blockers were not the deprecation warnings; they were `AsyncStorage` access during server render and Expo CLI trying to auto-open a browser in a restricted shell.
  - `OperatorWorkbench.tsx` syntax now passes lint again; the earlier parser failure was no longer reproducible after the file was normalized.
- Next steps / open questions:
  - Remaining `shadow*`, `pointerEvents`, and `expo-av` messages are framework deprecation warnings, not blockers; they should be cleaned in separate focused refactors.
  - If web startup is still flaky on a specific machine, compare local shell/browser policy rather than app code first.## 2026-03-17 (import wizard preview visibility)
- Changed files + intent:
  - `duoxx/components/admin/OperatorWorkbench.tsx`: Import tab middle panel now explains Step 2/Step 3 responsibilities and renders `导入题目预览`, showing each parsed exercise with type, prompt, answer, options, and knowledge point.
- Learned:
  - Operators do not need opaque step-completion messages; they need immediate parsed-question visibility before publish.
  - For import UX, Step 2 is the correct place to surface extracted exercises, while Step 3 should remain validation-only.
- Next steps / open questions:
  - Add per-item inline edit from import preview if operators need to fix individual questions before publish instead of only editing course-level metadata.- 2026-03-17: Reworked `duoxx/components/admin/OperatorWorkbench.tsx` import UX into a flat sidebar + result panel flow. Step 2 now reports only structure/precheck issues, Step 3 shows publish validation, and preview is gated until validation passes. Fixed root-cause parse failure caused by an extra `};` after `runImportValidation`, then normalized the JSX in Import/Knowledge/Exercises/Tasks tabs and removed BOM so ESLint passes cleanly.

## 2026-03-17 (operator import whitelist v1)
- What changed:
  - Rebuilt `duoxx/components/admin/OperatorWorkbench.tsx` into a stable flat workbench after the previous file became partially corrupted and syntactically unsafe.
  - Import tab now uses `upload -> immediate parse/preview -> row edit -> template select -> generate draft -> publish` instead of empty manual Step 2 / Step 3 clicks.
  - Import UI now supports whitelist-first source handling:
    - `word_knowledge_csv`
    - `sentence_asset_csv`
    - markdown/txt structured course fallback
  - Added row-level parsed source preview and editor for word/sentence rows, row selection, filtered template selection, local draft generation, draft save, and publish fallback.
  - Preserved review workflows in the same workbench for:
    - knowledge nodes
    - exercise candidates
    - task blueprints
  - Verified current import pipeline files with lint and kept admin import parser tests green.
- What was learned:
  - The earlier step-based import UI failed because it exposed internal parse stages instead of operator intent; operators need immediate visibility into parsed source rows.
  - The clean boundary is: source preview first, generation template second, draft third.
  - `OperatorWorkbench.tsx` had accumulated encoding and structure damage; replacing the component root-to-leaf was lower risk than trying to keep patching it.
- Next steps / open questions:
  - Add row-level delete/add actions in import preview so operators can finish source correction without leaving the workbench.
  - Add explicit unsupported-CSV diagnostics panel that lists missing required headers for each whitelist protocol.
  - Consider moving markdown/txt draft conversion into `adminImportWizard.ts` so the component stops owning that conversion logic.
- Follow-up:
  - Improved unsupported CSV diagnosis in import UI.
  - Validation now exposes actual headers, missing required headers for both whitelist protocols, legacy knowledge-block hint, and first raw file lines for quick operator debugging.
## 2026-03-18 (word asset v2 import + sentence-first templates)
- Changed files + intent:
  - duoxx/src/services/adminImportWizard.ts: upgraded word import rows into sentence-first word assets with 	ranslationZh, British/American phonetics, structured example sentence tokens, target token anchors, collocations, and audio status placeholders; added new word templates (word_in_sentence_card, word_in_sentence_cloze, word_usage_multiple_choice, word_spelling) and stricter CSV blocking rules requiring example sentence + translation.
  - duoxx/components/admin/OperatorWorkbench.tsx: updated Word CSV template and import editor to new word-asset fields, improved unsupported CSV diagnostics to align with word_asset_csv, and changed row editing to reparse CSV preview after edits so validation/issues refresh instead of staying stale.
  - duoxx/src/services/__tests__/adminImportWizard.test.ts: replaced old word CSV tests with word-asset coverage for token anchors, missing example sentence blocking, and sentence-cloze generation with structured gaps.
- Learned:
  - Word import cannot stay on word -> translation only; once the product goal is sentence-first learning, example sentence and highlight anchors become publish-gate fields, not optional decoration.
  - Re-parsing edited preview rows is the simplest way to keep import validation honest without introducing a second client-side truth model.
- Next steps / open questions:
  - Backend TTS generation is still not wired for British/American audio URLs; current import only stores missing status placeholders.
  - Import preview still lacks row add/delete actions and richer sentence highlight visualization; current editor is functional but not yet operator-polished.
## 2026-03-18 (word csv template encoding + first-row Chinese annotations)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: changed Word CSV template download to include a Chinese annotation row as the first line and forced UTF-8 BOM in template downloads to prevent Chinese mojibake in Excel/Windows text viewers.
  - duoxx/src/services/adminImportWizard.ts: updated CSV parsing to ignore comment rows starting with #, so annotated template files can be uploaded back without breaking whitelist detection.
  - duoxx/src/services/__tests__/adminImportWizard.test.ts: added coverage for annotated first-row Word CSV templates.
- Learned:
  - Template usability and parser contract must change together; adding human-readable annotation rows without comment-row tolerance would immediately break admin re-import.
- Next steps / open questions:
  - If sentence CSV templates also need Chinese annotation rows, apply the same pattern there instead of inventing a separate import behavior.
## 2026-03-18 (word template annotation row simplified)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: simplified the Word CSV template annotation row to positional Chinese labels only, removed repeated ieldName: prefixes, and rewrote the Chinese sample text using unicode escapes so downloaded templates no longer depend on source-file encoding.
- Learned:
  - For downloadable templates, keep source files BOM-free and add BOM only at download time; otherwise lint and editor behavior diverge.
## 2026-03-18 (sentence csv template annotation row)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: updated Sentence CSV Template to use the same first-row Chinese positional annotation pattern as the Word template, with unicode-escaped Chinese sample text to keep downloads stable.
  - duoxx/src/services/__tests__/adminImportWizard.test.ts: added a sentence-template comment-row test so annotated sentence CSV files can round-trip through upload parsing.
- Learned:
  - CSV templates can share the # ע������ + ��ʽ��ͷ�ڶ��� convention safely because the parser now ignores comment rows; Markdown/TXT cannot blindly copy that rule because their protocol starts with frontmatter markers.
## 2026-03-18 (markdown/txt template help blocks)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: added Chinese help/comment blocks to Markdown and TXT import templates without breaking their frontmatter-based protocol; sentence CSV template was also aligned to the comment-row convention.
  - duoxx/src/services/__tests__/adminImportWizard.test.ts: added structured-text tests proving Markdown/TXT templates still parse when operator help comment lines are present after frontmatter.
- Learned:
  - For Markdown/TXT imports, the safe place for operator guidance is inside the body as comment lines after frontmatter, not before the opening --- marker.
## 2026-03-18 (import page protocol guide)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: added an in-page import protocol guide under template download buttons so operators can see required columns and template rules without opening files first.
- Learned:
  - For admin-facing Chinese UI text in source files that have shown encoding instability, unicode escapes are safer than raw literals because the runtime still renders correctly while the source remains stable.
## 2026-03-18 (import entry labels localized)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: localized import entry labels and status metadata labels to Chinese product copy (section title, choose-file CTA, template buttons, legacy/advanced actions, and file status lines) while keeping internal field/protocol names unchanged.
- Learned:
  - Admin discoverability improves when the page explains protocol expectations in the same language as operation labels; this reduces template trial-and-error.
## 2026-03-18 (jsx unicode text rendering fix)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: fixed import-tab label rendering where literal \uXXXX sequences were displayed to users; switched affected JSX text nodes to expression strings (e.g. {'\\u6587...'}) so React decodes unicode escapes correctly at runtime.
- Learned:
  - In JSX text nodes, \uXXXX is not interpreted; unicode escapes must be inside JavaScript string expressions or replaced by actual UTF-8 literals.
## 2026-03-18 (import word v3: sense/example model + single-page review editor)
- Changed files + intent:
  - `duoxx/src/services/adminImportWizard.ts`: upgraded `word_knowledge_csv` parsing from single-value rows to `word + senses[] + examples[]` model; added multi-value protocol (`|| / ;; / |`), sense/example-level validation localization (`rowNumber/senseIndex/exampleIndex`), template availability gating by row capability, legacy single-value word protocol hard block (`LEGACY_WORD_PROTOCOL_BLOCKED`), and word exercise generation by sense/example with metadata (`senseIndex/exampleIndex`).
  - `duoxx/components/admin/OperatorWorkbench.tsx`: updated Word CSV template to v3 protocol, changed required-header diagnostics to `word + translationZh`, and refactored import row editor to structured word editing (word base fields + per-sense + per-example edit/add/remove) with immediate re-parse/re-validation on every change.
  - `duoxx/src/services/__tests__/adminImportWizard.test.ts`: replaced word import tests with v3 coverage (multi-sense parse, legacy protocol block, sense/example mismatch localization, template gating, cloze generation metadata).
  - `duoxx/src/services/pipelineApi.ts`: extended `ImportValidationIssue` with optional `rowNumber/senseIndex/exampleIndex` to carry fine-grained import error location.
  - `duoxx/docs/import-word-v3-contract.md`: added backend contract checklist for preview payload shape, v3 CSV rules, validation localization, and publish audit `protocolVersion`.
- Learned:
  - Word import needs sense-aware structure as source truth; otherwise multi-POS/multi-example data always collapses into lossy single-value patches.
  - Template visibility must be data-capability-driven (sentence templates require anchored examples), not source-kind-only.
  - Running Jest in this environment requires `--runInBand` due worker spawn permission (`EPERM`).
- Verification:
  - `cmd /c npx eslint components/admin/OperatorWorkbench.tsx src/services/adminImportWizard.ts src/services/__tests__/adminImportWizard.test.ts src/services/pipelineApi.ts` passed.
  - `cmd /c npm test -- src/services/__tests__/adminImportWizard.test.ts --runInBand` passed (13/13).
- Next steps / open questions:
  - Backend `import-jobs` routes need to adopt the same v3 preview payload and validation fields for full end-to-end parity.
  - Import UI can further add inline “jump to first blocking issue�?and row-level issue filtering for large files.
## 2026-03-18 (import v3 stabilization: OperatorWorkbench unicode labels + type refs)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: imported missing WordKnowledgeEntryPreview / WordSensePreview types used by sense editor callbacks; fixed five metadata labels that were rendered as literal \\uXXXX to real Chinese (文件/文件类型/来源类型/解析�?状�?.
- Learned:
  - JSX text nodes containing escaped unicode must not be double-escaped; otherwise UI prints raw \\u sequences.
  - Current repo has many pre-existing TS errors outside import flow; targeted lint/test for import module remains green.
- Verification:
  - cmd /c npx eslint components/admin/OperatorWorkbench.tsx src/services/adminImportWizard.ts src/services/__tests__/adminImportWizard.test.ts src/services/pipelineApi.ts passed.
  - cmd /c npm test -- src/services/__tests__/adminImportWizard.test.ts --runInBand passed.
- Next steps / open questions:
  - Optional: migrate all import-page Chinese literals to single style (either direct UTF-8 or \u escapes) to avoid future mixed-encoding regressions.
## 2026-03-18 (word template mojibake fix)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: replaced corrupted WORD_CSV_TEMPLATE content with stable unicode-escaped Chinese text (annotation row + sample rows) and fixed import info labels that had become broken literals.
- Learned:
  - Previous garbled output came from already-corrupted source template constants, not the download BOM logic.
  - Keeping template text as ASCII + \\u escapes avoids editor/encoding-dependent corruption in this file.
- Verification:
  - cmd /c npx eslint components/admin/OperatorWorkbench.tsx passed.
- Next steps / open questions:
  - If needed, apply the same hardening strategy to any other template constants that still contain raw Chinese literals.
## 2026-03-18 (operator import corruption cleanup round 2)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: replaced corrupted word-template Chinese text with stable unicode-escaped content, repaired broken info-grid labels (including one unterminated string) and kept file ASCII-safe.
- Learned:
  - The observed downloaded CSV mojibake can originate from already-corrupted template constants in source; even correct UTF-8 BOM cannot fix that at download time.
- Verification:
  - cmd /c npx eslint components/admin/OperatorWorkbench.tsx passed.
  - cmd /c npm test -- src/services/__tests__/adminImportWizard.test.ts --runInBand still passed.
- Next steps / open questions:
  - If operators still see mojibake, likely from old downloaded files or browser cache; re-download after hard refresh should be the canonical check.
## 2026-03-18 (word template v3 visibility update)
- Changed files + intent:
  - duoxx/components/admin/OperatorWorkbench.tsx: made word template explicitly V3 (added V3 rule comment line + stronger multi-sense/multi-example sample), changed download filename to word_knowledge_template_v3.csv, and fixed template button label rendering.
- Learned:
  - Even with V3 parser in place, operators may perceive "same as old" unless template filename and content clearly communicate version semantics.
- Verification:
  - cmd /c npx eslint components/admin/OperatorWorkbench.tsx passed.
  - cmd /c npm test -- src/services/__tests__/adminImportWizard.test.ts --runInBand passed.
- 2026-03-18
  - Changed:
    - `duoxx/src/services/lexicalAssetImport.ts`: added V4 lexical asset standardization for `entries/senses/examples/relations`, validation, JSONL preview, publish-state marking, and course draft generation from approved assets.
    - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: added new V4 asset import panel with multi-file CSV upload, validation review, semantic preview, asset patching, draft generation, and course publish.
    - `duoxx/components/admin/OperatorWorkbench.tsx`: wired the new V4 panel into `/admin/pipeline` import tab as the default import mode, while keeping legacy course import available.
    - `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`: added coverage for FK validation, target span validation, publish-state transitions, JSONL output, and cloze draft generation.
    - `duoxx/docs/lexical-assets-v4-contract.md`, `duoxx/docs/lexical-assets-v4-postgres.md`: documented the new V4 asset contract and Postgres projection design.
  - Learned:
    - The old import flow in `OperatorWorkbench` is tightly coupled to `adminImportWizard`; isolating V4 into a standalone panel is the safest way to replace the data model without breaking legacy structured-course import.
    - Current backend still only exposes course-write endpoints, so asset publish is front-end-local for now; course publish can already go through `pipelineApi.writeCourse` using V4-generated drafts.
    - Repo-wide TypeScript still has many unrelated pre-existing errors; targeted lint/tests are the reliable gate for this slice until those are cleaned up.
  - Next:
    - Add real backend `asset-packages` endpoints and persist V4 assets into DB/version tables instead of local preview-only publish state.
    - Replace legacy word CSV templates with the new 4-file official lexical asset package templates across the admin UI.
    - Add sentence/document asset V4 flows using the same `asset package -> standard assets -> review -> publish -> course draft` pattern.
## 2026-03-18 (V4 asset package API bridge + backend-first import panel)
- Changed files + intent:
  - `duoxx/src/services/pipelineApi.ts`: added V4 asset-package client methods for `create/standardize/preview/patch/validate/publish/generate-course-draft`.
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: rebuilt the panel cleanly after corruption; it now prefers backend asset-package APIs and falls back to local standardization when backend endpoints are unavailable.
- Learned:
  - The earlier `pipelineApi.ts` failure was caused by PowerShell replacement swallowing template-literal backticks, which broke every generated endpoint path after insertion.
  - For this repo, whole-file rewrite is safer than incremental patching once JSX + encoding corruption starts; targeted `tsc` filtering is a more reliable verification gate than repo ESLint commands that depend on local/global config state.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"pipelineApi.ts" /C:"lexicalAssetImport.ts" /C:"OperatorWorkbench.tsx"` returned no matching errors.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed.
- Next steps / open questions:
  - Wire the real backend `asset-packages` routes so the panel no longer needs local fallback for publish and draft generation.
  - Decide whether to keep Chinese literals in source or convert this panel to ASCII + unicode escapes for long-term encoding stability.## 2026-03-18 (V4 panel stabilization: unicode-safe labels + JSONL export)
- Changed files + intent:
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: normalized operator-facing labels/status strings to unicode-escaped literals so future Windows/PowerShell edits do not corrupt Chinese copy again; also added JSONL download buttons for `entries/senses/examples/relations` previews.
- Learned:
  - In this repo, line-based PowerShell rewrites are safer than bulk string replacement once JSX contains template literals and mixed unicode.
  - `findstr`-filtered `tsc` checks return exit code `1` when there are no matched errors, so the absence of output is the success signal for targeted file gating.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"pipelineApi.ts" /C:"lexicalAssetImport.ts" /C:"OperatorWorkbench.tsx"` produced no matched errors.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed.
- Next steps / open questions:
  - The panel still mixes Chinese UI text with English field ids; consider a small presentation-layer copy map so labels can be changed without touching logic-heavy JSX.
  - The next major step remains real backend `asset-packages` persistence and preview patch routes.## 2026-03-18 (V4 panel compile recovery)
- Changed files + intent:
  - duoxx/components/admin/LexicalAssetImportPanel.tsx: rewrote the trailing styles block to remove duplicated keys, restore styles.column, and keep the V4 import panel in a clean compile state after prior line-based edits.
- Learned:
  - The most fragile point in this panel is the large bottom StyleSheet.create block; once duplicate keys or missing style names appear, targeted filtered 	sc is the fastest reliable gate.
- Verification:
  - cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"pipelineApi.ts" /C:"lexicalAssetImport.ts" /C:"OperatorWorkbench.tsx" produced no matched errors.
  - cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand passed.
- Next steps / open questions:
  - Continue with real backend sset-packages persistence so V4 no longer depends on preview-local publish state.
  - Add a smaller copy/label layer to the V4 panel so future UI text changes do not require editing logic-heavy JSX.
## 2026-03-18 (V4 panel rewrite + copy layer)
- Changed files + intent:
  - duoxx/components/admin/LexicalAssetImportPanel.tsx: rebuilt the V4 panel into a shorter clean implementation after prior JSX corruption; kept backend-first asset-package flow, local fallback, asset editing, JSONL export, draft generation, and course publish.
  - duoxx/src/config/adminLexicalAssetV4.ts: added centralized V4 copy/template download constants so protocol/template changes no longer require editing logic-heavy JSX.
- Learned:
  - In this repo, long incremental PowerShell JSX rewrites are too fragile once template literals are involved; full file regeneration is more reliable than line surgery.
  - useState fed from an s const copy object needs an explicit <string> generic or TypeScript will narrow the setter too aggressively.
- Verification:
  - cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"adminLexicalAssetV4.ts" produced no matched errors.
  - cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand passed.
- Next steps / open questions:
  - Connect real backend sset-packages persistence so the panel no longer depends on local preview publish state.
  - Add a lightweight browser smoke pass on /admin/pipeline to validate the rebuilt panel interaction end to end.
## 2026-03-18 (V4 panel search + protocol guidance)
- Changed files + intent:
  - duoxx/components/admin/LexicalAssetImportPanel.tsx: added search filters for entries/senses/examples, upload guidance text, and kept publish/draft actions blocked when validation has blocking issues.
  - duoxx/src/config/adminLexicalAssetV4.ts: added upload guide and search placeholder copy so operator-facing protocol instructions stay out of JSX logic.
- Learned:
  - PowerShell source edits must use single-quoted literals when writing TypeScript template strings; otherwise ${...} fragments get eaten before they hit disk.
  - The V4 panel is now stable enough for incremental UX work as long as changes are validated with targeted 	sc after each edit batch.
- Verification:
  - cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"adminLexicalAssetV4.ts" produced no matched errors.
  - cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand passed.
- Next steps / open questions:
  - Add a browser smoke pass for /admin/pipeline to verify the rebuilt panel interaction and search UX on web.
  - Move backend asset-package status/audit rendering to a typed view model instead of directly reading raw summary fields in the component.
- 2026-03-18 (V4 issue focus)
  - Changed: `duoxx/components/admin/LexicalAssetImportPanel.tsx`.
  - Intent: make validation issues actionable by allowing operators to click an error/warning and jump to the related entry/sense/example/relation in the V4 lexical asset panel.
  - Learned: `ImportValidationIssue.block` already carries enough routing context (`file:assetType:assetId`) to drive local UI focus without adding new backend fields.
  - Verification: targeted `tsc` grep produced no matches for changed files; `npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (5/5).
  - Next: add real scroll-to-item/focus highlighting and then continue with backend `asset-packages` persistence.
- 2026-03-18 (V4 issue focus scroll/highlight)
  - Changed: `duoxx/components/admin/LexicalAssetImportPanel.tsx`.
  - Intent: improve issue-to-asset navigation by scrolling the three preview columns to the referenced asset and visually highlighting the focused entry/sense/example/relation.
  - Learned: list-level `ScrollView` refs plus `issue.block -> assetType:assetId` are sufficient for pragmatic operator navigation without introducing new data contracts.
  - Verification: targeted `tsc` grep produced no matches for changed files; `npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (5/5).
  - Next: if needed, replace approximate index-based scroll with exact row measurement, then continue backend `asset-packages` persistence.
- 2026-03-18 (V4 template Chinese header notes)
  - Changed: `duoxx/src/config/adminLexicalAssetV4.ts`.
  - Intent: add one Chinese explanation row above the CSV header row for all V4 lexical asset templates (`entries/senses/examples/relations`) so operators can understand each column order directly in the downloaded file.
  - Verification: targeted `tsc` grep produced no matches for `adminLexicalAssetV4.ts` / `LexicalAssetImportPanel.tsx`.
  - Next: if needed, shorten the explanation rows to avoid horizontal overflow in spreadsheet editors.
- 2026-03-18 (V4 template header simplification)
  - Changed: `duoxx/src/config/adminLexicalAssetV4.ts`.
  - Intent: simplify the Chinese explanation row above V4 CSV headers by removing repeated English field names and keeping only Chinese labels in matching column order.
  - Verification: targeted `tsc` grep produced no matches for `adminLexicalAssetV4.ts` / `LexicalAssetImportPanel.tsx`.
- 2026-03-18 (V4 ids auto-generated)
  - Changed: `duoxx/src/services/lexicalAssetImport.ts`, `duoxx/src/config/adminLexicalAssetV4.ts`, `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`.
  - Intent: stop requiring upstream generators to handcraft internal asset ids; make `entryId/senseId/exampleId/relationId` optional on import and let the standardizer auto-generate stable ids while supporting natural references like `light::noun::1`.
  - Learned: the clean boundary is `ids required internally, optional in import payload`; natural refs are enough to keep multi-file CSV packages linked without exposing system ids upstream.
  - Verification: targeted `tsc` grep produced no matches for changed files; `npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (6/6).
  - Next: surface this protocol more clearly in the V4 UI so operators know they can leave ids blank.
- 2026-03-18 (V4 UI id policy hint)
  - Changed: `duoxx/src/config/adminLexicalAssetV4.ts`, `duoxx/components/admin/LexicalAssetImportPanel.tsx`.
  - Intent: surface the new import contract directly in the V4 upload UI so operators know ids can be blank and natural references are allowed without opening the CSV templates first.
  - Verification: targeted `tsc` grep produced no matches for `adminLexicalAssetV4.ts` / `LexicalAssetImportPanel.tsx` / `lexicalAssetImport.ts`.
- 2026-03-19 (V4 staged upload + dependency-first validation)
  - Changed: `duoxx/components/admin/LexicalAssetImportPanel.tsx`, `duoxx/src/config/adminLexicalAssetV4.ts`, `duoxx/src/services/lexicalAssetImport.ts`, `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`.
  - Intent: make the V4 import flow explicit for operators by staging `entries/senses/examples/relations` in visible upload slots, requiring the three mandatory files before parse, and replacing cascaded foreign-key noise with root-cause dependency errors.
  - Learned: when a multi-file import has strict prerequisites, the UI must model file roles directly; a single generic picker causes incorrect mental models and noisy error states.
  - Verification: targeted `tsc` grep produced no matches for changed files; `npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (7/7).
  - Next: run a browser smoke pass on `/admin/pipeline` to verify the new staged upload UX, then continue backend `asset-packages` persistence.
## 2026-03-19 (V4 operator-review layout)
- Changed files + intent:
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: refactored the V4 lexical asset import UI from a technical 3-column asset view into an operator-review layout with a compact horizontal upload/status strip, clickable validation list, left-side entry rail, and right-side entry detail pane that shows all senses, per-sense examples, relations, and inline editing in one place.
  - `duoxx/src/services/lexicalAssetImport.ts`: added `LexicalEntryPreviewGroup` plus entry-centered grouping helpers so the UI can consume `entry -> senses -> examples/relations` instead of rendering raw arrays directly.
  - `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`: added grouping coverage for the new entry-centered preview model.
- Learned:
  - Operators need an entry-centered review surface, not separate `entries / senses / examples` columns; the true review unit is "one word and everything under it".
  - For this file, keeping user-facing Chinese copy as JS string literals with `\u` escapes is the safest way to avoid repeated Windows/PowerShell mojibake.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"lexicalAssetImport.ts" /C:"lexicalAssetImport.test.ts" /C:"adminLexicalAssetV4.ts"` produced no matched errors for the touched files.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (8/8).
- Next steps / open questions:
  - Run a browser smoke pass on `/admin/pipeline` to verify the compact upload strip and entry-detail review pane on real web layout.
  - Continue with backend `asset-packages` persistence so V4 asset publish is no longer local-preview-only.
## 2026-03-19 (V4 slot validation + parse-gated review)
- Changed files + intent:
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: refined the V4 import flow so global `������У��` only appears after `��ʼ����`; moved each CSV template button into its own upload slot; slot badges now switch from `����/��ѡ` to `���ϴ�/����`; per-slot header errors are shown immediately after file selection; fixed slot inspection call order and parse-state reset behavior.
  - `duoxx/src/services/lexicalAssetImport.ts`: added `inspectLexicalAssetUploadFile()` for immediate per-file contract checking before full package parse.
  - `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`: added coverage for immediate slot inspection and wrong-file-in-forced-slot detection.
- Learned:
  - The screenshoted `SENSE_HEADERS_INVALID` root cause was not parser randomness; it came from allowing a file with `examples` shape to be placed into the `senses` slot and only catching it later during package parse.
  - Operators need two layers of feedback: immediate slot-level format checks on upload, and package-level validation only after explicit parse.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"lexicalAssetImport.ts" /C:"lexicalAssetImport.test.ts" /C:"adminLexicalAssetV4.ts"` produced no matched errors for touched files.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (10/10).
- Next steps / open questions:
  - Run a browser smoke pass on `/admin/pipeline` to verify the new slot-level error UX and badge transitions on real uploads.
  - Consider adding file-name mismatch warnings when a known `examples.csv` filename is intentionally placed into the `senses` slot, even if the user clicks that slot manually.
## 2026-03-19 (V4 upload slots: immediate file checks + parse-gated validation)
- Changed files + intent:
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: changed V4 slot behavior so file-level feedback happens immediately on upload, while the global `������У��` section stays hidden until `��ʼ����`; moved each CSV template button into its corresponding upload slot; changed slot badges from `����/��ѡ` to runtime state (`���ϴ�` / `�д���`) after selection; reset stale parse preview state when files change.
  - `duoxx/src/services/lexicalAssetImport.ts`: formalized single-file slot inspection with header/empty-file checks so operators can detect wrong-file or wrong-header issues before full package parse.
  - `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`: added coverage for immediate slot inspection and forced-slot wrong-file detection.
- Learned:
  - There are two distinct validation layers in V4 and they must stay visually separate: upload-time slot inspection (cheap, local, per file) and parse-time package validation (cross-file, entry/sense/example graph aware).
  - The screenshot root cause is mostly UX, not parser correctness: when a user drops `examples.csv` into the `senses` slot, the current parser correctly reports `SENSE_HEADERS_INVALID`, but the UI previously delayed useful feedback and kept misleading `����` badges.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"lexicalAssetImport.ts" /C:"lexicalAssetImport.test.ts" /C:"adminLexicalAssetV4.ts"` produced no matched errors for touched files.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (10/10).
- Next steps / open questions:
  - Run a browser smoke pass on `/admin/pipeline` to verify the new slot-level error state and compact upload strip visually.
  - Continue backend `asset-packages` persistence so V4 publish no longer depends on local preview state.
## 2026-03-19 (V4 grouped sense preview + edit-on-demand)
- Changed files + intent:
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: flattened the standard asset preview so the right pane reads as `entry -> sense -> examples`; each sense now shows POS and translation as a grouped review unit, examples stay directly under that sense, and entry/sense/example editors only expand on explicit `�༭` clicks instead of rendering open inputs by default.
- Learned:
  - The operator review unit is not a raw asset row but `one word + its senses + the examples attached to each sense`; default-open editors hide that structure and waste height.
  - For admin pages, separating `preview` from `edit` is more effective than adding more visual containers.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"lexicalAssetImport.ts" /C:"lexicalAssetImport.test.ts" /C:"adminLexicalAssetV4.ts"` produced no matched errors for touched files.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (10/10).
- Next steps / open questions:
  - Run a browser smoke pass on `/admin/pipeline` to verify the grouped POS/translation/example layout reads cleanly on real web width.
  - Rename `��������������` to a more operator-facing label after the workflow wording is finalized.
## 2026-03-19 (V4 lexical facets + de-duplicated entry detail)
- Changed files + intent:
  - `duoxx/src/services/lexicalAssetImport.ts`: extended entry metadata so V4 standard assets can carry word-level rendering facets from `entries.csv` (`phrases`, `collocations`, `idioms`, `slang`, `spokenExpressions`) instead of forcing the UI to invent empty sections.
  - `duoxx/src/config/adminLexicalAssetV4.ts`: updated the `entries.csv` V4 template to include the new lexical facet columns.
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: removed duplicate entry display noise when `headword === lemma`, reduced top-line repetition, and added a compact lexical-facets section so the right pane can render word-level dimensions like phrases/collocations/slang/spoken expressions alongside grouped senses and examples.
  - `duoxx/src/services/__tests__/lexicalAssetImport.test.ts`: added coverage for parsing the new lexical facet columns.
- Learned:
  - The right-pane review surface needs two levels of information only: word-level lexical facets and sense-level meaning/examples; repeating lemma/headword metadata in multiple places makes the page feel larger without adding information.
  - If future rendering dimensions are expected, they must enter through the standard asset contract first; UI-only placeholders create false completeness.
- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"lexicalAssetImport.ts" /C:"lexicalAssetImport.test.ts" /C:"adminLexicalAssetV4.ts"` produced no matched errors for touched files.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalAssetImport.test.ts --runInBand` passed (11/11).
- Next steps / open questions:
  - Run a browser smoke pass to verify the flattened word-level facets section still reads compactly on real web width.
  - Consider whether `phrases/collocations/idioms/slang/spokenExpressions` should later move from entry metadata into first-class assets when cross-linking and recommendation need stronger structure.
## 2026-03-19 (V5 single-file lexical import)
- Changed files + intent:
  - `duoxx/src/services/lexicalSingleFileImport.ts`: introduced a new single-file lexical asset parser that accepts Markdown as the primary protocol and TXT/CSV as compatible inputs, then normalizes them into a human-readable review model (`word -> senses -> examples -> facets`) instead of exposing technical entry/sense/example objects.
  - `duoxx/src/config/adminLexicalSingleFile.ts`: added the V5 operator copy plus official single-file templates for Markdown, TXT, and CSV long-table import.
  - `duoxx/components/admin/LexicalAssetImportPanel.tsx`: rewrote the import panel into a single upload-and-review page; auto-parse on upload, hide internal ids/JSONL/downstream actions, show word list on the left and grouped word/sense/example/facet review on the right, and surface only human-readable validation messages.
  - `duoxx/components/admin/OperatorWorkbench.tsx`: removed the old V4/legacy course import branch from the default Import tab so Review Hub now exposes only the new single-file lexical import flow.
  - `duoxx/src/services/__tests__/lexicalSingleFileImport.test.ts`: added parser coverage for Markdown, TXT, CSV aggregation, missing CSV headers, and missing-example validation.
- Learned:
  - The operator review unit is `one word and everything under it`; any UI that exposes `entries/senses/examples` as first-class visible columns becomes technical and hard to use.
  - For this admin flow, the correct simplification is not “fewer buttons on the same data model�?but “one upload contract + one review shape + no downstream generation in the main page.�?- Verification:
  - `cmd /c .\duoxx\node_modules\.bin\tsc -p duoxx\tsconfig.json --noEmit --pretty false 2>&1 | findstr /I /C:"LexicalAssetImportPanel.tsx" /C:"lexicalSingleFileImport.ts" /C:"lexicalSingleFileImport.test.ts" /C:"adminLexicalSingleFile.ts" /C:"OperatorWorkbench.tsx"` produced no matched errors for the touched files. `findstr` returned `1` because there were no matching error lines.
  - `cmd /c npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (5/5).
- Next steps / open questions:
  - Run a browser smoke pass on `/admin/pipeline` to validate the new single-file import page and check real-world Markdown/TXT/CSV uploads.
  - Remove or archive the now-dead V4 import service/UI code paths after confirming nothing else still depends on them.
- 2026-03-19 (V5 error filter empty-state fix)
  - Changed: `duoxx/components/admin/LexicalAssetImportPanel.tsx`.
  - Intent: fix the review-pane bug where filtering by `有错误` with zero matching entries still fell back to showing the first word (`run`) on the right; the panel now shows an explicit empty state instead of a false selection.
  - Verification: targeted `tsc` grep for `LexicalAssetImportPanel.tsx` produced no matched errors; `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (5/5).
- 2026-03-19 (V5 top-row compact review layout)
  - Changed: `duoxx/components/admin/LexicalAssetImportPanel.tsx`.
  - Intent: place `词汇资产导入与审阅` and `解析错误与提示` on the same row after parsing to reduce vertical waste; left side stays primary, right side shows parse issues without taking a full extra section.
  - Learned: for this page, top-level layout should be single-column before parse and compact two-column after parse; leaving errors in a full-width second block wastes height.
  - Verification: targeted `tsc` grep for `LexicalAssetImportPanel.tsx` produced no matched errors.
- 2026-03-20 (V5 filter label clarification)
  - Changed: `duoxx/src/config/adminLexicalSingleFile.ts`.
  - Intent: rename the left-rail filter label from `可审阅` to `无错误` so it matches the actual logic (`issueCount === 0`) and avoids workflow ambiguity for operators.
  - Verification: targeted `tsc` grep for `adminLexicalSingleFile.ts` / `LexicalAssetImportPanel.tsx` produced no matched errors.
- 2026-03-20 (V5 translated lexical facets)
  - Changed: `duoxx/src/services/lexicalSingleFileImport.ts`, `duoxx/src/config/adminLexicalSingleFile.ts`, `duoxx/components/admin/LexicalAssetImportPanel.tsx`, `duoxx/src/services/__tests__/lexicalSingleFileImport.test.ts`.
  - Intent: upgrade lexical facet fields (`phrases`, `collocations`, `sentencePatterns`, `spokenExpressions`, `slang`, `idioms`) from raw string lists to structured `text + translationZh` items, following the agreed `英文 || 中文翻译` protocol instead of burying Chinese glosses inside free text.
  - Learned: the right parsing rule is `;;` between items and `||` inside one item; splitting on a bare `|` must stay only as backward-compatible fallback for legacy untranslated lists.
  - Verification: `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (5/5). Targeted `tsc` grep for the touched files produced no matched error lines; `findstr` returned `1` only because there were no matches.
- 2026-03-20 (Operator Review Hub duplicate title removal)
  - Changed: `duoxx/components/admin/OperatorWorkbench.tsx`.
  - Intent: remove the redundant inner `Operator Review Hub` title from the workbench content area and keep only the modal/header title rendered by `PipelineDashboard`.
  - Learned: the duplicate title came from `PipelineDashboard.tsx` (modal header) plus `OperatorWorkbench.tsx` (content header); deleting only the inner title does not affect tab layout because the tabs start below the shared header row.
  - Verification: targeted `tsc` grep for `OperatorWorkbench.tsx` / `PipelineDashboard.tsx` produced no matched errors.
- 2026-03-20 (V5 richer template examples)
  - Changed: `duoxx/src/config/adminLexicalSingleFile.ts`.
  - Intent: expand the official single-file templates so users can see how to express multiple senses, multiple examples, multiple lexical facets, and phrase-like headwords in one file instead of guessing from a one-line example.
  - Learned: for operator-facing templates, example coverage matters as much as schema definition; if the template only shows one case, users will assume the protocol only supports one case.
  - Verification: `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (5/5). Targeted `tsc` grep for `adminLexicalSingleFile.ts` / `lexicalSingleFileImport.ts` / `LexicalAssetImportPanel.tsx` / `lexicalSingleFileImport.test.ts` produced no matched error lines.
- 2026-03-20: Added attached quiz support to V5 single-file lexical assets.
  - Files: duoxx/src/services/lexicalSingleFileImport.ts, duoxx/src/config/adminLexicalSingleFile.ts, duoxx/components/admin/LexicalAssetImportPanel.tsx, duoxx/src/services/__tests__/lexicalSingleFileImport.test.ts
  - Intent: allow Markdown/TXT/CSV lexical templates to carry optional quiz blocks under each sense while keeping knowledge assets and exercises logically separated.
  - Learned: current V5 parser already had quiz parsing scaffolding; main gaps were validation, template examples, and review rendering.
  - Next: if this protocol is kept, decide whether attached quizzes remain review-only or should later be split into formal exercise candidates.
- 2026-03-20: Added explicit progress-doc maintenance rule and created `PROJECT_PROGRESS.md`.
  - Files: AGENTS.md, AUTOPILOT_WORKFLOW.md, PROJECT_PROGRESS.md
  - Intent: make progress tracking mandatory after each completed task so operators and future sessions can see current status without scanning the full memory log.
  - Learned: `PROJECT_MEMORY.md` is better for session history; `PROJECT_PROGRESS.md` is better for current state and next actions.
  - Next: keep both documents updated together whenever a task finishes.
- 2026-03-20: Documented dynamic quiz persistence and admin review flow.
  - Files: duoxx/docs/lexical-quiz-lifecycle-v1.md, PROJECT_PROGRESS.md
  - Intent: define where AI-updated quizzes should persist (separate from lexical assets) and how admins should review seed/generated/active quiz layers.
  - Learned: attached quizzes in V5 should remain seed data until a separate versioned quiz store and active binding layer exist.
  - Next: implement backend persistence for generated quiz versions and an admin review surface beyond lexical import review.
- 2026-03-20: Implemented local dynamic quiz versioning and review chain for V5 lexical assets.
  - Files: duoxx/src/models/lexicalQuiz.ts, duoxx/src/storage/lexicalQuizReviewStorage.ts, duoxx/src/storage/__tests__/lexicalQuizReviewStorage.test.ts, duoxx/src/services/pipelineApi.ts, duoxx/components/admin/LexicalAssetImportPanel.tsx, duoxx/components/admin/LexicalQuizReviewPanel.tsx, duoxx/components/admin/OperatorWorkbench.tsx, PROJECT_PROGRESS.md
  - Intent: move attached quizzes from review-only display toward a real versioned lifecycle: persist seed quizzes from lexical review, generate new quiz versions, approve/reject them, activate one current version, and mark related versions stale when source senses change.
  - Learned: the right boundary is `Import & Review` for content truth and `Exercise Review` for quiz lifecycle operations; keeping both concerns in one page would make the operator flow harder to reason about.
  - Verification: targeted `tsc` grep for `pipelineApi.ts`, `LexicalAssetImportPanel.tsx`, `LexicalQuizReviewPanel.tsx`, `lexicalQuizReviewStorage.ts`, `lexicalQuizReviewStorage.test.ts`, and `OperatorWorkbench.tsx` produced no matched error lines; `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (7/7); `npm --prefix duoxx test -- src/storage/__tests__/lexicalQuizReviewStorage.test.ts --runInBand` passed (4/4).
  - Next: replace the local AsyncStorage-backed quiz lifecycle with backend collections and make the learning side read `activeQuizBindings` with seed fallback.


## 2026-03-20 (Mongo/local fallback visibility + stable Exercise Review copy)
- What changed:
  - Extended `duoxx/src/services/apiClient.ts` to allow response metadata.
  - Updated `duoxx/src/services/pipelineApi.ts` so lexical quiz import/list calls annotate whether data came from backend Mongo or local fallback.
  - Updated `duoxx/components/admin/LexicalAssetImportPanel.tsx` to show explicit save result text: backend Mongo vs local fallback.
  - Integrated the Exercise Review copy rewrite through `duoxx/components/admin/LexicalQuizReviewPanel.tsx` and `duoxx/src/config/adminLexicalQuizReview.ts`, including stable Chinese labels for filters, statuses, quiz types, and generation reasons.
- What was learned:
  - The earlier empty Mongo symptom is operationally indistinguishable from a silent local fallback unless the UI exposes storage source.
  - Exercise Review mojibake was a display-layer/source-copy problem; keeping all operator copy in a dedicated config is the safer pattern.
- Next steps / open questions:
  - Have the operator repeat `�����ʲ��뱣����` after backend restart and verify the status line says backend Mongo.
  - If item content still looks garbled after the panel fix, inspect and clean old imported quiz records rather than the panel code.

## 2026-03-20 (lexical quiz API route correction + import tab session restore)
- What changed:
  - Fixed lexical quiz frontend API paths in `duoxx/src/services/pipelineApi.ts` from bare `/pipeline/...` and `/learning/...` to `/api/pipeline/...` and `/api/learning/...`, matching the actual backend routing.
  - Added explicit response-source metadata handling so the UI can distinguish backend Mongo success from local fallback.
  - Updated `duoxx/components/admin/LexicalAssetImportPanel.tsx` to surface backend-vs-local save results in the status line.
  - Integrated the import-session persistence fix for `Import & Review`, so switching to `Exercise Review` and back no longer drops the just-imported lexical review state.
  - Integrated the `Exercise Review` UTF-8 copy/config rewrite from the subagent.
- What was learned:
  - The empty Mongo symptom was caused by a concrete route mismatch (`/pipeline/...` 404) rather than missing backend models.
  - Tab switching really unmounts the import panel in `OperatorWorkbench`, so review state must be persisted outside component memory.
- Next steps / open questions:
  - Run a live save after frontend hot reload and backend restart, then verify `lexicalAssets` and `seedQuizzes` in Mongo.
  - If old quiz content itself is still garbled, clear or re-import stale records; the panel copy is now fixed.

## 2026-03-20 (Import & Review left list density pass)
- What changed:
  - Compressed the left-hand lexical entry list in `LexicalAssetImportPanel.tsx` by moving `词性x | 例句x` onto the title row and reducing item padding/margins.
  - Kept the layout flat and left all parsing/saving behavior unchanged.
- What was learned:
  - The left rail was duplicating summary information that already exists in the right detail pane, which made the review list taller than necessary.
- Next steps / open questions:
  - If the operator still wants denser scanning, tighten the lemma line spacing next and consider reducing the badge footprint.
## 2026-03-20 (Import review list density pass)
- What changed:
  - Updated `duoxx/components/admin/LexicalAssetImportPanel.tsx` so each word row shows `���� x | ���� x` inline with the headword.
  - Reduced left-rail row spacing to save vertical space in `Import & Review`.
- What was learned:
  - The operator primarily scans word rows for density, not cards; moving counts onto the title row removes one full line per item.
- Next steps / open questions:
  - Confirm in-browser whether very long headwords or count labels need truncation instead of wrap.
## 2026-03-20 (lexical quiz review remove seed/fallback concept from UI)
- What changed:
  - Updated `duoxx/components/admin/LexicalAssetImportPanel.tsx` to remove the visible `������` concept from the import flow.
  - Renamed the primary action to `�����ʲ�`, removed the seed quiz count display, and simplified status text to only describe asset saving and stale-marking.
  - Updated `duoxx/components/admin/LexicalQuizReviewPanel.tsx` to remove the `seed` review view filter and hide seed-specific generation reason/promotion text in the detail pane.
  - Updated `duoxx/src/config/adminLexicalQuizReview.ts` to drop seed-related review labels and the promotion prefix from UI copy.
- What was learned:
  - The backend/local storage still contains legacy seed quiz records, but the UI does not need to expose that internal concept for reviewers.
- Next steps / open questions:
  - Optional: if desired later, rename backend storage/API identifiers away from `seed` as a separate cleanup, but that is not required for the current UI simplification.
## 2026-03-20 (Exercise Review compactness + version-copy cleanup)
- What changed:
  - Tightened `duoxx/components/admin/LexicalQuizReviewPanel.tsx` row spacing and reduced section padding to make the left grouped list denser.
  - Normalized display labels so pipe-delimited fragments like `| v1` no longer leak into the UI.
  - Updated `duoxx/src/config/adminLexicalQuizReview.ts` to add missing selection/help labels and rename active-state wording to `当前题` / `已替换`.
- What was learned:
  - The review panel can stay versioned internally while presenting a question-centric operator UI.
- Next steps / open questions:
  - Browser smoke test the compact list with real imported assets and adjust only if long labels wrap too aggressively.

## 2026-03-20 (learning-side active lexical quiz adapter)
- What changed:
  - Added `duoxx/src/services/lexicalQuizLessonAdapter.ts` plus `duoxx/src/services/__tests__/lexicalQuizLessonAdapter.test.ts` to convert active lexical quiz records into `LessonEngine`-compatible lesson/exercise data.
  - Extended `duoxx/src/services/learningApi.ts` with `getActiveLexicalQuizzes`.
  - Updated `duoxx/app/lesson-exercise/[lessonId].tsx` so vocab lesson routes try active lexical quizzes first, then safely fall back to the existing lesson payload if none are usable.
- What was learned:
  - The lowest-risk learner-side integration is to treat active lexical quizzes as another lesson data source instead of rewriting `LessonEngine` or the exercise render path.
  - `multiple_choice` and `true_false` can be adapted immediately; `word_card` needs a separate interaction decision rather than being shoved through the current renderer.
- Verification:
  - `npm --prefix duoxx test -- src/services/__tests__/lexicalQuizLessonAdapter.test.ts --runInBand` passed (3/3).
  - `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (7/7).
  - `npm --prefix duoxx test -- src/storage/__tests__/lexicalQuizReviewStorage.test.ts --runInBand` passed (4/4).
  - Targeted `tsc` grep for `lesson-exercise/[lessonId].tsx`, `learningApi.ts`, `lexicalQuizLessonAdapter.ts`, and `lexicalQuizLessonAdapter.test.ts` produced no matched error lines.
- Next steps / open questions:
  - Decide whether to support `word_card` on the learner side or disallow marking it as current until a renderer exists.
  - Do one real browser smoke test with an active lexical multiple-choice item to confirm the lesson route now surfaces reviewed Mongo-backed questions.

## 2026-03-20 (learning-side word_card support via adapter)
- What changed:
  - Extended `duoxx/src/services/lexicalQuizLessonAdapter.ts` so `word_card` active quizzes are now adapted into learner-side multiple-choice meaning questions.
  - Updated `duoxx/src/services/__tests__/lexicalQuizLessonAdapter.test.ts` to cover `word_card` mapping and fallback behavior.
- What was learned:
  - The fastest safe path is to treat `word_card` as a meaning-recognition multiple-choice item in the learner, instead of introducing a new renderer midstream.
  - This keeps the active lexical quiz path unified under the existing `MultipleChoiceExercise` component.
- Verification:
  - `npm --prefix duoxx test -- src/services/__tests__/lexicalQuizLessonAdapter.test.ts --runInBand` passed (4/4).
  - `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (7/7).
  - `npm --prefix duoxx test -- src/storage/__tests__/lexicalQuizReviewStorage.test.ts --runInBand` passed (4/4).
  - Targeted `tsc` grep for `lesson-exercise/[lessonId].tsx`, `learningApi.ts`, `lexicalQuizLessonAdapter.ts`, and `lexicalQuizLessonAdapter.test.ts` produced no matched error lines.
- Next steps / open questions:
  - Decide whether this `word_card -> multiple_choice` mapping is the final UX or just a temporary bridge before a dedicated flashcard interaction.

## 2026-03-20 (lexical quiz V2: knowledge-only templates + translation_choice as formal type)
- What changed:
  - Rewrote `duoxx/src/config/adminLexicalSingleFile.ts` so the official Markdown/TXT/CSV templates now only demonstrate knowledge assets and no longer encourage hand-written quiz blocks.
  - Rewrote `duoxx/components/admin/LexicalAssetImportPanel.tsx` into a clean UTF-8 version, removed attached-quiz rendering from the review UI, and kept the page focused on lexical asset reading plus asset save feedback.
  - Updated `duoxx/src/models/lexicalQuiz.ts`, `duoxx/src/storage/lexicalQuizReviewStorage.ts`, `duoxx/components/admin/LexicalQuizReviewPanel.tsx`, `duoxx/src/config/adminLexicalQuizReview.ts`, `duoxx/src/services/lexicalQuizLessonAdapter.ts`, and the matching tests so `translation_choice` is now the formal generated word-meaning exercise type; new generation no longer emits `word_card`.
  - Updated backend generation logic in `duoxx_server/src/services/lexicalQuizService.js` so new lexical question generation produces `translation_choice` with 4-option Chinese translation distractors, and returns a clear error when distractors are insufficient.
- What was learned:
  - The cleanest V2 boundary is: templates carry knowledge only; exercises are system-generated around a sense.
  - Keeping legacy parser support is acceptable as long as the official template and operator UI stop presenting hand-written quiz as the primary workflow.
  - `translation_choice` can reuse the existing multiple-choice learner renderer without introducing a new front-end exercise component.
- Verification:
  - `npm --prefix duoxx test -- src/services/__tests__/lexicalQuizLessonAdapter.test.ts --runInBand` passed (4/4).
  - `npm --prefix duoxx test -- src/storage/__tests__/lexicalQuizReviewStorage.test.ts --runInBand` passed (4/4).
  - `npm --prefix duoxx test -- src/services/__tests__/lexicalSingleFileImport.test.ts --runInBand` passed (7/7).
  - Targeted `tsc` grep for `LexicalAssetImportPanel.tsx`, `LexicalQuizReviewPanel.tsx`, `adminLexicalSingleFile.ts`, `adminLexicalQuizReview.ts`, `lexicalQuizLessonAdapter.ts`, `lexicalQuizReviewStorage.ts`, `lexicalQuiz.ts`, and the related tests produced no matched error lines.
  - A direct Node `require` check confirmed `duoxx_server/src/services/lexicalQuizService.js` still exports a callable `generateQuizVersions`.
- Next steps / open questions:
  - Run a live browser/Mongo smoke test for the exact V2 flow: import knowledge-only markdown, save assets, generate a translation-choice quiz, approve it, and confirm the learner reads it.
  - Decide later whether to physically delete legacy quiz parsing from `lexicalSingleFileImport.ts` once old imported files are no longer needed.

## 2026-03-20 (Lexical practice answer-validation investigation)
- What changed:
  - Updated `duoxx/src/services/lexicalQuizLessonAdapter.ts` to normalize translation-choice options on read:
    - legacy `word_card` distractors now ignore non-Chinese answers such as `true/false`
    - translation-like options are deterministically rotated so the correct answer is not always in slot A
  - Updated `duoxx/src/storage/lexicalQuizReviewStorage.ts` so newly generated `translation_choice` questions are stored with rotated option order.
  - Updated backend generation in `duoxx_server/src/services/lexicalQuizService.js` so Mongo-generated `translation_choice` questions use the same rotated option order.
  - Expanded `duoxx/src/services/__tests__/lexicalQuizLessonAdapter.test.ts` with:
    - legacy distractor pollution regression coverage
    - explicit wrong-answer-must-fail coverage
  - Updated `duoxx/src/storage/__tests__/lexicalQuizReviewStorage.test.ts` to assert generated translation-choice options are no longer answer-first.
- What was learned:
  - The user-reported ��all answers are correct�� symptom was not reproducible in the learner screen; browser verification showed wrong answers still fail.
  - The real root causes were stale lexical option construction rules: polluted legacy distractors and overly predictable answer placement.
  - Active legacy `word_card` rows already stored in Mongo can still be read safely because the learner adapter now normalizes them at runtime.
- Next steps / open questions:
  - Decide whether to run a one-off migration for existing Mongo `translation_choice` / promoted legacy `word_card` records so review-side stored options also match the new order.

## 2026-03-20 (translation-choice distractor quality upgrade)
- What changed:
  - Tightened `duoxx_server/src/services/lexicalQuizService.js` and `duoxx/src/storage/lexicalQuizReviewStorage.ts` so translation-choice distractors are now ranked by same part of speech, difficulty proximity, and scene-tag overlap.
  - Filtered distractors to translation-like Chinese text only; ASCII-only `true/false` style values are excluded from translation-choice pools.
  - Added backend and local-storage tests that verify the higher-quality distractor selection path.
- What was learned:
  - The generator can stay simple at the API boundary while still producing better questions by scoring candidates before option rotation.
  - Keeping the backend and local fallback aligned avoids a split-brain quiz experience when the API is unavailable.
- Next steps / open questions:
  - If older stored quiz rows should be rewritten too, add a separate data cleanup later; do not mix that with the generator rule change.


## 2026-03-23 (structured-learning skill/workflow)
- What changed:
  - Added `.agents/skills/structured-learning-curator/SKILL.md`.
  - Added `.agents/workflows/structured-learning-import.md`.
  - Added `STRUCTURED_LEARNING_CSV_SPEC.md`.
- What was learned:
  - Existing import contracts already cover the right database shape for front-end loading.
  - For mixed CSVs like `15��.csv`, the safest default is one lesson per `category`, with `example` expanded into individual knowledge cards and uncertain rows flagged for review.
- Next steps / open questions:
  - If runtime conversion is needed, add a dedicated importer that emits the existing course CSV package format.
## 2026-03-26 (Learning tab three-section IA refactor)
- What changed:
  - Refactored `duoxx/app/(tabs)/index.tsx` non-parent mode into three explicit sections: `��ѧϰ·��` / `����븴ϰ` / `ѧϰ��Դ`.
  - Introduced `LearningCard` type to unify home card data shape.
  - Reorganized card data into `growthTrackCards`, `diagnosticReviewCards`, `learningResourceCards`, and merged resource rendering via `resourceCards`.
  - Removed duplicate progress-entry card from resource list and removed the old `ѧϰͳ��` block from the home tab to reduce first-screen noise.
  - Added visual step index badges for main path cards and compacted diagnostic action cards.
- What was learned:
  - The previous home layout had repeated routes (`/progress-dashboard`) and too many same-weight blocks, weakening task-first behavior.
  - Converging to three semantic sections is a low-risk refactor because existing route actions can be preserved while reducing cognitive load.
- Next steps / open questions:
  - Run device-level UX check (adult + child mode) for spacing and action discoverability.
  - Decide whether to move `����ֵ` into a dedicated gamification panel if the diagnostic section still feels dense on small screens.
## 2026-03-26 (Learning tab compact layout tuning)
- What changed:
  - Added responsive compact-mode logic in `duoxx/app/(tabs)/index.tsx` using `useWindowDimensions` with threshold `width < 390`.
  - In compact mode, diagnostic summary cards and diagnostic action buttons switch to stacked vertical layout for readability.
  - Added compact spacing variants for section paddings, headers, avatar/hearts blocks, and quick-start area.
  - Child/student avatar size now scales from `large` to `medium` on compact screens.
  - Heart icon size now adapts (`24` -> `22`) for tighter but clear display in compact mode.
- What was learned:
  - The three-section IA works better when narrow-screen layout is explicitly handled; relying on default flex row compression creates readability loss in diagnostic cards.
- Next steps / open questions:
  - Validate visual balance on 360/375 width devices in both adult and child modes.
  - Decide whether compact mode should also hide secondary subtitles in resource cards for even faster scanning.
## 2026-03-26 (Home tab minimal IA + deferred entry registry)
- What changed:
  - Rebuilt `duoxx/app/(tabs)/index.tsx` non-parent home into a minimal IA with only 3 visible primary blocks: `ѧϰ��֪ʶ��AI�Ƽ���`, `��ϰ��֪ʶ`, and `չ������`.
  - Added `showMoreMenu` collapse/expand state so secondary features are hidden by default and only revealed on demand.
  - Added a preserved secondary-entry registry in code (`moreCards`) for routes that were previously visible but now intentionally deferred behind `����`.
- What was learned:
  - The previous layout overload came from exposing too many same-priority actions at once; moving secondary actions behind one controlled expansion keeps clarity while retaining capability.
- Next steps / open questions:
  - Validate whether learners still discover `����ѵ��` and `�����޸�` at expected rates after moving them behind `����`.
  - If discoverability drops, consider pinning one adaptive secondary item under `��ϰ��֪ʶ` as contextual suggestion.

## 2026-03-27 (CEO review retry: prioritized improvement map)
- What changed:
  - Re-validated project baseline using existing lint and TypeScript logs.
  - Consolidated a P0/P1/P2 improvement roadmap based on current blockers and product direction.
  - Recorded the roadmap in PROJECT_PROGRESS.md for next-session continuity.
- What was learned:
  - The biggest delivery risk is currently engineering baseline debt (81 lint errors + 25 TS errors), not feature idea scarcity.
  - Product direction has improved (home IA simplification), but consistency and speed now depend on restoring a green technical baseline.
  - Data quality must be enforced at import/publish contract boundaries to avoid recurring UI-side cleanup cycles.
- Next steps / open questions:
  - Decide whether the next sprint is strictly P0 stability (recommended) or mixed with additional feature work.
  - After P0, run focused design-system pass to unify admin + learner UI tokens and interaction patterns.
## 2026-03-27 (P0 implementation: compile/lint error burn-down)
- What changed:
  - Fixed TS blockers in `duoxx` app and module contracts:
    - `src/modules/user-profile/index.ts` (removed conflicting re-export surface)
    - `src/modules/vocab-recognition/index.ts` (fixed invalid `units` exports)
    - `src/modules/listening-speaking/screens/ListeningSpeakingScreen.tsx` (provider callback type aligned to `LSUnit | null`)
    - `src/services/avatarAchievementIntegration.ts` (expression mapping aligned to `ExpressionId`)
    - `app/achievements.tsx`, `app/placement-test.tsx`, `app/settings.tsx`, `app/share-achievement.tsx` (state/type mismatches fixed)
    - `src/components/feedback/MilestoneModal.tsx` (removed hard dependency on `expo-blur` by using `View` overlay)
  - Added missing typed routes for survival phrases:
    - `app/modules/survival-phrases/index.tsx`
    - `app/modules/survival-phrases/pack/[packId].tsx`
    - `app/modules/survival-phrases/phrase/[phraseId].tsx`
    - `app/modules/survival-phrases/practice/[phraseId].tsx`
  - Cleared major lint errors by resolving duplicate export surfaces and unescaped entities:
    - `src/modules/speaking-avatar/index.ts`
    - `src/modules/sentence-building/index.ts`
    - `src/components/exercises/IdiomSlangExercise.tsx`
    - `src/modules/imperfect-dialogue/components/FeedbackPanel.tsx`
    - `src/modules/survival-phrases/components/SearchBar.tsx`
    - `src/modules/speaking-avatar/services/TTSService.ts`
- What was learned:
  - Most blocking failures came from barrel export collisions and route-file drift, not deep logic bugs.
  - Keeping route files physically present in `app/modules/*` is required for Expo Router typed-route stability.
  - Dynamic optional dependencies like `expo-speech` need explicit lint handling when not guaranteed in all environments.
- Verification:
  - `cmd /c npx tsc --noEmit` -> pass.
  - `npm run lint` -> `0 errors, 248 warnings`.
- Next steps / open questions:
  - Decide whether to install `expo-speech` as a hard dependency or keep it optional with dynamic load/fallback policy.
  - Run operator + learner smoke path to confirm no runtime regressions on pipeline workflows.

## 2026-03-27 (Lint warning burndown to zero + stability verification)
- What changed:
  - Cleared remaining lint warnings across hook deps, unused imports/vars, and test typing in:
    - `duoxx/app/error-detail.tsx`
    - `duoxx/app/scene-course/[sceneId].tsx`
    - `duoxx/src/components/celebration/CelebrationScreen.tsx`
    - `duoxx/src/components/feedback/AchievementToast.tsx`
    - `duoxx/src/components/feedback/MilestoneModal.tsx`
    - `duoxx/src/modules/dynamic-lexicon/components/AchievementBadges.tsx`
    - `duoxx/src/modules/dynamic-lexicon/components/ChunkExercise.tsx`
    - `duoxx/src/modules/dynamic-lexicon/components/VocabMeter.tsx`
    - `duoxx/src/modules/dynamic-lexicon/screens/DynamicLexiconScreen.tsx`
    - `duoxx/src/modules/imperfect-dialogue/components/DialogueAvatarDemo.tsx`
    - `duoxx/src/modules/speaking-avatar/components/SpeakingAvatarView.tsx`
    - `duoxx/src/modules/survival-phrases/screens/SurvivalPhrasesScreen.tsx`
    - plus targeted test files under `speaking-avatar`, `imperfect-dialogue`, `survival-phrases`.
  - Resolved two runtime parser breakages caused by malformed text literals in:
    - `duoxx/app/modules/listening-speaking/[unitId].tsx`
    - `duoxx/src/modules/dynamic-lexicon/components/ChunkExercise.tsx`
  - Updated test mocks to avoid Jest hoist/TDZ failures by inlining `Animated.Value` mock classes inside `jest.mock` factories.
- What was learned:
  - A large portion of residual warnings came from hook-deps rules on mount-only animation/setup effects; handling needed either dependency-complete callbacks or explicit lint intent comments.
  - When files contain mixed/legacy encoded literals, small line edits can accidentally introduce unterminated JSX/text nodes; lint + parser checks are the fastest safety net.
  - Jest mock hoisting can break top-level class references inside `jest.mock` factories; embedding mock classes inside the factory is the most robust pattern.
- Verification:
  - `cmd /c npm run lint` -> pass (0 warnings, 0 errors).
  - `cmd /c npx tsc --noEmit` -> pass.
  - `cmd /c npx jest ... --runInBand` (4 targeted suites) -> pass.
- Next steps / open questions:
  - Run a broader learner-flow smoke path (home -> module -> submit -> feedback) and admin pipeline smoke path to catch any UI text/encoding regressions not covered by tests.
## 2026-04-02 (Add development-phase constitution)
- What changed:
  - Updated `AGENTS.md` by adding `Development-Phase Constitution (Mandatory)`.
  - Added explicit rules: avoid minimal-diff bias, avoid workaround/fallback padding, prioritize architecture correctness and code simplicity.
- What was learned:
  - The project now explicitly requires development-phase decisions to favor clean architecture over short-term patches.
- Next steps / open questions:
  - Apply this constitution consistently in upcoming implementation tasks.
## 2026-04-02 (AGENTS.md structure optimization)
- What changed:
  - Optimized AGENTS.md structure for execution clarity.
  - Promoted Long-Term Memory to a mandatory heading and removed duplicated persistence details from protocol step 6.
  - Added Rule Priority (Mandatory) to resolve rule conflicts deterministically.
  - Clarified command execution scope: build/test commands run in duoxx/ by default.
  - Replaced the file-organization tree with an ASCII-safe version to avoid encoding artifacts.
- What was learned:
  - The previous document had small but meaningful execution friction (duplicate rules, missing precedence, and directory ambiguity).
- Next steps / open questions:
  - Keep this structure stable and only evolve constitutions via explicit rule updates.
## 2026-04-02 (Sentence Insight Pack V1: schema + index + UI sentence mode)
- What changed:
  - Implemented Sentence Insight Pack V1 contract and validation gate in `duoxx/src/services/lexicalSingleFileImport.ts`:
    - Added `SentenceInsightPackV1` model family and required-module gate (`structure`, `chunks`, `grammarPoints`, `collocations`).
    - Added deterministic `sentenceId` generation (`buildStableSentenceId`) and pack validator (`validateSentenceInsightPackV1`).
  - Implemented sentence insight indexing and sentence-mode resolution in `duoxx/src/modules/knowledge-absorb/knowledgeAbsorbEngine.ts`:
    - Added module registry ordering and sentence insight index build/read APIs.
    - Added sentence lookup maps (`sentenceNodeIdBySentenceId`, `sentenceNodeIdByExampleKey`) and resolver for example->sentence jump.
    - Extended word example presentation with `exampleKey` + `sentenceId` for front-end triggering.
  - Implemented knowledge absorb sentence-detail interaction in `duoxx/app/knowledge-absorb.tsx`:
    - Added per-example `详解` action (only shown when insight pack exists).
    - Added sentence-mode main card switch and `返回原词` anchor behavior.
    - Added module-tab drawer rendering driven by registry, with no client-side fallback assembly.
  - Restored and hardened targeted tests:
    - `duoxx/src/services/__tests__/lexicalSingleFileImport.test.ts`
    - `duoxx/src/modules/knowledge-absorb/knowledgeAbsorbEngine.test.ts`
- What was learned:
  - The `sentenceId = normalize(sentence) + sourceSenseKey` stable hash path is sufficient to bridge offline insight packs and in-app sentence nodes without new storage workarounds.
  - Registry-driven module rendering cleanly isolates future module growth from page-state logic.
- Verification:
  - `npx eslint app/knowledge-absorb.tsx src/modules/knowledge-absorb/knowledgeAbsorbEngine.ts src/modules/knowledge-absorb/knowledgeAbsorbEngine.test.ts src/services/__tests__/lexicalSingleFileImport.test.ts --max-warnings=0` -> pass.
  - `npm test -- src/services/__tests__/lexicalSingleFileImport.test.ts src/modules/knowledge-absorb/knowledgeAbsorbEngine.test.ts` -> pass (18 tests).
  - `npx tsc --noEmit` -> pass.
- Next steps / open questions:
  - Integrate offline batch exporter/importer job to produce and ingest full sentence insight packs at scale.
  - Add UI-level integration/e2e coverage for sentence-mode switching under multi-POS and pagination scenarios.
